# .gitignore 检查报告

生成时间: 2026-01-18

## 检查目的

检查 `.gitignore` 文件是否误忽略了应该被跟踪的重要文件。

---

## ⚠️ 发现的问题

### 1. `deploy/` 目录被完全忽略

**问题**: `.gitignore` 第 290 行包含 `deploy/`，导致整个 `deploy/` 目录被忽略。

**影响**:
- ❌ `deploy/scripts/*.sql` - 数据库修复脚本（应该被跟踪）
- ❌ `deploy/init-scripts/*.sql` - 数据库初始化脚本（应该被跟踪）
- ✅ `deploy/` 下的其他临时文件可以忽略

**建议修复**:
```gitignore
# 只忽略 deploy/ 下的特定文件，而不是整个目录
deploy/*.log
deploy/tmp/
deploy/temp/
# 但保留 deploy/scripts/ 和 deploy/init-scripts/
!deploy/scripts/
!deploy/init-scripts/
```

### 2. `package-lock.json` 被忽略

**问题**: `.gitignore` 第 76 行包含 `package-lock.json`。

**影响**:
- ❌ 无法锁定 npm 依赖版本
- ❌ 不同开发者可能使用不同版本的依赖
- ❌ CI/CD 构建可能不稳定

**建议**: 
- **应该跟踪** `package-lock.json`（npm 官方推荐）
- 如果使用 yarn，应该跟踪 `yarn.lock`
- 如果使用 pnpm，应该跟踪 `pnpm-lock.yaml`

**修复建议**:
```gitignore
# 移除或注释掉以下行：
# package-lock.json
# yarn.lock
# pnpm-lock.yaml
```

### 3. `scripts/` 目录被完全忽略

**问题**: `.gitignore` 第 291 行包含 `scripts/`。

**影响**:
- ❌ `scripts/remove-docs-kiro.sh` - 工具脚本（应该被跟踪）
- ❌ `scripts/remove-tracked-ignored-files.sh` - 工具脚本（应该被跟踪）
- ❌ 其他自动化脚本（应该被跟踪）

**建议修复**:
```gitignore
# 只忽略 scripts/ 下的临时文件，而不是整个目录
scripts/*.tmp
scripts/*.log
# 但保留 .sh 和 .ps1 脚本
!scripts/*.sh
!scripts/*.ps1
```

---

## ✅ 正确忽略的文件

以下文件/目录被正确忽略（应该忽略）：

- ✅ `target/` - Maven 编译输出
- ✅ `node_modules/` - npm 依赖
- ✅ `logs/` - 日志文件
- ✅ `*.log` - 日志文件
- ✅ `.DS_Store` - macOS 系统文件
- ✅ `.env` - 环境变量文件（包含敏感信息）
- ✅ `*.pem`, `*.key` - 密钥文件
- ✅ `docs/` - 文档目录（用户要求忽略）
- ✅ `.kiro/` - AI 日志目录（用户要求忽略）

---

## 🔧 修复建议

### 方案 1: 精确控制（推荐）

更新 `.gitignore`，使用更精确的规则：

```gitignore
# =============================================================================
# EXTERNAL: SCRIPTS
# =============================================================================
# 只忽略 scripts/ 下的临时文件
scripts/*.tmp
scripts/*.log
scripts/temp/
# 但保留脚本文件
!scripts/*.sh
!scripts/*.ps1

# =============================================================================
# DEPLOY: 只忽略临时文件，保留重要脚本
# =============================================================================
deploy/*.log
deploy/tmp/
deploy/temp/
deploy/*.bak
# 但保留重要脚本和初始化脚本
!deploy/scripts/
!deploy/init-scripts/
!deploy/**/*.sql

# =============================================================================
# Node.js / Frontend - 保留锁定文件
# =============================================================================
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
# 保留锁定文件以确保依赖版本一致
# package-lock.json  # 注释掉，应该被跟踪
# yarn.lock          # 注释掉，应该被跟踪
# pnpm-lock.yaml     # 注释掉，应该被跟踪
```

### 方案 2: 保持现状但添加例外

如果不想大幅修改，可以添加例外规则：

```gitignore
# =============================================================================
# EXTERNAL: SCRIPTS
# =============================================================================
scripts/
!scripts/*.sh
!scripts/*.ps1

# =============================================================================
# DEPLOY: 保留重要脚本
# =============================================================================
deploy/
!deploy/scripts/
!deploy/scripts/**/*.sql
!deploy/init-scripts/
!deploy/init-scripts/**/*.sql
```

---

## 📋 应该被跟踪的文件清单

### 数据库脚本（必须跟踪）

- ✅ `deploy/scripts/*.sql` - 数据库修复脚本
- ✅ `deploy/init-scripts/**/*.sql` - 数据库初始化脚本
- ✅ `backend/*/src/main/resources/db/migration/*.sql` - Flyway 迁移脚本

### 工具脚本（应该跟踪）

- ✅ `scripts/*.sh` - Shell 脚本
- ✅ `scripts/*.ps1` - PowerShell 脚本

### 依赖锁定文件（应该跟踪）

- ✅ `frontend/*/package-lock.json` - npm 依赖锁定
- ✅ `frontend/*/yarn.lock` - yarn 依赖锁定（如果使用）
- ✅ `frontend/*/pnpm-lock.yaml` - pnpm 依赖锁定（如果使用）

### 配置文件（必须跟踪）

- ✅ `pom.xml` - Maven 配置
- ✅ `package.json` - npm 配置
- ✅ `docker-compose.yml` - Docker 配置
- ✅ `application.yml` - Spring Boot 配置
- ✅ `*.properties` - 配置文件

### 源代码（必须跟踪）

- ✅ `*.java` - Java 源代码
- ✅ `*.vue` - Vue 组件
- ✅ `*.ts` - TypeScript 源代码
- ✅ `*.js` - JavaScript 源代码（非 node_modules）

---

## 🎯 推荐操作

### 立即修复

1. **修复 `deploy/` 规则** - 允许跟踪 SQL 脚本
2. **修复 `scripts/` 规则** - 允许跟踪 .sh 和 .ps1 脚本
3. **修复 `package-lock.json` 规则** - 应该被跟踪

### 验证步骤

修复后，验证重要文件是否可以被跟踪：

```bash
# 检查 SQL 脚本
git check-ignore -v deploy/scripts/*.sql
# 应该没有输出（不被忽略）

# 检查脚本文件
git check-ignore -v scripts/*.sh scripts/*.ps1
# 应该没有输出（不被忽略）

# 检查锁定文件
git check-ignore -v frontend/*/package-lock.json
# 应该没有输出（不被忽略）
```

---

## 总结

| 规则 | 当前状态 | 应该 | 优先级 |
|------|---------|------|--------|
| `deploy/` | ❌ 完全忽略 | ⚠️ 部分忽略 | 🔴 高 |
| `scripts/` | ❌ 完全忽略 | ⚠️ 部分忽略 | 🔴 高 |
| `package-lock.json` | ❌ 忽略 | ✅ 跟踪 | 🟡 中 |
| `yarn.lock` | ❌ 忽略 | ✅ 跟踪 | 🟡 中 |
| `docs/` | ✅ 忽略 | ✅ 忽略（用户要求） | ✅ 正确 |
| `.kiro/` | ✅ 忽略 | ✅ 忽略（用户要求） | ✅ 正确 |

---

**建议**: 立即修复 `deploy/` 和 `scripts/` 的规则，确保重要的 SQL 脚本和工具脚本可以被跟踪。
