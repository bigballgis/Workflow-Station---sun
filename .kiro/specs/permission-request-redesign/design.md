# Technical Design

## Overview

重新设计 User Portal 的权限申请功能，使其与新的角色体系（业务角色、管理角色、开发角色）对齐。用户可以申请组织单元的业务角色或加入虚拟组来获取权限。

## Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        User Portal Frontend                      │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │              permissions/index.vue                        │    │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │    │
│  │  │  我的权限    │  │  申请权限    │  │  申请历史    │   │    │
│  │  │  - 角色列表  │  │  - 申请角色  │  │  - 历史记录  │   │    │
│  │  │  - 虚拟组    │  │  - 加入虚拟组│  │              │   │    │
│  │  └──────────────┘  └──────────────┘  └──────────────┘   │    │
│  └─────────────────────────────────────────────────────────┘    │
│                              │                                   │
│                    permission.ts API                             │
└──────────────────────────────┼───────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                     User Portal Backend                          │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │              PermissionController                         │    │
│  │  - GET /permissions/available-roles                       │    │
│  │  - GET /permissions/available-virtual-groups              │    │
│  │  - POST /permissions/request-role                         │    │
│  │  - POST /permissions/request-virtual-group                │    │
│  │  - GET /permissions/my-roles                              │    │
│  │  - GET /permissions/my-virtual-groups                     │    │
│  │  - GET /permissions/requests                              │    │
│  └─────────────────────────────────────────────────────────┘    │
│                              │                                   │
│  ┌───────────────────────────┼───────────────────────────────┐  │
│  │              PermissionComponent                           │  │
│  │  - 权限申请逻辑                                            │  │
│  │  - 自动批准处理                                            │  │
│  │  - 申请记录管理                                            │  │
│  └───────────────────────────┼───────────────────────────────┘  │
│                              │                                   │
│  ┌─────────────────┐  ┌─────────────────┐                       │
│  │RoleAccessComponent│  │VirtualGroupAccess│                     │
│  │  - 调用角色API   │  │  - 调用虚拟组API │                     │
│  └─────────────────┘  └─────────────────┘                       │
└──────────────────────────────┼───────────────────────────────────┘
                               │ HTTP (RestTemplate)
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Admin Center Backend                        │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │  RoleController │  │VirtualGroupCtrl │  │  UserController │  │
│  │  - /roles       │  │  - /virtual-    │  │  - /users       │  │
│  │  - /roles/      │  │    groups       │  │  - /users/{id}/ │  │
│  │    business     │  │  - /virtual-    │  │    roles        │  │
│  │  - /roles/{id}/ │  │    groups/{id}/ │  │  - /users/{id}/ │  │
│  │    members      │  │    members      │  │    virtual-     │  │
│  │                 │  │                 │  │    groups       │  │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## Data Models

### PermissionRequestType Enum (Updated)

```java
public enum PermissionRequestType {
    // 新类型
    ROLE_ASSIGNMENT,      // 申请组织单元角色
    VIRTUAL_GROUP_JOIN,   // 申请加入虚拟组
    
    // 旧类型 (deprecated)
    @Deprecated FUNCTION,
    @Deprecated DATA,
    @Deprecated TEMPORARY
}
```

### PermissionRequest Entity (Updated)

```java
@Entity
@Table(name = "permission_requests")
public class PermissionRequest {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    private String applicantId;
    
    @Enumerated(EnumType.STRING)
    private PermissionRequestType requestType;
    
    // 新字段 - 角色申请
    private String roleId;
    private String roleName;  // 冗余存储，方便显示
    private String organizationUnitId;
    private String organizationUnitName;  // 冗余存储
    
    // 新字段 - 虚拟组申请
    private String virtualGroupId;
    private String virtualGroupName;  // 冗余存储
    
    // 旧字段 (deprecated)
    @Deprecated
    @ElementCollection
    private List<String> permissions;
    
    private String reason;
    
    @Enumerated(EnumType.STRING)
    private PermissionRequestStatus status;
    
    private LocalDateTime createdAt;
    private LocalDateTime processedAt;
}
```

### Frontend Types

```typescript
// 角色信息
interface RoleInfo {
  id: string
  name: string
  code: string
  description?: string
}

// 虚拟组信息
interface VirtualGroupInfo {
  id: string
  name: string
  description?: string
  boundRoles: RoleInfo[]  // 绑定的角色
}

// 组织单元信息
interface OrganizationUnit {
  id: string
  name: string
  parentId?: string
  children?: OrganizationUnit[]
}

// 用户角色分配
interface UserRoleAssignment {
  roleId: string
  roleName: string
  organizationUnitId: string
  organizationUnitName: string
  assignedAt: string
  source: 'DIRECT' | 'VIRTUAL_GROUP'  // 来源：直接分配或虚拟组
}

// 用户虚拟组成员身份
interface UserVirtualGroupMembership {
  groupId: string
  groupName: string
  joinedAt: string
  boundRoles: RoleInfo[]
}

// 角色申请请求
interface RoleRequestDto {
  roleId: string
  organizationUnitId: string
  reason: string
}

// 虚拟组申请请求
interface VirtualGroupRequestDto {
  virtualGroupId: string
  reason: string
}

// 权限申请记录
interface PermissionRequestRecord {
  id: number
  requestType: 'ROLE_ASSIGNMENT' | 'VIRTUAL_GROUP_JOIN'
  roleId?: string
  roleName?: string
  organizationUnitId?: string
  organizationUnitName?: string
  virtualGroupId?: string
  virtualGroupName?: string
  reason: string
  status: 'APPROVED' | 'PENDING' | 'REJECTED'
  createdAt: string
  processedAt?: string
}
```

## API Design

### User Portal Backend APIs

#### GET /permissions/available-roles
获取用户可申请的业务角色列表（排除已拥有的）

Response:
```json
{
  "success": true,
  "data": [
    {
      "id": "role-uuid",
      "name": "项目经理",
      "code": "PROJECT_MANAGER",
      "description": "负责项目管理"
    }
  ]
}
```

#### GET /permissions/available-virtual-groups
获取用户可加入的虚拟组列表（排除已加入的）

Response:
```json
{
  "success": true,
  "data": [
    {
      "id": "group-uuid",
      "name": "财务审批组",
      "description": "负责财务相关审批",
      "boundRoles": [
        { "id": "role-1", "name": "财务审批员" }
      ]
    }
  ]
}
```

#### POST /permissions/request-role
申请组织单元角色

Request:
```json
{
  "roleId": "role-uuid",
  "organizationUnitId": "dept-uuid",
  "reason": "工作需要"
}
```

Response:
```json
{
  "success": true,
  "data": {
    "id": 1,
    "requestType": "ROLE_ASSIGNMENT",
    "status": "APPROVED",
    "message": "角色分配成功"
  }
}
```

#### POST /permissions/request-virtual-group
申请加入虚拟组

Request:
```json
{
  "virtualGroupId": "group-uuid",
  "reason": "需要参与审批"
}
```

Response:
```json
{
  "success": true,
  "data": {
    "id": 2,
    "requestType": "VIRTUAL_GROUP_JOIN",
    "status": "APPROVED",
    "message": "已加入虚拟组"
  }
}
```

#### GET /permissions/my-roles
获取用户当前角色（按组织单元分组）

Response:
```json
{
  "success": true,
  "data": [
    {
      "roleId": "role-uuid",
      "roleName": "项目经理",
      "organizationUnitId": "dept-uuid",
      "organizationUnitName": "研发部",
      "assignedAt": "2026-01-01T10:00:00",
      "source": "DIRECT"
    }
  ]
}
```

#### GET /permissions/my-virtual-groups
获取用户当前虚拟组成员身份

Response:
```json
{
  "success": true,
  "data": [
    {
      "groupId": "group-uuid",
      "groupName": "财务审批组",
      "joinedAt": "2026-01-01T10:00:00",
      "boundRoles": [
        { "id": "role-1", "name": "财务审批员" }
      ]
    }
  ]
}
```

## UI Design

### 我的权限 Tab

```
┌─────────────────────────────────────────────────────────────────┐
│  我的角色                                                        │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ 📁 研发部                                                    ││
│  │   └─ 🏷️ 项目经理 (直接分配)                                 ││
│  │   └─ 🏷️ 开发工程师 (直接分配)                               ││
│  │ 📁 产品部                                                    ││
│  │   └─ 🏷️ 产品顾问 (来自虚拟组: 产品评审组)                   ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  我的虚拟组                                                      │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ 👥 财务审批组                                                ││
│  │   绑定角色: 财务审批员, 预算审核员                           ││
│  │ 👥 产品评审组                                                ││
│  │   绑定角色: 产品顾问                                         ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

### 申请权限对话框

```
┌─────────────────────────────────────────────────────────────────┐
│  申请权限                                                   [X] │
├─────────────────────────────────────────────────────────────────┤
│  申请类型: [申请角色 ▼] [加入虚拟组]                            │
│                                                                  │
│  ─── 申请角色模式 ───                                           │
│  组织单元: [请选择组织单元 ▼]                                   │
│  角色:     [请选择角色 ▼]                                       │
│  申请理由: [________________________]                           │
│            [________________________]                           │
│                                                                  │
│  ─── 加入虚拟组模式 ───                                         │
│  虚拟组:   [请选择虚拟组 ▼]                                     │
│  绑定角色: 财务审批员, 预算审核员 (只读预览)                    │
│  申请理由: [________________________]                           │
│            [________________________]                           │
│                                                                  │
├─────────────────────────────────────────────────────────────────┤
│                              [取消]  [提交申请]                  │
└─────────────────────────────────────────────────────────────────┘
```

## Implementation Notes

1. **自动批准**: 当前版本所有申请自动批准，后续可以添加审批流程
2. **冗余存储**: 在申请记录中存储名称字段，避免查询时需要关联
3. **来源标识**: 角色来源区分直接分配和虚拟组继承
4. **错误处理**: 如果 Admin Center API 调用失败，申请状态应为 FAILED
5. **并发控制**: 防止重复申请同一角色或虚拟组
