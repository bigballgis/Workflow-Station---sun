---
description: Vue 3 + TypeScript 前端组件、API 层与工程化规范
globs: frontend/**/*.{ts,vue,scss}
alwaysApply: false
---

# Vue 前端规范

## 组件编写

- 使用 `<script setup lang="ts">` 语法
- 文件名 PascalCase: `UserProfileDropdown.vue`
- 按功能分子目录: `components/ai/`, `components/designer/`
- UI: Element Plus + `@element-plus/icons-vue`
- 样式: `<style scoped lang="scss">`

## API 层

- 统一 axios 实例在 `api/index.ts`，baseURL: `/api/v1`
- 自动携带 JWT token 和 `X-User-Id`
- 内置 401 自动刷新 token 队列
- 每模块一个 API 文件: `api/{module}.ts`
- 返回类型: `ApiResponse<T>`（拦截器已解包 `response.data`）

## TypeScript 要求

- 类型定义放 `types/` 目录
- 接口用 `interface`，类型别名用 `type`
- **禁止 `any`**，必要时用 `unknown` + 类型守卫
- API 响应类型必须与后端 DTO 一一对应

## 目录结构

```
src/
├── api/             # axios 封装 + 模块 API
├── components/      # 可复用组件
├── composables/     # use*.ts 组合式函数
├── i18n/locales/    # zh-CN, zh-TW, en
├── router/          # Vue Router 配置
├── stores/          # Pinia Store
├── types/           # TypeScript 类型
├── utils/           # 工具函数
└── views/           # 页面视图
```

## 关键禁忌

- 禁止硬编码中文字符串 → 使用 `t('key')`
- 禁止 `v-html` 未经 DOMPurify 净化
- 禁止 `any` 类型
- 构建使用 `vite build`，Docker 必须用 `Dockerfile.local`

## UI 截图验证

改动可见页面后 MUST 跑 Playwright 截图（见 `frontend-screenshot-verification.mdc`）：`cd frontend && pnpm run verify:screenshot -- --app portal --url ... --name ...`
