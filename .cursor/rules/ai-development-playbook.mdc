---
description: AI 协作流程 — 解析任务模板、按模块验证、完成前交付清单（详文 docs/ai-rules/ai-development-playbook.md）
alwaysApply: true
---

# AI 协作开发 — 会话执行规则

完整手册：[docs/ai-rules/ai-development-playbook.md](../../docs/ai-rules/ai-development-playbook.md)  
PR 模板：[.github/pull_request_template.md](../../.github/pull_request_template.md)

## 0. Playbook 门禁 — 先「任务整理」，**用户确认后**才能执行

### 何时启用

用户消息含任一触发语即视为 **playbook 任务**：

- `按 playbook` / `playbook` / `/playbook`
- 引用 `ai-development-playbook` / 「按 playbook 整理…」

### 启用后 — 硬门禁（违反即 bug）

1. **禁止**在用户确认前使用会改动的工具：`Edit` / `Write` / `Delete`、改代码的 `Shell`、`git commit` 等。  
   **允许**：为整理任务而 `Read` / `Grep` / `Glob`（只读探查）。
2. **必须**先输出「任务整理（playbook）」块（从用户消息 + 上下文推断；不确定标 `[待确认]`）：

```markdown
## 任务整理（playbook）

【模块】portal | dw | …
【类型】fix | feat | perf | refactor
【Issue】#14xx（无则写「无」）
【验收】
  - 反例：…
  - 正例：…
  - taskId / applicationId / FU：（有则写，无则 [待确认]）
【范围】
  - 允许：…
  - 禁止：…
【验证】（按 §3 推断最低命令）
  - …

---
请确认以上任务整理；回复 **确认** / **可以** / **执行** 后我再开始改代码。若要调整请直接说明。
```

3. **模块**或**验收正例**无法推断 → 先用 `AskQuestion`（≤2 题），再输出任务整理，**仍须等确认**。
4. 用户回复 **确认** / **可以** / **执行** / **OK** / **go**（或等价）→ 方可进入 §2 执行；用户修正字段 → 更新任务整理并**再次等待确认**。
5. **仅当用户明确写**「无需确认直接执行」/「跳过 playbook 确认」→ 可跳过门禁。

### 未触发 playbook 时

纯问答、只读 review、用户明确「不要改代码」→ 不强制任务整理。  
其它实现类任务仍遵循 §1 解析任务，但不强制「先确认再动手」（除非用户另行要求）。

## 1. 解析用户任务（缺则追问，勿静默假设）

从首条消息提取或向用户确认：

| 字段 | 取值示例 |
|------|----------|
| **模块** | portal / dw / admin / backend-* / platform-common |
| **类型** | fix / feat / perf / refactor |
| **验收** | 反例 + 正例；taskId / applicationId / FU 名 |
| **范围** | 允许改的路径；**禁止**动哪些（如 shared 语义层、platform-common） |
| **验证** | 预期命令（regression:mi / vitest / mvn / docker） |

模糊需求（无验收、无范围）→ **先问 1–2 个关键问题**再写代码，禁止自行扩大 scope。

## 2. 执行中

- 先 **ORIENT**：读目标文件 + grep 确认符号存在（`ai-guardrails.mdc`）
- **最小 diff**；fix 与 perf **不得**同一 PR 混语义层改动（`performance-change-safety.mdc`）
- 触达 MI 热路径 → 必读 `portal-mi-subtable-my-request.mdc` + invariant I1–I7
- 可见 UI → `/verify-ui` skill + 截图保留 `verification-screenshots/`
- 改可部署代码 → build + 重建 compose service + `docker logs`（`debug-mode-docker-workflow.mdc`）
- **全程对照** `.cursor/skills/code-review/SKILL.md` 门禁写代码（见 §4.1），禁止为过验收而留下 Blocker/未接受 Major

## 3. 按模块 — 完成前最低验证

| 模块 | 最低验证 |
|------|----------|
| **dw** 表单/Preview | `cd frontend/developer-workstation && pnpm run build`；UI 改动 + 截图 |
| **portal** 非 MI | build + 相关 vitest + portal 截图 |
| **portal MI 热路径** | `cd frontend && pnpm run regression:mi`（单元+截图，禁止仅 unit-only 当完整通过） |
| **backend 单服务** | `mvn -pl backend/<m> -am package -DskipTests` + 重建 service + logs |
| **platform-common** | 重建所有依赖 jar 的服务 |

MI 热路径文件：`applications/detail.vue`、`tasks/detail.vue`、`shared.ts`、`SubTableField.vue`、`FormRenderer.vue` 等（见 `performance-change-safety.mdc`）。

## 4. 完成回复 — 必须包含（缺一不可）

```text
## 改动文件
- path/to/file（一句话用途）

## 验证
- 命令：… → 结果（pass / 截图路径 / logs 摘要）

## 验收对照
- 用户正例：… → 是否满足

## Code-review 就绪
- 自检结论：PASS | CONDITIONAL（仅允许已记录豁免/环境缺口）
- 已知 Blocker/Major：无 | …（有则不得宣称完成）

## 未做 / 需人工
- （无则写「无」）
```

Bug 验证通过后：`.kiro/issues/index.yaml` **一条** `fixed`；至少补单测 / verify 脚本 / 专项 rule 之一。

## 4.1 Code-review 门禁 — playbook 交付必须可过审（硬门禁）

按 playbook **整理并修改**的内容，在宣称完成前必须达到：用 **code-review skill** 审查当前 **staged changes** 时能给出 **PASS**（或仅 `CONDITIONAL` 且无 Blocker/未接受 Major，且缺口已写入「未做/需人工」）。

### 用户标准审查指令（完成后应能扛住）

```text
使用 code-review skill 审查当前 staged changes。
不要修改、提交或推送；按 Blocker/Major/Minor/Question 输出，给出 PASS/CONDITIONAL/FAIL 结论。
```

### 执行要求

1. **对照真源**：`.cursor/skills/code-review/SKILL.md` + `reference.md`（触发矩阵、放行规则、固定输出格式）。
2. **staging 卫生**：相关源码/测试/i18n/SQL/配置一并 staged；禁止漏提交导致 Major/Blocker；无关噪声勿入 stage。
3. **自检等价于审查**：完成前按 skill §3–§9 自检——需求/正反例、最小 diff、影响链、编译/build、测试/UI/MI、安全与增量 secret、FU 通用性/生命周期（未触达标 N/A）、性能（命中才查）。
4. **放行标准**（与 skill §9 一致）：Blocker = 0 且无未接受 Major；受影响模块编译/build 有真实命令结果；必需测试/截图已过；secret 扫描干净 → 才可写「Code-review 就绪：PASS」。关键验证环境不可用 → 最多 `CONDITIONAL`，不得假装 PASS。
5. **审查时只读**：用户发起上述审查时，**禁止**修改产品代码、提交或推送；按 Blocker/Major/Minor/Question 与 PASS/CONDITIONAL/FAIL 固定格式输出。
6. **FAIL 即未完成**：若审查或自检为 FAIL → 先修到可 PASS/CONDITIONAL，再交付；禁止「验收过了但过不了 code-review」。

## 5. 用户可复制 — 任务开场白

**懒人版（须等你确认后才执行）：**

```text
（一两句话描述需求）
按 playbook 整理。
```

确认 AI 输出的「任务整理」无误后，回复：**确认**

**跳过确认（慎用）：**

```text
… 按 playbook，无需确认直接执行。
```

**完整手填版：**

```text
按 playbook 执行（先输出任务整理，等我确认）。

【模块】
【类型】fix | feat | perf | refactor
…
```
