---
description: Spring Boot 后端分层架构、代码归属决策树与编码规范
globs: backend/**/*.java
alwaysApply: false
---

# 后端架构规范

## 代码归属决策树 — "这段代码应该放在哪里？"

```
新增一段逻辑 →
├─ 它接收 HTTP 请求？ → Controller（继承 BaseController，只做路由+校验）
├─ 它编排多个 Service/外部调用？ → Component（接口 + impl/）
├─ 它是纯领域逻辑（单职责）？ → Service（接口 + impl/）
├─ 它是数据库查询？ → Repository（Spring Data JPA 接口）
├─ 它是跨服务调用？ → client/ 包中的 REST Client
├─ 它是跨模块共享的？ → platform-common（注意：改它影响所有下游）
├─ 它是配置/Bean 定义？ → config/ 包
└─ 它是校验规则？ → validation/ 包（自定义 ConstraintValidator）
```

## 分层依赖方向（不可违反）

```
Controller → Component → Service → Repository
     ↓            ↓          ↓
   DTO          DTO      Entity
```

- Controller 不能直接调用 Service 或 Repository
- Service 不能依赖 Controller 或 Component
- Component 是唯一允许注入多个 Service 的编排层

## Controller 模式

```java
@PostMapping
public ResponseEntity<ApiResponse<EntityResponse>> create(
        @RequestBody @Valid EntityRequest req) {
    return handleRequest(() -> component.create(req));
}
```

## 包结构

```
com.{module}/
├── client/          # 跨服务 REST 调用
├── component/       # 业务组件接口 + impl/
├── config/          # Spring 配置类
├── controller/      # REST 控制器
├── dto/             # Request/Response DTO
├── entity/          # JPA 实体
├── enums/           # 枚举
├── exception/       # 自定义异常 (继承 ApplicationException)
├── repository/      # JPA Repository
├── service/         # 服务接口 + impl/
└── validation/      # 自定义校验器
```

## 编码要点

- Lombok: `@Data`, `@Builder`, `@Slf4j`, `@RequiredArgsConstructor`
- MapStruct: Entity ↔ DTO 转换（不要手写 getter/setter 映射）
- 写操作标注 `@Transactional`，只读查询加 `@Transactional(readOnly = true)`
- 异常: `BusinessException` / `BusinessLogicException` 体系，携带 `errorCode` + `ErrorContext`
- 日志: `@Slf4j`，禁止记录密码/token/PII
- 新方法必须先搜索是否已有可复用的实现（Grep），避免重复代码
