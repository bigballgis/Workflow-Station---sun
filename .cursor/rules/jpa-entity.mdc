---
description: JPA 实体定义规范 — Lombok 注解、审计字段、表命名与关联关系
globs: backend/**/entity/**/*.java
alwaysApply: false
---

# JPA 实体规范

## 标准注解组合

```java
@Entity
@Table(name = "dw_function_unit")
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
@EqualsAndHashCode(of = "id")
@EntityListeners(AuditingEntityListener.class)
public class FunctionUnit {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Version
    private Long lockVersion;

    @CreatedBy private String createdBy;
    @CreatedDate private Instant createdAt;
    @LastModifiedBy private String updatedBy;
    @LastModifiedDate private Instant updatedAt;
}
```

## 规则

| 要求 | 说明 |
|------|------|
| 表名前缀 | `dw_`(developer), `ac_`(admin), `up_`(portal), `we_`(workflow) |
| 时间字段 | 统一 `Instant`，不用 `LocalDateTime` |
| 乐观锁 | `@Version` + `lockVersion` 字段 |
| 审计 | `@CreatedBy`, `@CreatedDate`, `@LastModifiedBy`, `@LastModifiedDate` |
| 集合字段 | `@Builder.Default` 初始化为 `new ArrayList<>()` |
| 关联加载 | 默认 `FetchType.LAZY`，注意 N+1 查询 |
| equals/hashCode | 只基于 `id` 字段: `@EqualsAndHashCode(of = "id")` |

## DTO 配套

- 请求: `{Entity}Request` + Jakarta Validation 注解
- 响应: `{Entity}Response`
- 转换: MapStruct Mapper

## Schema 同步

新增/修改实体后，检查 `deploy/init-scripts/` SQL 脚本是否需要同步更新:
- 新表 → 新增 SQL 脚本（序号递增）
- 新字段 → `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`
- 新关联 → 检查外键约束
- `.sql` 文件必须 LF 换行
