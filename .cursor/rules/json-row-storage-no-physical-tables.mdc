---
description: Relation Table / Table Design 行数据仅用 JSON 存储，禁止为每张业务表创建物理表
globs: backend/**/*RelationTable*,backend/**/*TableDesign*,backend/**/*RowData*,deploy/init-scripts/**
alwaysApply: false
---

# JSON 行存储 — 禁止 per-table 物理表

**核心原则：** Admin Center 的 **Relation Table** 与 Developer Workstation 的 **Table Design** 均只维护**结构元数据**；**行数据一律 JSON/JSONB 存储**，**绝不**按 `table_name` 动态 `CREATE TABLE` / `ALTER TABLE` / `DROP TABLE`。

## 适用范围

| 场景 | 模块 | 结构元数据 | 行数据存储 |
|------|------|-----------|-----------|
| Relation Table | admin-center / user-portal | `rt_table_definitions`、`rt_field_definitions`、`rt_table_versions.snapshot_data` | `rt_table_data_rows.data` (JSONB) |
| Table Design | developer-workstation / workflow / portal | `dw_table_definitions`、`dw_field_definitions` | JSON（流程变量、`__subTables__`、统一行表或等价 JSON 容器） |

平台自有固定表（`ac_*`、`dw_*` 元数据表、`up_*`、`we_*`、Flowable 表等）**不在此限**——本规则针对**用户/设计师定义的业务表**。

## 必须遵守

1. **部署 = 元数据 + 版本快照**，不执行物理 DDL  
   - Relation Table `deploy()` 只写 `rt_table_versions` 与状态，**不**对 `table_name` 建表。  
   - Table Design 保存/发布只更新 `dw_*` 元数据，**不**在 DB 创建与设计师表名同名的物理表。

2. **CRUD 走 JSON 行表或 JSON 容器**  
   - Admin / Portal 读写 `rt_table_data_rows`：`table_id` + `row_id` + `data`（字段名 → 值）。  
   - 查询、过滤、分页在 JSONB 或应用层完成，**不** `SELECT FROM {table_name}`。

3. **跨模块契约一致**  
   - DTO/API 传 `Map<String, Object>` 或 `RelationTableDataRowDTO.data`，与 JSONB 字段一一对应。  
   - user-portal、platform-common 不得假设存在 per-table 物理表。

## 绝对禁止

| 禁止 | 说明 |
|------|------|
| `CREATE TABLE {table_name}` | 按 Relation Table / Table Design 的 `table_name` 动态建表 |
| `ALTER TABLE` / `DROP TABLE` | 按字段变更同步物理列 |
| `jdbcTemplate.query("SELECT * FROM " + tableName)` | 动态表名 SQL |
| init 脚本为示例 FU 建物理业务表 | 新 sample 只写元数据 + JSON 行数据 |
| 测试 mock「物理表已存在」部署路径 | 测试应断言**无 DDL 执行** |

## 变更检查清单

修改 Relation Table / Table Design / Portal 取数 / 部署 / 迁移时逐项确认：

- [ ] 是否新增或恢复了 `CREATE/ALTER/DROP TABLE` 调用？→ **删除或改为 JSON 路径**
- [ ] Flyway / `deploy/init-scripts/` 是否新增 per-table 物理表？→ **仅允许 `*_data_rows` 等统一 JSON 容器**
- [ ] Service 是否仍引用 `physicalTableExists`、`generateCreateTableDdl` 等遗留 DDL？→ **新逻辑不得调用；清理时勿恢复**
- [ ] 前端是否假设列名 = DB 物理列？→ **字段来自元数据/快照，值来自 JSON `data`**
- [ ] 文档/注释是否仍写「首次部署 CREATE TABLE」？→ **改为 JSON 存储描述**

## 参考实现

```java
// ✅ admin-center — 行数据写入 JSONB
private static final String DATA_ROWS_TABLE = "rt_table_data_rows";
// INSERT ... (table_id, row_id, data, ...) VALUES (?, ?, ?::jsonb, ...)

// ✅ deploy — 仅版本快照
// 行数据存 rt_table_data_rows (JSONB)，部署仅更新结构元数据与版本快照，不执行 CREATE/ALTER TABLE
```

```java
// ❌ 禁止 — 按业务表名建物理表
jdbcTemplate.execute("CREATE TABLE " + tableName + " (...)");
```

## 与领域模型

- 术语仍用 `RelationTable` / `TableDefinition`（见 `domain-model.mdc`）。  
- 「表」= **逻辑模型**（字段定义 + JSON 行），≠ PostgreSQL 中的一张物理表。
