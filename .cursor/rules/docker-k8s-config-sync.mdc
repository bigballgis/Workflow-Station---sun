---
description: Docker dev 配置变更时必须同步 K8s ConfigMap/Secret（与 deploy/CONFIG_SYNC.md 一致）
globs: "deploy/**,backend/**/application.yml,backend/**/application-docker.yml"
alwaysApply: false
---

# Docker ↔ K8s 配置同步（强制）

当本次变更涉及以下任一项时，**同一 PR/同一会话内**完成 K8s 同步，不要只改 Docker：

- `deploy/environments/dev/.env` 或 `docker-compose.dev.yml` 的 `environment`
- `backend/*/application.yml` / `application-docker.yml` 新增或修改 `${ENV_*}` 占位
- `deploy/kong/` 路由或前端 nginx 代理相关 env

## 必做

1. 按 **[deploy/CONFIG_SYNC.md](../../deploy/CONFIG_SYNC.md)** §1–§4 执行（应用 yml → `.env` + compose → K8s ConfigMap/Secret → `BUILD_GUIDE.md` §12）。
2. **非敏感** → `deploy/k8s/config_map/<Environment>/configmap-workflow-platform-config.yml`（默认 `preprod`）。
3. **敏感** → `deploy/k8s/secret/<Environment>/secret-workflow-paltform.yml`；生产禁止 ConfigMap 明文密码/密钥。
4. 环境变量名与 Docker **完全一致**；禁止仅在 compose 写变量而 K8s 遗漏。
5. 完成时在回复中列出已同步的 K8s 文件路径。

## 不必同步 K8s

- 仅 `volumes`、`ports`、`depends_on`、镜像 build 上下文等 Compose 专有项
- 纯文档、注释、无行为变化的格式化

## 交叉引用

- 人类可读全文：`deploy/CONFIG_SYNC.md`
- 部署细节：`.cursor/rules/deployment-infra.mdc`、`debug-mode-docker-workflow.mdc`
