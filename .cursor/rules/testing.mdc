---
description: 前后端测试规范 — JUnit 5 + jqwik + Vitest + fast-check
globs: "**/*{Test,test,Tests,PropertyTest,Properties,spec}*.{java,ts}"
alwaysApply: false
---

# 测试规范

## 后端 — JUnit 5

```java
@BeforeEach
void setUp() { /* 初始化 mock 和测试数据 */ }

@Test
void shouldReturnUser_whenValidIdProvided() {
    // given - when - then 结构
}
```

- 命名: `{ClassName}Test.java`
- Mock: `@Mock` + `@InjectMocks` 或手动构造
- 一个测试只验证一个关注点

## 后端 — jqwik 属性测试

```java
@Property(tries = 100)
void allValidInputsShouldProduceConsistentOutput(
        @ForAll @StringLength(min = 1, max = 255) String name) {
    // 属性断言: 不变量、幂等性、边界条件
}
```

- **命名约定取决于目录**（新增前先确认目标目录已有文件的模式）:
  - `service/` 目录 → `{Feature}Properties.java`
  - `property/` 目录 → `{Feature}PropertyTest.java`
- 使用 `@BeforeProperty` 初始化

## 前端 — Vitest

```typescript
describe('FunctionUnitStore', () => {
  it('should fetch items successfully', async () => {
    // arrange - act - assert
  })
})
```

- 命名: `*.test.ts` 或 `__tests__/` 目录
- 组件测试: `@vue/test-utils` + jsdom
- 属性测试: fast-check 集成 Vitest

## 运行命令

```bash
mvn test -pl backend/developer-workstation              # 单模块
mvn test -pl backend/admin-center -Dtest=UserTest       # 单类
npm run test                                            # 前端 (vitest run)
```

## 原则

- 测试行为而非实现细节
- Mock 外部依赖，不 mock 被测对象内部逻辑
- 测试名称描述预期行为: `shouldDoX_whenY`
