---
description: Form Preview FK/PK runtime — pkGeneration rules, readonly auto-PK, allocate API, deep rule sync, sub-table add orchestration, Import Fields metadata sync, mergeFormRowWithSeed
globs: frontend/developer-workstation/**/*.{vue,ts},frontend/user-portal/src/utils/{subTableRowRuntime,tableFkRuntime,formFieldMeta,pkGenerationConfig}.ts,frontend/user-portal/src/components/{SubTableField.vue,SubTableAddDialog.vue,subTableAddDialogHelpers.ts,FormRenderer.vue,FormRendererFields.vue},frontend/user-portal/src/views/{processes/start.vue,tasks/detail.vue,applications/detail.vue},backend/user-portal/src/main/java/com/portal/component/PortalPrimaryKeyAllocationComponent.java,backend/admin-center/src/main/java/com/admin/component/FunctionUnitManagerComponent.java
alwaysApply: false
---

# Form Preview × FK/PK 运行时（DW 设计侧）

与 `portal-design-parity.mdc` 配套：**Form Preview 弹窗 = 设计稿运行时**；PK 生成、FK 填充、主表就绪、只读呈现 MUST 与 PRD（`docs/table-design-fk-pk-requirements.md`）及 Portal 共享 runtime 一致。

## 1. 主表 / 子表字段只读（非 manual PK、readonly FK）

| 条件 | Form Preview / 画布 rule |
|---|---|
| `isPrimaryKey` 且 `pkGeneration.strategy !== 'manual'` | **Readonly**（`props.readonly` → form-create `disabled`） |
| 结构 FK 且 `fkDisplayMode === 'readonly'` | **Readonly** |
| 结构 FK 且 `fkDisplayMode === 'hidden'` | **不渲染**（runtime 仍填充） |

**实现 MUST 走共享工具，禁止在 Preview 组件内手写 if：**

- `formFieldMeta.ts` — `isAutoGeneratedPrimaryKey`、`applyTableFieldMetaToFormRule`、`syncFormRulesWithTableFields`
- `normalizeFieldDefinitionForRuntime` — 统一 `pkGenerationJson`、`primaryKey`（Jackson 别名）、默认 UUID
- `formCreateRuleUtils.ts` — `mapFormCreateRulesReadonlyDeep`（Preview 渲染前）
- `registerFormCreateReadonlyParser.ts` — 全局 `props.readonly` → `disabled`

**深度同步（必须）：** `syncFormRulesWithTableFields` MUST **递归** `elCard` / `fcRow` / `group` 等 `children`（及 `props.children|list|items|fields`）。仅 sync 顶层 rule 会导致 card 内 `id` 仍可读——这是已知缺陷模式。

`handlePreview` / `buildSavedFormPreviewItems` 路径：`syncFormRulesWithTableFields` → `mapFormCreateRulesReadonlyDeep`。

## 2. 主键生成规则（`pkGeneration`）

### 2.1 设计态（Table Design / AC Relation Table 结构）

字段勾选 **`isPrimaryKey`** 后配置 `pkGeneration`（DW：`FieldDefinition.pkGeneration` / `pkGenerationJson`；AC：`RelationFieldDTO` 等价字段）：

| 策略 | 说明 | Preview / Portal 行为 |
|---|---|---|
| `uuid` | 后端生成 UUID（**新建 PK 默认**） | 只读 + 后端 `allocate` |
| `manual` | 用户手填 | 可编辑；为空时阻断依赖其 FK 链的子表 Add |
| `autoIncrement` | 后端递增整数 | 只读 + 后端 `allocate` |
| `prefixedSequence` | `{prefix}{zeroPaddedSeq}`，如 `CASE-000001` | 只读 + 后端 `allocate` |

**配置项：**

- `autoIncrement` / `prefixedSequence`：`startValue`（默认 1）
- `prefixedSequence` 额外：`prefix`、`padWidth`（默认 6）
- **作用域固定 `perTable`**（每张表独立连续编号）；序列化时写 `scope: 'perTable'`；UI **不提供** `perFunctionUnit` / `perPrefix`
- **联合主键**：同表多个字段 `isPrimaryKey=true`；每个 PK 列可独立配置 strategy（通常一致）；FK 引用侧 `refPrimaryKeyFields` MUST 列全目标表 PK 列名

**Table Design UI（DW）：**

- `PkGenerationEditor.vue` + `pkGenerationConfig.ts` — 策略下拉 + gear popover（起始值 / 前缀 / 位数）
- `FieldForeignKeyEditor.vue` — 选 Ref Table 后 **自动填充** `refPrimaryKeyFields` 为目标表全部 PK 列
- 表名 **全局唯一**（DW ↔ AC 互检）；见 Table Design / AC structure 保存前 `name-available` 校验

### 2.2 运行时识别（Preview / Portal 共用）

**禁止**前端自增、禁止忽略元数据仅用 UUID fallback。

| 函数 | 职责 |
|---|---|
| `normalizeFieldDefinitionForRuntime` | 统一 `isPrimaryKey`（含 `primaryKey` Jackson 别名）；PK 且缺 `pkGeneration` → **`{ strategy: 'uuid' }`** |
| `isAutoGeneratedPrimaryKey` | `isPrimaryKey && strategy !== 'manual'` → 画布/Preview **readonly** |
| `pkNeedsAllocation`（`subTableRowRuntime.ts`） | 同上；用于 Add 路径是否调用 `allocatePrimaryKeys` |
| `parsePkGeneration` / `serializePkGeneration` | 读写策略；非 manual 序列化带 `scope: 'perTable'` |

### 2.3 后端分配 API

```
POST .../primary-keys/allocate
Body: { tableId, fieldName, count?: 1, scopeKey?: functionUnitId }
Response: { values: string[] }
```

| 模块 | 入口 |
|---|---|
| DW Preview | `functionUnitApi.allocatePrimaryKeys` → `TableDesignController` |
| Portal | `PortalPrimaryKeyAllocationComponent` / 等价 REST |
| AC 数据页 | `RelationTablePrimaryKeyAllocationService` |
| 共享实现 | `platform-common` — `JdbcPrimaryKeyAllocationService`（DB 序号表，`scope_type=perTable`） |

**Portal 后端 MUST 与 DW 对齐的两点：**

1. **`pk_generation_json` 为空但 `is_primary_key=true`**：`loadPkGenerationJson` 返回空 Map → `toPkConfig` 默认 **`uuid`**（禁止抛 `NOT_PK_FIELD`）。
2. **`functionUnitId` 解析**：Process Start 传 FU **code**（route param）；**To Do / My Request / Task detail** 传 Flowable **`processDefinitionKey`**（如 `Process_1_KK`）。`PortalPrimaryKeyAllocationComponent` MUST：先 direct `dw_function_units` id/code → 再 `FunctionUnitAccessComponent.resolveFunctionUnitId`（process key → catalog id）→ `sys_function_units.code = dw_function_units.code` 映射 **dw numeric id**。禁止仅按 code 查而忽略 process key。

**`tableBindings.fieldDefinitions` 前置条件（Portal 子表 Add）：** admin `attachTableBindings` 取 designer 表元数据时 MUST 用 `td.display_name`（**非** `td.description`——`dw_table_definitions` 无 description 列）；否则 FU content 失败 → binding 无 `fieldDefinitions` → `parentTablesById` / allocate 链断裂。

- **并发安全**：后端计数；**不在**前端、流程变量或 JSON 行里模拟 SERIAL
- 已有非空 PK 值：**不覆盖**（`allocateAutoPrimaryKeysForRow` 跳过）

### 2.4 Form Preview 分配时机

| 场景 | 行为 |
|---|---|
| **子表 Add**（`prepareSubTableAddRow`） | guard / ensure 父行 → `applyFkToInitialRow` → 对子表所有 **非 manual** PK 调用 `allocatePrimaryKeys` |
| **主表 PK 未就绪**（子表 FK 依赖 MAIN） | `ensureParentRowsForChildAdd`：对缺失父 PK 的 `refTableId` 在 `parentTablesById` 上 allocate auto-PK → `primaryFormDataPatch` 写回 Preview 主表单 |
| **主表 PK 为 `manual` 且仍为空** | **阻断**子表 Add；提示 `subTable.fkGuardMainNotReady`（三语言） |
| **主表单 auto-PK 字段** | 画布 / Preview **readonly**；值由保存或 allocate 路径写入，用户不可手改 |

**Form Preview 子表 Add 顺序（必须）：**

```
prepareSubTableAddRow
  → filterStructuralFkMetasForBinding（MI 排除结构 FK）
  → guardBeforeChildRowAdd
  → ensureParentRowsForChildAdd（autoEnsurePrimaryRecord && Preview 有 primaryFormData）
  → applyFkToInitialRow
  → allocatePrimaryKeys（子表非 manual PK）
  → 对话框 initialData seed（§4）
```

### 2.5 与 FK / 联合 PK 的交互

- FK 填充源：父行 **实际 PK 列值**（`tableFkRuntime.ts` — `applyFkToInitialRow`）
- 联合 PK → 单 FK 字段：按 PRD 5.3 合成（`encodeCompositePrimaryKey`）；FK 字段 `length` 须能容纳合成串
- 子表 Add 依赖主表 PK：**manual 主表 PK 必须先有值**，auto-PK 主表可由 `ensureParentRowsForChildAdd` 补全

## 3. 子表 Add — 主表未就绪时自动建主表行（Form Preview）

**禁止**在 Form Preview 中仅弹 `subTable.fkGuardMainNotReady` 而不尝试补全主表。

当子表 Add 且结构 FK guard 失败时：

1. 若 `autoEnsurePrimaryRecord === true`（Preview：`primaryFormData != null`）且存在 `allocatePrimaryKeys` + `parentTablesById`
2. 调用 `ensureParentRowsForChildAdd` — 对缺失父 PK 的 `refTableId` 调用后端 `allocatePrimaryKeys`
3. 将结果写回 `primaryFormData` / `ancestorRowsByTableId`，并通过 `primaryFormDataPatch` + `update:primaryFormData` 同步 Preview 主表单
4. 重新 guard；**仍失败**（如主表 PK 为 `manual` 且为空）才保留阻断提示

**FormDesigner 必须传入：** `primaryTableId`、`parentTablesById`（PRIMARY + SUB 表的 `fieldDefinitions`）、`previewTableBindingsForContext`。

## 4. 子表 Add — PK 分配与对话框 seed（禁止丢 id）

`prepareSubTableAddRow`（`subTableRowRuntime.ts`）顺序见 **§2.4**。

**对话框 MUST 保留 `initialData` 中的已分配 PK/FK：**

| 组件 | Add 模式 | Save |
|---|---|---|
| `SubTableFormDialog.vue` | `formData = { ...initialData }`，维护 `seedRow` | `mergeFormRowWithSeed(seedRow, formData)` |
| `SubTableAddDialog.vue` | `{ ...buildInitialRow(columns), ...initialData }` | `mergeFormRowWithSeed(initialData, formData)` |
| `SubTableField.vue` | `handleDialogSave` | `mergeFormRowWithSeed(dialogInitialData, rowData)` |

**禁止** Add 模式打开对话框时重置 `formData = {}`——会导致子表 PK（如 `id_idw`，列表列显示名 `id`）保存后为空，表格显示 `-`。  
**禁止** Save 时用 `{ ...seed, ...formData }` 直接覆盖——form-create 对 readonly/disabled 字段常回写 `null`/`''`，必须用 `mergeFormRowWithSeed` 保留已分配 PK/FK。

### 4.1 `mergeFormRowWithSeed`（Save 时保留已分配 PK/FK）

**位置：** `subTableAddDialogHelpers.ts` — `mergeFormRowWithSeed(seed, form)`

**语义：** 以 `form` 为基底；对 `seed` 中每个 key，若 `form[key]` 为空（`null` / `undefined` / `''`）且 `seed[key]` 非空，则回退为 seed 值。用户手填的非空值仍优先。

```typescript
// ✅ 正确 — 空 form 值不覆盖已 allocate 的 PK/FK
const row = mergeFormRowWithSeed(seedRow, formData)

// ❌ 禁止 — form-create readonly 字段常写 null，会抹掉 id_idw
const row = { ...seedRow, ...formData }
```

### 4.2 auto-PK 列控件类型（`inputNumber` × `prefixedSequence`）

子表 subForm 若对 PK 字段使用 `inputNumber`（如 `id_idw`），但 Table Design 配置 `prefixedSequence` / `uuid`，allocate 返回 **字符串**（如 `Test-000001`）。`el-input-number` 无法绑定 → 表单校验报 `{displayName} is required`（如 `id is required`）。

**MUST：** `applyFkPresentationToDialogColumns` 在标记 auto-PK `readonly` 时，若 strategy 为 `prefixedSequence` 或 `uuid` 且列 type 为 `number`，**降级为 `text`**（只读 `el-input` 可展示字符串 PK）。`autoIncrement` 仍保持 `number`。

**调用链（三处 MUST 一致）：**

| 组件 | Save 入口 |
|---|---|
| `SubTableFormDialog.vue` | `mergeFormRowWithSeed(seedRow.value, formData.value)` |
| `SubTableAddDialog.vue` | `mergeFormRowWithSeed(props.initialData, formData.value)` |
| `SubTableField.vue` | `mergeFormRowWithSeed(dialogInitialData.value, rowData)` |

**典型症状：** 子表 Add 对话框里 PK 已显示/已分配，保存后列表 `id` 列 `-` → 优先查 Save 是否仍用 spread 覆盖 seed。

## 5. Import Fields × 表元数据同步（防 PK/FK 回归）

**已知缺陷模式：** 在 Table Design 新增字段后，于 Form Design **Import Fields** 导入到子表，Preview 子表 Add 时 PK 不再按设计生成或列表显示 `-`。

### 5.1 根因

| 根因 | 后果 |
|---|---|
| Import 只 **append** 新 rule，未对**已有** PK rule 重新 sync | 旧 PK 字段仍为可编辑 `inputNumber`，form-create Save 回写空值 |
| Preview / bindingMap 使用 **陈旧** `store.tables` | `fieldDefinitions` 缺 `pkGenerationJson` → `pkNeedsAllocation` 为 false → 不 allocate |
| Save 用 spread 覆盖 seed | 见 §4.1 |

Import 对话框打开时虽会 `fetchTables`，但 **Preview 打开前** 若 Table Design 刚保存、Form Design 未刷新，仍可能 stale。

### 5.2 子表 Import Fields 路径（`FormDesigner.handleConfirmImportFields`）

```
isImportingSubTable()
  → mapFieldsToFormRules(selectedImportFields)     // 仅新增字段 → fieldToFormRule + applyTableFieldMetaToFormRule
  → targetRef.setRule([...currentRules, ...newRules])  // 跳过 duplicate field
  → appendSubTableListFieldColumns(baseColumns, subFields)  // 列表 viewFields
  → refreshFormRulesFromTableMetadata()            // MUST — 对主表 + 全部 sub designer 全量 sync
```

**`refreshFormRulesFromTableMetadata` MUST：**

- 对每个 binding 的 **全部** current rules（含 Import 前就存在的 PK）调用 `syncFormRulesWithTableFields(rules, getTableFieldDefinitions(tableId))`
- 使 auto-PK / readonly FK 的 `props.readonly` 与 Table Design 元数据一致
- **不能**仅 sync 本次 Import 的 `newRules`——已有 PK rule 不会被更新

### 5.3 Preview 打开前刷新（`FormDesigner.handlePreview`）

```
async handlePreview()
  → await store.fetchTables(functionUnitId)        // 最新 fieldDefinitions / pkGenerationJson
  → refreshFormRulesFromTableMetadata()            // designer getRule() 前 sync readonly
  → buildPreview()
       → syncFormRulesWithTableFields(rawRule, primaryFields)   // 主表
       → per SUB binding: syncFormRulesWithTableFields + mapFormCreateRulesReadonlyDeep
       → bindingMap[].fieldDefinitions ← store.tables  // prepareSubTableAddRow / allocate 用
```

**禁止** `handlePreview` 不 `fetchTables` 直接 `buildPreview`——Table Design 保存后 PK 策略变更不会进入 Preview。

### 5.4 Import 与 PK 分配的数据流（端到端）

```
Table Design 保存 PK 策略
  → store.fetchTables（Table Designer 或 Import 对话框）
Form Design Import Fields（子表）
  → append rules + refreshFormRulesFromTableMetadata
Preview 打开
  → fetchTables + refreshFormRulesFromTableMetadata + buildPreview
子表 Add
  → prepareSubTableAddRow(fieldDefinitions from bindingMap)
  → allocatePrimaryKeys（pkNeedsAllocation）
  → SubTableFormDialog seed initialData
Save
  → mergeFormRowWithSeed → 列表列 fieldName（如 id_idw）有值
```

列表列 `displayName` 可为 `id`，**行数据 key MUST 为 `fieldName`（如 `id_idw`）**——Import 的 `appendSubTableListFieldColumns` 使用 `field.fieldName`，勿与 displayName 混淆。

## 6. MI 与结构 FK 分流

- `bindingLinkMode === 'miParticipantRow'`：`filterStructuralFkMetasForBinding` 排除 legacy `foreignKeyField`，**不**走结构 FK guard/fill。
- 结构 FK 以字段元数据 `isForeignKey` + `refTableId` 为准，不以 binding 上手工 `foreign_key_field` 为 SoT。

## 7. Portal 流程发起（Process Start）× MI 子表

与 `portal-design-parity.mdc` 配套：**发起流程页**（`processes/start.vue`）子表 Add MUST 与 Form Preview 同一 FK/PK 管道。

### 7.1 业务场景（Multi-Instance Subtask Demo 等）

| 步骤 | 行为 |
|---|---|
| 发起人填主表 | 主表 PK 可为 auto（uuid / autoIncrement / prefixedSequence）；画布 readonly，值由 allocate 或子表 Add 时 `ensureParentRowsForChildAdd` 补全 |
| 发起人 Add MI 子表行 | 子表结构 FK 指向主表 PK → 先 ensure 主表 PK → `applyFkToInitialRow` 填 FK → allocate 子表非 manual PK |
| MI participant 行 | `bindingLinkMode === 'miParticipantRow'`：legacy `foreignKeyField` 不参与结构 FK guard/fill（见 §6） |
| 列表 / 对话框 label | MUST 用 Table Design `displayName`（`dataTables` / `enrichColumnsWithTableFieldDisplayNames`），禁止仅显示 `field_name` |

### 7.2 Process Start 数据加载（`start.vue`）

```
loadFunctionUnitContent
  → cachedRelationTableFieldIndex ← buildRelationTableFieldIndexFromDataTables(content.dataTables)
  → cachedContentForms ← content.forms
  → per SUB binding:
       columns ← resolveSubTableBindingColumnsForStart（subListViews + dataTables displayName 回退）
       fieldDefinitions ← resolveBindingFieldDefinitions（tableBindings 优先，dataTables 回退）
  → PRIMARY binding → primaryTableBinding.fieldDefinitions（供 ensureParentRowsForChildAdd）
```

### 7.3 Portal 组件契约（与 Preview 对齐）

| 组件 | MUST |
|---|---|
| `FormRenderer` | `parentTablesById`（PRIMARY + SUB `fieldDefinitions`）、`primaryTableId`、`handlePrimaryFormDataPatch` |
| `SubTableField` | `prepareSubTableAddRow` 传 `parentTablesById`、`autoEnsurePrimaryRecord: primaryFormData != null`；处理 `primaryFormDataPatch` → `update:primaryFormData` |
| `SubTableAddDialog` | Add 模式 seed `initialData`；Save → `mergeFormRowWithSeed` |
| `subTableRowRuntime.ts` | 与 DW 同版：`ensureParentRowsForChildAdd`、`normalizeFieldDefinitionForRuntime` |

### 7.4 典型症状（Process Start）

| 症状 | 优先检查 |
|---|---|
| 列头 / 对话框 label 为 `id`、`main_id` 而非 designer displayName | `dataTables` 索引 + `enrichColumnsWithTableFieldDisplayNames` |
| Add 对话框 PK/FK 必填但空 | `fieldDefinitions` 是否为空；`ensureParentRowsForChildAdd` 是否执行 |
| 对话框已显示 PK，Save 后列表 `-` | `mergeFormRowWithSeed` 是否用于 Save |
| 主表 PK 未生成导致子表 Add 阻断 | `parentTablesById` 是否含 PRIMARY；`allocatePrimaryKeys` API 是否可达 |

## 7.5 Portal To Do / My Request / Task detail × 子表 Add

与 §7 Process Start 共用 `prepareSubTableAddRow` / `SubTableField`，但 **functionUnitId 来源不同**：

| 视图 | `functionUnitId` 传入值 | 后端解析 |
|---|---|---|
| Process Start | FU **code**（`route.params.key`） | direct `dw_function_units.code` |
| To Do / My Request / Task detail | **`processDefinitionKey`** | §2.3 process key → catalog → dw id |

**典型症状：** 子表 Add 报 `FUNCTION_UNIT_NOT_FOUND` → 查 `PortalPrimaryKeyAllocationComponent.resolveFunctionUnitId` 是否支持 process key；前端 `tasks/detail.vue` / `applications/detail.vue` 的 `functionUnitIdRef` 使用 `processDefinitionKey` 为 **预期行为**，修后端而非改回 code。

**数据加载：** `loadFunctionUnitContent(processDefinitionKey)` 经 admin `getFunctionUnitByProcessKey` 正常；子表 Add 仍依赖 binding 上 **`fieldDefinitions`**（见 §2.3 attachTableBindings）。

## 8. 变更检查清单

修改 Form Preview、子表 Add、表字段元数据、PK 策略 UI、**Import Fields**、**Portal Process Start** 时逐项确认：

- [ ] `syncFormRulesWithTableFields` 是否仍 **递归** 嵌套 layout？card 内 auto-PK 是否 readonly？
- [ ] Preview 打开前是否 `mapFormCreateRulesReadonlyDeep`？
- [ ] 新建/勾选 PK 默认 strategy 是否为 **`uuid`**（`normalizeFieldDefinitionForRuntime` 缺省一致）？
- [ ] 非 `manual` PK 是否 **只读** 且走 **后端** `allocatePrimaryKeys`（禁止前端自增）？
- [ ] `serializePkGeneration` 是否仍写 **`scope: 'perTable'`**？
- [ ] 子表 Add 是否在 guard 失败时尝试 `ensureParentRowsForChildAdd`（Preview 路径）？
- [ ] **manual 主表 PK 仍空** 时是否阻断子表 Add（不误用 auto-allocate）？
- [ ] `SubTableFormDialog` / `SubTableAddDialog` Add 模式是否 seed `initialData`？Save 是否 **`mergeFormRowWithSeed`**（非 spread）？
- [ ] 子表 **Import Fields** 成功后是否调用 **`refreshFormRulesFromTableMetadata()`**（全量 sync 含已有 PK）？
- [ ] **`handlePreview`** 是否 `await fetchTables` + **`refreshFormRulesFromTableMetadata`** 后再 `buildPreview`？
- [ ] 子表 PK 分配是否用 `normalizeFieldDefinitionForRuntime` / `pkNeedsAllocation`？`bindingMap.fieldDefinitions` 是否来自最新 `store.tables`？
- [ ] 改 `subTableRowRuntime.ts` 时是否同步 **user-portal** 同文件（Portal parity）？
- [ ] Ref Table 变更是否仍自动填充 `refPrimaryKeyFields`（`FieldForeignKeyEditor`）？
- [ ] i18n：`subTable.fkGuardMainNotReady` 三语言仍在；仅 **manual 主键仍空** 时使用。
- [ ] **Process Start**（`start.vue`）是否加载 `dataTables` 并 `resolveBindingFieldDefinitions` / `enrichColumnsWithTableFieldDisplayNames`？
- [ ] Portal `SubTableField` 是否传 `parentTablesById` + `autoEnsurePrimaryRecord`，并处理 `primaryFormDataPatch`？
- [ ] Portal `SubTableAddDialog` Add 是否 seed `initialData`，Save 是否 `mergeFormRowWithSeed`？
- [ ] **To Do / Task detail** 子表 Add：`PortalPrimaryKeyAllocationComponent` 是否解析 **processDefinitionKey**？空 `pk_generation_json` 是否默认 uuid？
- [ ] admin **`attachTableBindings`** 是否仍用 `td.display_name`（非 `td.description`）？

## 9. 参考实现

```
pkGenerationConfig.ts          — parse/serialize 策略，PK_GENERATION_STRATEGIES
PkGenerationEditor.vue         — Table Design PK 策略 UI（compact 表格模式）
FieldForeignKeyEditor.vue      — FK popover；Ref Table → 自动 refPrimaryKeyFields
formFieldMeta.ts               — sync / readonly / normalizeFieldDefinitionForRuntime / isAutoGeneratedPrimaryKey
subTableAddDialogHelpers.ts    — mergeFormRowWithSeed, buildInitialRow
subTableRowRuntime.ts          — prepareSubTableAddRow, ensureParentRowsForChildAdd, pkNeedsAllocation
tableFkRuntime.ts              — guardBeforeChildRowAdd, applyFkToInitialRow, encodeCompositePrimaryKey
FormDesigner.vue               — handlePreview (fetchTables + refresh), handleConfirmImportFields (sub → refreshFormRulesFromTableMetadata), refreshFormRulesFromTableMetadata, previewParentTablesById
FormPreviewItems.vue           — mergePrimaryFormData, props 下发
SubTableField.vue              — openRowDialog, handleDialogSave (mergeFormRowWithSeed), allocatePrimaryKeys 注入
SubTableFormDialog.vue         — seedRow + initialData on add; handleSave → mergeFormRowWithSeed
SubTableAddDialog.vue          — seed initialData on add; handleSave → mergeFormRowWithSeed
FormRenderer.vue               — parentTablesById, primaryTableId, handlePrimaryFormDataPatch
processes/start.vue            — dataTables index, resolveSubTableBindingColumnsForStart, fieldDefinitions
formFieldMeta.ts / pkGenerationConfig.ts — Portal normalizeFieldDefinitionForRuntime (parity with DW)
platform-common (后端)          — JdbcPrimaryKeyAllocationService / PrimaryKeyAllocationService
TableDesignController          — POST .../primary-keys/allocate
PortalPrimaryKeyAllocationComponent — process key → dw id；空 pk_generation_json → uuid
FunctionUnitManagerComponent.attachTableBindings — td.display_name for dw tables
tasks/detail.vue / applications/detail.vue — functionUnitIdRef = processDefinitionKey
```

## 禁止

- Form Preview 对 auto-PK 字段展示可编辑 Input（应 disabled + 灰色只读样式，见 `form-readonly.scss`）。
- 前端自增 PK、或忽略 `pkGeneration` 元数据硬编码 UUID（须读字段定义 + 后端 allocate）。
- 仅 sync 顶层 form rule、忽略 `elCard.children` 内的 PK 字段。
- 子表 Add 分配了 PK 但对话框不加载 `initialData`，导致列表 PK 列为 `-`。
- Save 用 `{ ...seed, ...formData }` 覆盖 seed（须 `mergeFormRowWithSeed`）。
- 子表 Import Fields 后**不**调用 `refreshFormRulesFromTableMetadata`（仅 append 新 rule 不同步已有 PK readonly）。
- `handlePreview` 不 `fetchTables` 即 build（Table Design 变更后 Preview 用 stale `fieldDefinitions`）。
- 在 Preview 子组件内重复实现 guard/allocate，而不复用 `subTableRowRuntime.ts`。
- UI 暴露已废弃的 PK scope（`perFunctionUnit` / `perPrefix`）或与 `serializePkGeneration` 不一致的序列化。
