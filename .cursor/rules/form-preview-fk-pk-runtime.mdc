---
description: Form Preview FK/PK runtime — pkGeneration rules, readonly auto-PK, allocate API, deep rule sync, sub-table add orchestration (Save-time PK), Import Fields metadata sync, mergeFormRowWithSeed, regression bug catalog
globs: frontend/developer-workstation/**/*.{vue,ts},frontend/user-portal/src/utils/{subTableRowRuntime,tableFkRuntime,formFieldMeta,pkGenerationConfig,inlineFormBelowTableRuntime}.ts,frontend/user-portal/src/components/{SubTableField.vue,SubTableAddDialog.vue,SubTableInlineForm.vue,subTableAddDialogHelpers.ts,FormRenderer.vue,FormRendererFields.vue},frontend/user-portal/src/views/{processes/start.vue,tasks/detail.vue,applications/detail.vue},backend/user-portal/src/main/java/com/portal/component/PortalPrimaryKeyAllocationComponent.java,backend/admin-center/src/main/java/com/admin/component/FunctionUnitManagerComponent.java
alwaysApply: false
---

# Form Preview × FK/PK 运行时（DW 设计侧 + Portal 生产）

与 `portal-design-parity.mdc` 配套：**Form Preview 弹窗 = 设计稿运行时**；PK 生成、FK 填充、主表就绪、只读呈现 MUST 与 PRD（`docs/table-design-fk-pk-requirements.md`）及 Portal 共享 runtime 一致。

---

## 0. 子表新增主键分配时机（Portal 生产 SoT — 改代码前必读）

**核心契约（2026-06-08 起，User Portal 全部子表路径）：**

| 时机 | 主表 PK | 子表 PK | 结构 FK（如 `main_id`） |
|---|---|---|---|
| 点击 **+Add**（打开对话框） | **不** allocate | **不** allocate | 仅当父行**已有** PK 时预填；主表空则 FK 也空 |
| 点击 **Save**（真正创建行） | 若主表尚无 auto-PK → **先** allocate 主表 | **后** allocate 子表 | `applyFkToInitialRow` 用主表 PK 填充 |

**禁止**在 `openAddRowDialog` / `prepareSubTableAddRow`（`deferPkAllocationUntilSave: true`）路径上 allocate 任何 PK。  
**禁止**在 Save 之前分配子表 PK 再打开对话框（会导致用户取消 Add 时已消耗序号）。

### 0.1 Add 打开对话框（`prepareSubTableAddRow` + `deferPkAllocationUntilSave`）

```
prepareSubTableAddRow（deferPkAllocationUntilSave: true）
  → filterStructuralFkMetasForBinding
  → 跳过 guard 阻断 + 跳过 ensureParentRowsForChildAdd   // 主表空也允许开对话框
  → applyFkPresentationToDialogColumns（auto-PK readonly；prefixedSequence/uuid 降级 text）
  → buildInitialRow
  → applyFkToInitialRow（仅父行已有 PK 时填 FK）
  → applyMiParticipantRowSeedToInitialRow（仅 miParticipantRowId 存在时）
  → 不调用 allocateChildRowAutoPrimaryKeys
```

`SubTableField.openAddRowDialog` MUST 传 `deferPkAllocationUntilSave: true`（所有 Portal 子表新增）。

### 0.2 Save 创建行（`finalizeSubTableRowOnSave` — 唯一 PK allocate 入口）

```
finalizeSubTableRowOnSave（SubTableField.handleDialogSave → SubTableAddDialog.saveRow）
  ① ensureParentRowsForChildAdd     // 主表无 auto-PK → 先 allocate 主表（tableId=PRIMARY）
  ② guardBeforeChildRowAdd          // manual 主表 PK 仍空 → 失败，弹 fkGuardMainNotReady
  ③ applyFkToInitialRow             // 结构 FK 填主表 PK 值
  ④ applyMiParticipantRowSeedToInitialRow（MI 附件等，有 miParticipantRowId 时）
  ⑤ allocateChildRowAutoPrimaryKeys // 子表非 manual PK
  ⑥ primaryFormDataPatch → update:primaryFormData → FormRenderer.handlePrimaryFormDataPatch
  ⑦ mergeFormRowWithSeed 后 push 行
```

**顺序不可颠倒：** 子表 PK allocate（⑤）MUST 在父表 PK 就绪（①②③）之后。

### 0.3 关键函数对照

| 函数 | 调用时机 | 职责 |
|---|---|---|
| `prepareSubTableAddRow` | +Add | 开对话框；`deferPkAllocationUntilSave` 时不 allocate |
| `finalizeSubTableRowOnSave` | Save | 主表 PK → FK → 子表 PK 完整编排 |
| `ensureParentRowsForChildAdd` | Save（非 Add） | 主表 auto-PK 补全 |
| `allocateChildRowAutoPrimaryKeys` | Save（非 Add） | 子表 auto-PK；含 MI seed 跳过规则 |
| `mergeFormRowWithSeed` | Save | 保留 readonly 字段已分配值 |

### 0.4 DW Form Preview 与 Portal 差异

| 环境 | 默认时机 | 说明 |
|---|---|---|
| **User Portal**（`SubTableField`） | **Save** | `deferPkAllocationUntilSave: true` + `finalizeSubTableRowOnSave` |
| **DW Form Preview**（`FormDesigner`） | 可为 Add 时 allocate | `prepareSubTableAddRow` 无 defer 时仍走 §2.4 旧路径；**改 Portal 逻辑不得破坏 DW 无 defer 测试** |

改 `subTableRowRuntime.ts` 时：Portal 行为以 §0 为准；DW 无 `deferPkAllocationUntilSave` 的路径须保持可测。

---

## 1. 主表 / 子表字段只读（非 manual PK、readonly FK）

| 条件 | Form Preview / 画布 rule |
|---|---|
| `isPrimaryKey` 且 `pkGeneration.strategy !== 'manual'` | **Readonly**（`props.readonly` → form-create `disabled`） |
| 结构 FK 且 `fkDisplayMode === 'readonly'` | **Readonly** |
| 结构 FK 且 `fkDisplayMode === 'hidden'` | **不渲染**（runtime 仍填充） |

**实现 MUST 走共享工具，禁止在 Preview 组件内手写 if：**

- `formFieldMeta.ts` — `isAutoGeneratedPrimaryKey`、`applyTableFieldMetaToFormRule`、`syncFormRulesWithTableFields`
- `normalizeFieldDefinitionForRuntime` — 统一 `pkGenerationJson`、`primaryKey`（Jackson 别名）、默认 UUID
- `formCreateRuleUtils.ts` — `mapFormCreateRulesReadonlyDeep`（Preview 渲染前）
- `registerFormCreateReadonlyParser.ts` — 全局 `props.readonly` → `disabled`

**深度同步（必须）：** `syncFormRulesWithTableFields` MUST **递归** `elCard` / `fcRow` / `group` 等 `children`（及 `props.children|list|items|fields`）。仅 sync 顶层 rule 会导致 card 内 `id` 仍可读——这是已知缺陷模式。

`handlePreview` / `buildSavedFormPreviewItems` 路径：`syncFormRulesWithTableFields` → `mapFormCreateRulesReadonlyDeep`。

---

## 2. 主键生成规则（`pkGeneration`）

### 2.1 设计态（Table Design / AC Relation Table 结构）

字段勾选 **`isPrimaryKey`** 后配置 `pkGeneration`（DW：`FieldDefinition.pkGeneration` / `pkGenerationJson`；AC：`RelationFieldDTO` 等价字段）：

| 策略 | 说明 | Preview / Portal 行为 |
|---|---|---|
| `uuid` | 后端生成 UUID（**新建 PK 默认**） | 只读 + 后端 `allocate` |
| `manual` | 用户手填 | 可编辑；为空时阻断依赖其 FK 链的子表 **Save** |
| `autoIncrement` | 后端递增整数 | 只读 + 后端 `allocate` |
| `prefixedSequence` | `{prefix}{zeroPaddedSeq}`，如 `Test-000001` | 只读 + 后端 `allocate` |

**配置项：**

- `autoIncrement` / `prefixedSequence`：`startValue`（默认 1）
- `prefixedSequence` 额外：`prefix`、`padWidth`（默认 6）
- **作用域固定 `perTable`**（每张表独立连续编号）；序列化时写 `scope: 'perTable'`；UI **不提供** `perFunctionUnit` / `perPrefix`
- **联合主键**：同表多个字段 `isPrimaryKey=true`；每个 PK 列可独立配置 strategy（通常一致）；FK 引用侧 `refPrimaryKeyFields` MUST 列全目标表 PK 列名

**Table Design UI（DW）：**

- `PkGenerationEditor.vue` + `pkGenerationConfig.ts` — 策略下拉 + gear popover（起始值 / 前缀 / 位数）
- `FieldForeignKeyEditor.vue` — 选 Ref Table 后 **自动填充** `refPrimaryKeyFields` 为目标表全部 PK 列名
- 表名 **全局唯一**（DW ↔ AC 互检）；见 Table Design / AC structure 保存前 `name-available` 校验

### 2.2 运行时识别（Preview / Portal 共用）

**禁止**前端自增、禁止忽略元数据仅用 UUID fallback。

| 函数 | 职责 |
|---|---|
| `normalizeFieldDefinitionForRuntime` | 统一 `isPrimaryKey`（含 `primaryKey` Jackson 别名）；PK 且缺 `pkGeneration` → **`{ strategy: 'uuid' }`** |
| `isAutoGeneratedPrimaryKey` | `isPrimaryKey && strategy !== 'manual'` → 画布/Preview **readonly** |
| `pkNeedsAllocation`（`subTableRowRuntime.ts`） | 同上；用于 Save 路径是否调用 `allocatePrimaryKeys` |
| `parsePkGeneration` / `serializePkGeneration` | 读写策略；非 manual 序列化带 `scope: 'perTable'` |

### 2.3 后端分配 API

```
POST .../primary-keys/allocate
Body: { tableId, fieldName, count?: 1, scopeKey?: functionUnitId }
Response: { values: string[] }
```

| 模块 | 入口 |
|---|---|
| DW Preview | `functionUnitApi.allocatePrimaryKeys` → `TableDesignController` |
| Portal | `PortalPrimaryKeyAllocationComponent` / 等价 REST |
| AC 数据页 | `RelationTablePrimaryKeyAllocationService` |
| 共享实现 | `platform-common` — `JdbcPrimaryKeyAllocationService`（DB 序号表，`scope_type=perTable`） |

**Portal 后端 MUST 与 DW 对齐的两点：**

1. **`pk_generation_json` 为空但 `is_primary_key=true`**：`loadPkGenerationJson` 返回空 Map → `toPkConfig` 默认 **`uuid`**（禁止抛 `NOT_PK_FIELD`）。
2. **`functionUnitId` 解析**：Process Start 传 FU **code**（route param）；**To Do / My Request / Task detail** 传 Flowable **`processDefinitionKey`**（如 `Process_1_KK`）。`PortalPrimaryKeyAllocationComponent` MUST：先 direct `dw_function_units` id/code → 再 `FunctionUnitAccessComponent.resolveFunctionUnitId`（process key → catalog id）→ `sys_function_units.code = dw_function_units.code` 映射 **dw numeric id**。禁止仅按 code 查而忽略 process key。

**`tableBindings.fieldDefinitions` 前置条件（Portal 子表 Add）：** admin `attachTableBindings` 取 designer 表元数据时 MUST 用 `td.display_name`（**非** `td.description`——`dw_table_definitions` 无 description 列）；否则 FU content 失败 → binding 无 `fieldDefinitions` → `parentTablesById` / allocate 链断裂。

- **并发安全**：后端计数；**不在**前端、流程变量或 JSON 行里模拟 SERIAL
- 已有非空 PK 值：**不覆盖**（allocate 跳过已有值）

### 2.4 PK 分配时机（分路径）

#### Portal 生产（`deferPkAllocationUntilSave: true`）— 见 §0

- **+Add**：不 allocate 主表/子表 PK
- **Save**：`finalizeSubTableRowOnSave`（主表 → FK → 子表）

#### DW Form Preview（无 defer，遗留路径）

| 场景 | 行为 |
|---|---|
| **子表 Add**（`prepareSubTableAddRow`） | guard / ensure 父行 → `applyFkToInitialRow` → 子表 **非 manual** PK `allocatePrimaryKeys` |
| **主表 PK 未就绪** | `ensureParentRowsForChildAdd` → `primaryFormDataPatch` |
| **主表 PK 为 `manual` 且仍为空** | **阻断** Add；`subTable.fkGuardMainNotReady` |

```
prepareSubTableAddRow（无 defer）
  → filterStructuralFkMetasForBinding
  → guardBeforeChildRowAdd
  → ensureParentRowsForChildAdd（autoEnsurePrimaryRecord）
  → applyFkToInitialRow
  → allocateChildRowAutoPrimaryKeys
  → 对话框 initialData seed
```

### 2.5 与 FK / 联合 PK 的交互

- FK 填充源：父行 **实际 PK 列值**（`tableFkRuntime.ts` — `applyFkToInitialRow`）；用 **`fieldName`**（如 `id_idw`），非 `displayName`（如 `id`）
- 联合 PK → 单 FK 字段：按 PRD 5.3 合成（`encodeCompositePrimaryKey`）；FK 字段 `length` 须能容纳合成串
- 子表 Save 依赖主表 PK：**manual 主表 PK 必须先有值**；auto-PK 主表由 `ensureParentRowsForChildAdd` 在 **Save** 时补全

---

## 3. 子表 Save — 主表未就绪时自动建主表行

**禁止**在 Portal 仅弹 `subTable.fkGuardMainNotReady` 而不在 Save 时尝试补全主表（`autoEnsurePrimaryRecord`）。

Save 时结构 FK guard 失败：

1. `autoEnsurePrimaryRecord === true` 且存在 `parentTablesById` + `allocatePrimaryKeys`
2. `ensureParentRowsForChildAdd` — 对缺失父 PK 的 `refTableId` allocate auto-PK
3. `primaryFormDataPatch` + `update:primaryFormData` 写回主表单
4. 重新 guard；**仍失败**（manual 主键仍空）才 `fkGuardMainNotReady`

**FormRenderer / SubTableField 必须传入：** `primaryTableId`、`parentTablesById`（PRIMARY + SUB `fieldDefinitions`）、`autoEnsurePrimaryRecord: primaryFormData != null`。

---

## 4. 子表 Add 对话框 — seed 与 Save（禁止丢 id）

### 4.1 `mergeFormRowWithSeed`（Save 时保留已分配 PK/FK）

**位置：** `subTableAddDialogHelpers.ts` — `mergeFormRowWithSeed(seed, form)`

**语义：** 以 `form` 为基底；对 `seed` 中每个 key，若 `form[key]` 为空且 `seed[key]` 非空，则回退 seed。用户手填非空值优先。

```typescript
// ✅ 正确
const row = mergeFormRowWithSeed(seedRow, formData)

// ❌ 禁止 — form-create readonly 常写 null，抹掉 id_idw
const row = { ...seedRow, ...formData }
```

| 组件 | Save 入口 |
|---|---|
| `SubTableFormDialog.vue` | `mergeFormRowWithSeed(seedRow, formData)` |
| `SubTableAddDialog.vue` | `saveRow` async → 父级 `finalizeSubTableRowOnSave` 后再关对话框 |
| `SubTableField.vue` | `handleDialogSave` → `finalizeSubTableRowOnSave` |

**禁止** Add 模式 `formData = {}` 丢弃 seed；**禁止** Save 用 spread 覆盖 seed。

### 4.2 auto-PK 列控件类型（`inputNumber` × `prefixedSequence` / `uuid`）

子表 subForm 对 PK 用 `inputNumber`（字段 `id_idw`），但策略为 `prefixedSequence` / `uuid` → allocate 返回**字符串**（`Test-000001`）。`el-input-number` 无法绑定 → 校验 **`id is required`**（label 是 displayName，key 是 `id_idw`）。

**MUST：** `applyFkPresentationToDialogColumns` 对 auto-PK 且 strategy 为 `prefixedSequence`/`uuid`、列 type 为 `number` 时 **降级为 `text` readonly**。

### 4.3 `buildRules` — readonly 列不加 required

`subTableAddDialogHelpers.buildRules`：**`col.readonly === true` 时不生成 required 规则**。  
form-create disabled 字段常回写空值；若仍 required 会误报「id is required」即使 Save 路径会 allocate。

### 4.4 `fieldName` vs `displayName`（高频混淆）

| 概念 | 示例 | 用途 |
|---|---|---|
| `fieldName` | `id_idw` | 行数据 key、allocate API `fieldName`、`applyAutoPkColumnPresentation` 匹配 |
| `displayName` | `id` | 列表/对话框 **label**（`subListViews.columns[].displayName`） |

- `subListViews` MUST 用 `fieldName: "id_idw"` + `displayName: "id"`（非把列 field 写成 `id`）
- `applyAutoPkColumnPresentation` 按 `col.field === fieldDef.fieldName` 匹配；列 field 错误则 readonly/allocate/type 降级全部失效
- 行 JSON 存 `id_idw`，列表列 prop 也是 `id_idw`；显示名可为 `id`

---

## 5. Import Fields × 表元数据同步（防 PK/FK 回归）

**已知缺陷模式：** Table Design 改 PK 后 Import Fields / Preview 未 refresh → PK 不按设计生成。

| 根因 | 后果 |
|---|---|
| Import 只 append 新 rule，未 sync **已有** PK rule | PK 仍可读 `inputNumber`，Save 回写空 |
| Preview 用陈旧 `store.tables` | `fieldDefinitions` 缺 `pkGenerationJson` → 不 allocate |
| Save 用 spread 覆盖 seed | 列表 PK 列 `-` |

**MUST：** `refreshFormRulesFromTableMetadata` 全量 sync；`handlePreview` 前 `fetchTables` + refresh。

列表列 `displayName` 可为 `id`，**行数据 key MUST 为 `fieldName`（如 `id_idw`）**。

---

## 6. MI 与结构 FK 分流（高频回归区）

### 6.1 结构 FK vs legacy `foreignKeyField`

- `bindingLinkMode === 'miParticipantRow'`：`filterStructuralFkMetasForBinding` **排除** legacy `foreignKeyField`，不走结构 FK guard/fill（如 Sub Task binding 上 `foreignKeyField=id_idw` 不参与 `main_id` 结构链）
- 结构 FK 以 `fieldDefinitions` 中 `isForeignKey` + `refTableId` 为准（如 `main_id` → table 19 PK `id`）

### 6.2 MI 行 PK seed vs allocate（#1429 / #1431 回归）

**场景 A — 流程发起 / 无 `miParticipantRowId`（Sub Task collection `id_idw`）：**

- `bindingLinkMode=miParticipantRow`，`foreignKeyField=id_idw`（与行 PK 同名）
- **MUST** 在 **Save** 时 `allocateChildRowAutoPrimaryKeys` 生成 `id_idw`（`Test-00000x`）
- **禁止** 因 `legacyFkIsRowPk` 无条件跳过 allocate

**场景 B — 待办 MI 附件 / 有 `miParticipantRowId`（如 `row_id`）：**

- **MUST** `applyMiParticipantRowSeedToInitialRow` 从参与者行 seed `row_id`
- **MUST** 跳过对该字段的 allocate（`miParticipantSeedPresent === true` 时才跳过）

```typescript
// ✅ allocateChildRowAutoPrimaryKeys 跳过条件（四者同时满足才跳过）
bindingLinkMode === 'miParticipantRow'
&& legacyFkIsRowPk
&& field.fieldName === legacyFk
&& miParticipantRowId != null && trim() !== ''

// ❌ 禁止缺少 miParticipantRowId 判断 — 会导致流程发起 Sub Task 永不生成 id_idw
```

### 6.3 `bindingForeignKeyFieldIsRowPrimaryKey`

当 `foreignKeyField` 名与行 PK 相同（`id_idw`），它是 MI 参与者链接语义，**不是**结构 FK。`seedLinkChildForeignKeysFromParentRow` 不得把父 `id_idw` 写入子行 PK。

### 6.4 MI form-below-table × Link Form 子表（People / `sub_task_id` — #1435）

**场景：** 待办 MI 子任务；设计师 `assigneeTodo = formBelowTable`；父表 Sub Task collection 上 Link Form 列指向子表（如 People binding 30）；子行 FK `sub_task_id` → 父 `id_idw`。

| 契约 | 说明 |
|---|---|
| **数据 hydrate** | `mergeRowsForInlineFormTarget` 在 `suppressLinkFormInitialData`（MI 隔离）下从父行 `__subTables__` 拉嵌套子行；**多于 1 条**嵌套行时 MUST 用 `sub_task_id` / `currentMiRowId` 对齐（`resolveMiLinkIsolateInlineRow`），禁止仅在 `rows.length === 1` 时选行否则内联表单空白 |
| **FK 展示** | 结构 FK `sub_task_id`（`fkDisplayMode: readonly`）显示 ID 值（如 `Test-000057`），非父表 lookup 展开；业务字段（age/sex）来自对齐后的子行本身 |
| **Save 管道** | 与 `SubTableAddDialog` **不同**：走 `SubTableInlineForm` → `handleInlineFormUpdate` → `syncMainSubTableRows` → 任务 **Save**（`saveCurrentTaskFormWithMiPersist`） |
| **Save 前 flush** | `SubTableInlineForm` 点击 Save MUST **先** `emit('update:row', rowModel)` **再** `emit('save')`，确保未触发 field-change 的编辑也写入 binding |
| **PK allocate 时机** | 任务 Save / Complete 前：`seedMiParticipantScopedBindingForeignKeys(myRowId, { allocateMissingPrimaryKeys: true })` → `ensureAutoPrimaryKeysForRows`；**不在**页面 load 时 allocate（避免空 link-form stub 风暴） |
| **Link 目标 binding 元数据** | `mergeLinkFormTargetBindingsInto`（`tasks/detail.vue`、`applications/detail.vue`）合并 Link Form 目标时 MUST 带 `fieldDefinitions` + `bindingLinkMode`；遗漏则 People `id` 等 auto-PK **永不 allocate** |
| **嵌套持久化** | Save 后子行须同步到 `parentRow.__subTables__[childBindingId]`（`syncMiLinkChildRowsIntoParentNested`），reload / 流程图才可见 |

```typescript
// ✅ getCurrentRowForInlineForm — MI link isolate 多行
resolveMiLinkIsolateInlineRow(rows, parentId, findInlineRowIndexForMi, pickPreferredInlineRow)

// ✅ mergeLinkFormTargetBindingsInto — 禁止省略
bindings.push({ ...raw, fieldDefinitions: raw.fieldDefinitions ?? [], bindingLinkMode: raw.bindingLinkMode })

// ✅ SubTableInlineForm Save
emit('update:row', merged); emit('save')  // 顺序固定
```

**DW ↔ Portal parity（form-below-table Save 按钮）：**

| 位置 | 组件 |
|---|---|
| Portal 运行时 | `SubTableInlineForm.vue` — 内联 Save → `FormRenderer` `@save` → `saveCurrentTaskFormWithMiPersist` |
| DW Form Preview | `SubTableField.vue` — `preview-inline-form-below` 底部 Save（可编辑预览时可用） |
| DW Portal Display 编辑器 | `SubTableListView.vue` — 双栏 To Do 条带底部 Save（设计态只读可 disabled，**布局须一致**） |

改 Portal 内联 Save / hydrate 时，DW 对应条带 MUST 同步（`portal-design-parity.mdc`）。

**手测（Process_1_KK / sub form1 / People）：**

1. `_currentItem.id_idw = Test-000057` → 内联表单带出 `sub_task_id`、age、sex（非空白）
2. 点内联 **Save**（或底部 Action SAVE）→ People `id` 生成 UUID；reload 后仍可见
3. DW Form Preview → form below table 可见同款 Save 按钮位置

### 6.5 My Request Details 弹窗（Link Form 只读 — #1441）

**场景：** 发起人 `applications/detail.vue`；Sub Task 上 Link Form 列 **Details** → 弹窗「Add subtable2 A」；`viewContext=initiatorRequest`、`editable=false`。

| 契约 | 说明 |
|---|---|
| **sub task id** | 父 Sub Task 参与者 FK；未处理行 MUST 从父 `id_idw` 回填（如 `Test-000060`） |
| **id（PK）** | People 行 **Save 后 allocate 的 UUID**；未处理行 MUST **空**；禁止把父 `id_idw` 写入 `id` |
| **number + UUID** | 只读弹窗：`resolveLinkFormFieldValueForModal(..., { readonly: true })` 保留 UUID 字符串；`FieldRenderer.showNumberAsText` |
| **Sub Task 行数** | 发起人 MI collection **禁止** `mergeAllSubTableSlicesFromVariables`（见 `portal-mi-subtable-my-request.mdc` §#1441） |
| **数据回退** | `collectLinkFormRowsFromProcessVariables` + `hydrateMiLinkChildBindingsForInitiatorMyRequest`；`backfillMiLinkFormModalFieldsFromParent` |

**验证：** `node frontend/scripts/verify-myrequest-details-modal.mjs`；截图 `verification-screenshots/…-060-unprocessed.png`、`…-061-filled.png`。

---

## 7. Portal 流程发起 / 待办 / 我的申请

与 `portal-design-parity.mdc` 配套；**全部**经 `SubTableField` + §0 Save 管道。

### 7.1 Process Start（`start.vue`）

```
loadFunctionUnitContent
  → cachedRelationTableFieldIndex ← dataTables
  → fieldDefinitions ← resolveBindingFieldDefinitions（tableBindings 优先）
  → primaryTableBinding.fieldDefinitions → parentTablesById
SubTableField
  → deferPkAllocationUntilSave: true
  → Save → finalizeSubTableRowOnSave
```

### 7.2 `functionUnitId` 来源

| 视图 | 传入值 | 后端解析 |
|---|---|---|
| Process Start | FU **code**（`route.params.key`） | `dw_function_units.code` |
| To Do / My Request / Task detail | **`processDefinitionKey`** | process key → catalog → dw id |

### 7.3 Portal 组件契约

| 组件 | MUST |
|---|---|
| `FormRenderer` | `parentTablesById`、`primaryTableId`、`handlePrimaryFormDataPatch`；`getCurrentRowForInlineForm` / `handleInlineFormUpdate`；`@save` → 任务 persist |
| `SubTableInlineForm` | form-below-table 内联表单；Save 先 flush `update:row` 再 `save`；与 DW 按钮位置 parity |
| `SubTableField` | `deferPkAllocationUntilSave`；**对话框** Save 用 `finalizeSubTableRowOnSave`；`saveRow` async |
| `SubTableAddDialog` | `saveRow` prop；Save loading；失败不关对话框 |
| `subTableRowRuntime.ts` | §0 函数齐全；改后跑 `subTableRowRuntime.test.ts` |
| `inlineFormBelowTableRuntime.ts` | MI link isolate 多行选行；改后跑 `inlineFormBelowTableRuntime.test.ts` |
| `tasks/detail.vue` | `mergeLinkFormTargetBindingsInto` 含 `fieldDefinitions`；`seedMiParticipantScopedBindingForeignKeys` 在任务 Save 时 `allocateMissingPrimaryKeys: true` |

### 7.4 待办 MI — form-below-table Save（非 Add 对话框管道）

与 §0 `finalizeSubTableRowOnSave` **并列**的第二条 Save 管道，仅用于 **表格下内联表单**：

```
SubTableInlineForm.handleSaveClick
  → emit('update:row')           // flush 当前 rowModel
  → emit('save')
FormRenderer.handleInlineFormSave → detail.saveCurrentTaskFormWithMiPersist
  → mergeMiParticipantScalarsFromForm（MI）
  → seedMiParticipantScopedBindingForeignKeys({ allocateMissingPrimaryKeys: true })
      → materializeMiLinkChildBindingRowsFromParents（binding.data 空时从父 __subTables__ 物化）
      → seedLinkChildForeignKeysFromParentRow（sub_task_id 等）
      → repairMisassignedPrimaryKeyFromParentId（清除误拷父 id_idw 到子 PK）
      → ensureAutoPrimaryKeysForRows（依赖 binding.fieldDefinitions — 见 §6.4）
      → syncMiLinkChildRowsIntoParentNested
  → buildCurrentTaskFormSubmitPayload → apiSubmitTaskForm → loadTaskDetail（MI）
```

**禁止**假定 form-below-table 走 `SubTableAddDialog` / `finalizeSubTableRowOnSave`；**禁止**在 `mergeLinkFormTargetBindingsInto` 省略 `fieldDefinitions` 后仍期望 Save allocate 子表 PK。

---

## 8. 已知回归缺陷模式（改 FK/PK 代码必读）

每次改 `subTableRowRuntime.ts` / `SubTableField.vue` / `prepareSubTableAddRow` / Save 路径，对照本表自测。

| ID | 症状 | 根因 | 修复要点 |
|---|---|---|---|
| **R1** | Add 对话框 `id is required`，PK 空 | Add 时 allocate + `inputNumber` 绑不了字符串 PK；或 `buildRules` 对 readonly 仍 required | Save 时 allocate；`applyFkPresentationToDialogColumns` 降级 text；`buildRules` 跳过 readonly |
| **R2** | 流程发起 Sub Task Add 后 `id_idw` 永远空 | `miParticipantRow` + `foreignKeyField=id_idw` 时**无条件**跳过 PK allocate | 仅 `miParticipantRowId` 存在时才跳过 |
| **R3** | Add 时就消耗 `Test-00000x`，取消也跳号 | Add 路径调用 `allocatePrimaryKeys` | `deferPkAllocationUntilSave`；仅 `finalizeSubTableRowOnSave` allocate |
| **R4** | 主表空时 Add 就生成主表 UUID | Add 路径 `ensureParentRowsForChildAdd` | defer 时 Add 跳过；Save 再 ensure 主表 |
| **R5** | Save 后列表 `id` 列 `-` | Save 用 `{...seed,...form}` 或 seed 未合并 | `mergeFormRowWithSeed` |
| **R6** | `fieldDefinitions` 空，从不 allocate | `attachTableBindings` 用 `td.description`（列不存在）或 FU content 失败 | `td.display_name`；`resolveBindingFieldDefinitions` 回退 dataTables |
| **R7** | `FUNCTION_UNIT_NOT_FOUND` on allocate | 待办传 code 而非 `processDefinitionKey` | 后端 `resolveFunctionUnitId` 支持 process key |
| **R8** | `NOT_PK_FIELD` 主表 allocate 失败 | 空 `pk_generation_json` 抛错 | 默认 `uuid` 策略 |
| **R9** | auto-PK 列可编辑 / 非 readonly | 仅 sync 顶层 rule 或 Import 未 refresh | 递归 `syncFormRulesWithTableFields` + `refreshFormRulesFromTableMetadata` |
| **R10** | `main_id` 空但子表 Save 成功 | Save 未走 `applyFkToInitialRow` 或主表 PK 未先 ensure | §0.2 顺序 ①→③ |
| **R11** | 列头显示 `id_idw` 而非 designer 名 | 未 `enrichColumnsWithTableFieldDisplayNames` | start/detail 加载 dataTables 索引 |
| **R12** | MI form-below-table 内联表单空白，变量里已有 People 行 | `suppressLinkFormInitialData` + 嵌套 `rows.length > 1` 时未按 `sub_task_id` 选行 | `resolveMiLinkIsolateInlineRow` + `findInlineRowIndexForMi` |
| **R13** | 内联 Save 后 People `id` 仍空 | `mergeLinkFormTargetBindingsInto` 缺 `fieldDefinitions` → `ensureAutoPrimaryKeysForRows` 跳过 | 合并时带 `raw.fieldDefinitions` + `bindingLinkMode` |
| **R14** | 点 Save 未持久化最新内联编辑 | Save 未 flush `rowModel` 到 binding | `SubTableInlineForm` 先 `update:row` 再 `save` |
| **R15** | DW Preview 与 Portal form-below-table UI 分叉 | 仅 Portal 有内联 Save | DW `SubTableField` + `SubTableListView` 同款 Save 条带 |
| **R16** | My Request Details 弹窗 id/age 空或 id=Test-xxxx | 父 `id_idw` 误写入 link-child `id`；allSlices 污染 Sub Task；`linkFormRowsLackFormPayload([])` false | `backfillMiLinkFormModalFieldsFromParent`；发起人 collection 仅 own slice；§6.5 |

**手测清单（Process_1_KK / Sub Task binding 64）：**

1. 主表 `id` 空 → Add Sub Task → 对话框 `id`/`main id` 均可空 → Save → 主表有 UUID，子表有 `Test-00000x` + `main_id`
2. 主表已有 `id` → Add → `main_id` 预填 → Save → 仅子表 `id_idw` 新增
3. 待办 MI 附件 + `miParticipantRowId` → Add → `row_id` seed，不 allocate 新 `row_id`

**手测清单（Process_1_KK / sub form1 / People binding 30 — form-below-table）：**

1. 任务含 `_currentItem` → People 内联表单非空（`sub_task_id`、age、sex）
2. 内联 **Save** → People `id` 生成；reload 后 id 仍在
3. DW Form Preview → form below table 底部可见 Save（与 Portal 同位置）

---

## 9. 变更检查清单

- [ ] Portal 子表 Add 是否 **`deferPkAllocationUntilSave: true`**？PK 仅 **`finalizeSubTableRowOnSave`**？
- [ ] Save 顺序：主表 PK → FK → 子表 PK？`primaryFormDataPatch` 是否回写主表单？
- [ ] `miParticipantRow` 跳过 allocate 是否**仅**在 `miParticipantRowId` 存在时？
- [ ] `applyFkPresentationToDialogColumns` 是否将 prefixedSequence/uuid 的 number 列降级 text？
- [ ] `buildRules` 是否跳过 readonly 列 required？
- [ ] Save 是否 **`mergeFormRowWithSeed`**（非 spread）？
- [ ] 行数据 key 是否用 **`fieldName`**（`id_idw`），非 displayName（`id`）？
- [ ] `fieldDefinitions` / `parentTablesById` / `functionUnitId` 是否传入 Save 路径？
- [ ] manual 主表 PK 空时 Save 是否阻断（不误 allocate）？
- [ ] 改 `subTableRowRuntime.ts` 是否跑 **`subTableRowRuntime.test.ts`**（含 defer + 主表空 Save 用例）？
- [ ] DW 无 defer 路径是否仍通过 **`prepareSubTableAddRow autoEnsurePrimaryRecord`** 测试？
- [ ] admin **`attachTableBindings`** 仍用 **`td.display_name`**？
- [ ] i18n：`subTable.fkGuardMainNotReady` 三语言仍在。
- [ ] MI form-below-table：`rows.length > 1` 时是否仍按 `sub_task_id` / `currentMiRowId` hydrate（§6.4）？
- [ ] `mergeLinkFormTargetBindingsInto` 是否带 **`fieldDefinitions`**（Link 目标子表 Save allocate）？
- [ ] `SubTableInlineForm` Save 是否 **先 flush `update:row`** 再 persist？
- [ ] 任务 Save 是否走 `seedMiParticipantScopedBindingForeignKeys({ allocateMissingPrimaryKeys: true })`（非仅 Add 对话框管道）？
- [ ] DW `SubTableField` / `SubTableListView` form-below-table 是否与 Portal **Save 按钮 parity**？
- [ ] 改 `inlineFormBelowTableRuntime.ts` 是否跑 **`inlineFormBelowTableRuntime.test.ts`**？

---

## 10. 参考实现

```
subTableRowRuntime.ts
  prepareSubTableAddRow          — Add 开对话框（deferPkAllocationUntilSave）
  finalizeSubTableRowOnSave      — Save 唯一 PK 编排（Portal SoT）
  ensureParentRowsForChildAdd    — 主表 auto-PK（Save 时）
  allocateChildRowAutoPrimaryKeys — 子表 auto-PK（Save 时）
  applyMiParticipantRowSeedToInitialRow — MI seed（有 miParticipantRowId）
tableFkRuntime.ts              — guardBeforeChildRowAdd, applyFkToInitialRow
subTableAddDialogHelpers.ts    — mergeFormRowWithSeed, buildRules, resolveBindingFieldDefinitions
SubTableField.vue              — deferPkAllocationUntilSave; handleDialogSave → finalizeSubTableRowOnSave
SubTableInlineForm.vue         — form-below-table; Save flush update:row → save
SubTableAddDialog.vue          — saveRow async + saving loading
FormRenderer.vue               — getCurrentRowForInlineForm, handleInlineFormUpdate, @save
inlineFormBelowTableRuntime.ts — resolveMiLinkIsolateInlineRow（MI 多行嵌套选行）
tasks/detail.vue               — mergeLinkFormTargetBindingsInto + seedMiParticipantScopedBindingForeignKeys
applications/detail.vue        — mergeLinkFormTargetBindingsInto（同 tasks 契约）
developer-workstation/SubTableField.vue      — preview-inline-form-below Save parity
developer-workstation/SubTableListView.vue   — inline-form-below Save parity
processes/start.vue            — dataTables, fieldDefinitions, primaryTableBinding
PortalPrimaryKeyAllocationComponent — process key → dw id; 空 pk_generation_json → uuid
FunctionUnitManagerComponent.attachTableBindings — td.display_name
subTableRowRuntime.test.ts     — defer Save 路径 + 主表空时先主后子
inlineFormBelowTableRuntime.test.ts — MI link isolate 多行 FK 对齐
```

---

## 禁止

- Portal 在 **+Add** 时 allocate 主表或子表 PK（须 `deferPkAllocationUntilSave`）。
- Save 时**先**子表 PK **后**主表 PK（顺序颠倒）。
- `miParticipantRow` 在无 `miParticipantRowId` 时跳过 collection PK allocate（如流程发起 `id_idw`）。
- 前端自增 PK；忽略 `pkGeneration` 硬编码 UUID。
- Save 用 spread 覆盖 seed；Add 丢弃 `initialData`。
- 列 `field` 用 displayName（`id`）而非 fieldName（`id_idw`）导致 metadata 匹配失败。
- `buildRules` 对 readonly auto-PK 加 required。
- 子表 Import Fields 后不 `refreshFormRulesFromTableMetadata`。
- Preview 子组件内重复实现 guard/allocate，不复用 `subTableRowRuntime.ts`。
- UI 暴露已废弃 PK scope（`perFunctionUnit` / `perPrefix`）。
- MI form-below-table 仅在 `nestedRows.length === 1` 时 hydrate；多行时不按 `sub_task_id` 选当前参与者行。
- `mergeLinkFormTargetBindingsInto` 合并 Link 目标时省略 `fieldDefinitions`，却期望 Save 时 `ensureAutoPrimaryKeysForRows` 分配子表 PK。
- form-below-table Save 不经 `update:row` flush 直接 persist（内联编辑丢失 / PK 未分配）。
- Portal 有 form-below-table Save 而 DW Form Preview / SubTableListView 无同款按钮（parity 分叉）。
