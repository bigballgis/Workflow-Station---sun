---
description: My Request / MI 子表数据规则 — __subTables__ 规范 key 单一真相源、MI 字段名读 Sub-Task Config、发起人全量 vs 办理人切片、file-only 附件禁止 global MI merge
globs: frontend/user-portal/src/{views/applications,views/tasks,composables/tasks,composables/taskDetail,composables/applicationDetail,composables/subTableField}/**/*
alwaysApply: false
---

# Portal — My Request × MI 子表数据规则

与 `portal-design-parity.mdc` **数据 parity** 配套：同一 `__subTables__` 在不同 **viewContext** 下 MUST 按下列语义 hydrate，不得混用 To Do 的 MI 切片逻辑。

## `__subTables__` 规范 key —— 一张表一份数据（单一真相源）

**Key 规则**：`dw:<table_name>` / `rt:<relation_table_name>`（小写归一）。唯一入口
`composables/tasks/subTableStore.ts`：写 `writeSubTableRows()`、读 `readSubTableRows()` / `subTableStoreKey()`。

| MUST | MUST NOT |
|---|---|
| 每张设计器表**只写一个** key | 按 `bindingId` / 表名 / 展示名**扇出多份**（历史上同一批行存了 9 份，副本会分叉） |
| 用 `tableName` / `physicalTableName`（设计器名） | 用 `tableDisplayName`（`Participants` 这类展示名跨 FU 可重复） |
| 用**表名**做 key | 用**表 id**（FU clone / import / 部署到新环境会重新映射 id） |
| RT binding 落 `rt:` 命名空间 | 让 RT binding 掉进 `dw:`（构造 binding 对象时**必须透传** `relationTableId` / `relationTableName`，否则 `rt:` 切片永远解析不到） |
| 命中即返回，`Array.isArray` 判定 | 用 `length > 0` 判命中（刚被清空的表会回退到陈旧副本，表现为「删了又回来」） |

**为什么 binding 不能做数据身份**：binding 只是「表单 ↔ 表」的连接，同一张表被多个表单绑定
（实测 `subtable` 有 6 个 binding）。按 binding 存就会各存一份、各自更新、互相覆盖。

**新增读写点的自查**：改任何 hydrate / resync / merge / submit payload 前，先问
「这里是**读写 `__subTables__` 行**，还是读**表单配置**（`subForms[bindingId]` 等）？」
前者一律走 `subTableStore.ts`；后者按 binding id 索引是**正确**的，不要误改。

**回退与观测**：过渡期存量实例仍是旧 binding id key，读取侧可在规范 key 未命中时回退，
但**禁止按表名兜底**（共享展示名 key 无从判断行属于哪个 binding）。dev 下回退且取到数据会
`console.warn('canonical key missed')` —— 出现即说明**有写入路径没收敛**，须定位而非忽略。

> **踩过的坑（#1441 后续）**：规范 key 改造只切了「任务保存链路」，My Request 的读取入口
> `composables/applicationDetail/subTableRowHelpers.ts` 仍只按 binding id 取值，导致新实例
> 在 My Request 上 MI 子表**整块不渲染**。改 key 规则**必须同时扫读侧**：
> `grep -rn "savedSubTables\[" src/` 找出所有直接下标访问。

## MI 字段名一律读 Sub-Task Config —— 禁止硬编码

MI 的字段名由每个 FU 在 developer-workstation 的 **Sub-Task Config**（BPMN extension properties）配置：
`subTableId` / `subTableName` / `assigneeField` / `assigneeMode` / `roleField` / `buField` /
`miTaskStatusField` / `miTaskCurrentNodeField` / `rowIdVariable`。

| MUST | MUST NOT |
|---|---|
| 行主键取 binding 的 `primaryKeyFields`（来自 `dw_field_definitions`） | 写死 `id_idw` / `row_id`（实测 ATM_Transaction 的 PK 就是 `row_id`，`subtable` 是 `id_idwvvbz`） |
| 状态列取 `miTaskStatusField` | 写死 `task_status` |
| 当前节点列取 `miTaskCurrentNodeField` | 写死 `task_current_node` |
| 表判定取 scope 的 `subTableName` | 写死 `participants` / `subtable` |
| 分派字段取 `assigneeField` / `roleField` | 写死 `assignee` / `role_code` |

写死会让「PK 不叫 `id_idw` / 状态列不叫 `task_status` / 表不叫 `participants`」的 FU **静默失效**，
且**只在那个 FU 上复现**——这是 MI 反复出问题的结构性根因。

**配置缺失时 MUST 报错，不得猜列名。** 判据是「这个问题**只有配置**能回答，还是**平台自己**定义了答案」：

| 情形 | 处理 | 入口 |
|---|---|---|
| 行主键 / 子表名 / 分派字段 —— **只有配置能回答** | **抛 `MI_CONFIG_MISSING`** | `requireSubTablePrimaryKeyFields()` / `requireMiSubTableName()` / `requireMiAssigneeField()` |
| `task_status` / `task_current_node` —— **平台契约**（后端 `MiOverlaySupport` 就是用这两个字面量写数据；实测 19 个已部署 BPMN 无一配置过） | 用平台默认 | `getActiveMiFieldNames()` |
| 本就没有主键的合法 binding（共享附件 `main_id`、AP 服务任务行、列名启发式误判的 FU 副本） | 返回 `null`，调用方**跳过该判定** | `resolveMiCollectionPrimaryKeyFields()` |

> 与引擎侧一致：`MultiInstanceDataResolver` 对 `subTableName` 缺失
> `throw new WorkflowValidationException(...)`，对状态列缺失用默认名。
>
> **别把显示问题升级成崩溃**：在「合法地没有主键」的 binding 上抛错会让共享附件、AP 写回行
> 整页崩掉（实测会红 9 个测试）。那里要跳过判定，不是抛错、更不是拿 `id_idw` 顶上。
>
> **踩过的坑（真实事故）**：行匹配函数会**逐个遍历 peer binding**，而同一个表单上除了子表，
> 还挂着 **relation table**（实测 FU fu-20260422 的 binding 50550=`test`、50551=`sys_users`
> 虚拟只读表），它们不是设计器子表、`primaryKeyFields` 恒为 null。在那里抛
> `MI_CONFIG_MISSING` 会让用户**点 Save 直接中断** —— 而用户的子任务其实**配了主键**
> （`id_idwnn`），报错跟他的配置毫无关系。加抛错前先问：**这个函数会不会被非子表 binding 调到？**
>
> 降级方向按「猜错的后果」定，不是一律返回空：
> - `findMiIsolatedParentRow` 单行分支 → **拒绝该行**（放行=把别人的行交给你编辑）
> - `repairMisassignedPrimaryKeyFromParentId` → **什么都不删**（它是删值的）
> - `rowMatchesMiExpansionId` → 跳过主键匹配，继续跨字段兜底
>
> 回归测试：`saveWithRelationTableBindings.test.ts`（用现场 binding 集合，去掉修复红 3 个）。

**link-child 表不是「一行一参与者」**：People 式子表用**结构 FK**（`sub_task_id` / `participant_id` …）
指向参与者，自己的 PK 是行 UUID，**不携带 collection 的主键**。所以：

- 按 collection 的 `pkCols` 匹配它永远匹配不到 → 会被「own row missing」守卫拒掉整次保存；
- 但也**不能**塞进「单行替换」分支 —— 一个参与者可能有**多行**，那样只留第一行、其余静默丢失
  （比拒绝保存更糟）。正确做法：**整组替换该参与者的行**，其他参与者的行原样保留。

> 切片里**全是别人的行**（每行的结构 FK 都指向其他参与者）**不是**损坏 —— 只是这张表上
> 「我」还没有行，而同事的行搭在共享切片里。此时保持基线、放行保存；只有「行既没有结构 FK、
> 主键也对不上」才是真的语义不一致，才 fail loud。
>
> 空切片仍**不删除**基线（歧义：可能是"用户删光了"，也可能是"这个 binding 没渲染"），
> 这是既有的有意取舍；删除需由非空切片或专用删除接口承载。
>
> **`_currentItem` 是 execution 级的**：自己 execution 上没有副本的 task 会继承**流程实例级**
> 的那份（属于最后写入的参与者）。task 详情与 form-data **两条链都要**套用
> `EngineTaskMapper.applyTaskScopedMiCurrentItem` —— 实测只修了 form-data 时，task 详情仍返回
> 别人的参与者 id，表单据此提交，后端 MI 守卫拒绝每一次 Save。
> 测试：`mergeCurrentRowOnly_participantOwningSeveralLinkChildRowsKeepsAllOfThem`、
> `mergeCurrentRowOnly_emptyLinkChildSliceKeepsBaselineRatherThanDeleting`。

**后端同源规则**（`MiOverlaySupport` / `SubTableEnrichmentComponent`）：MI 镜像列一律写
**解析出的**列名（配置优先、`PORTAL_MI_STATUS_COLUMN` 等常量兜底），**不得**在写配置列的同时
再写一遍字面量 —— 那会让配了自定义列名的 FU 每行带两套状态列、且两者取值不一致。
配置列名会拼进 SQL 时**必须**先过 `SqlIdentifiers.requireIdentifier`（它来自 BPMN，不是可信字面量）。

**统一入口**：`composables/tasks/useMiConfig.ts`。详情页解析完 BPMN 后 `setActiveMiConfig()`
注册一次（`useTaskDetailMiScope` / `useApplicationDetailMiScope` 各一处），深层纯函数用
`getActiveMiFieldNames()` / `isMiStatusField()` 隐式取到 —— 不必给 113 个调用点逐个加参数
（漏一处就是一个静默失效的 FU）。优先级：**显式传参 > 活动配置 > 平台默认字面量**。

> **踩过的坑**：`resolveMiDashboardFieldNames(miFields)` 早就支持传配置，但实测**没有任何
> 调用点传过** —— 参数形同虚设、100% 落在 `task_status` 默认值上。**加了参数不等于收敛**，
> 必须确认调用点真的传了值。

完整分类清单（已修 / 保留及其依据）见 [docs/mi-hardcoded-field-names-inventory.md](../../docs/mi-hardcoded-field-names-inventory.md)。
新代码**不得**再增加此类写死。

## 视图与受众（谁先决定能不能 filter）

| 场景 | 识别 | 子表数据范围 |
|---|---|---|
| **发起人 My Request** | `viewerId === startUserId` 且非 Completed Tasks snapshot（`!snapshotTaskName`）→ `isInitiatorMyRequestView` | **全案快照**：MI collection（如 HMDC_Transaction）MUST 显示 **全部** 行，**禁止** `filterRunningMiBindingsByProcessDesignScope` |
| **办理人待办（MI 内）** | `tasks/detail.vue`，当前用户为 assignee | **参与者切片**：BPMN `subTableName` + `assigneeField` + 设计师 PK → `filterBindingsToMiParticipantRow` |
| **Completed Tasks snapshot** | `snapshotTaskName` 等 query | 按 assignee + 已完成状态过滤（独立路径，≠ 上两项） |

```typescript
// ✅ 发起人 My Request — 入口 guard
function filterRunningMiBindingsByProcessDesignScope(bindings) {
  if (isInitiatorMyRequestView.value) return  // 全案，不按 assignee 裁 transaction
  // ...
}
```

## 子表 binding 分类 —— **一律读配置，禁止靠列名/表名/FK 名猜**

> 这一节是 MI 反复出 bug 的**结构性根因所在**。归错类的后果不是显示问题，是**跨参与者数据串写**。
> 唯一判据见 `composables/tasks/miBindingKindFromConfig.ts`（`resolveMiBindingKindFromConfig`）。

### 三类的权威判据

| 类 | 含义 | **判据（全部来自设计器配置）** |
|---|---|---|
| **collection** | MI 子任务集合，一行 = 一个参与者 = 一个子任务 | `bindingLinkMode === 'miParticipantRow'`（DW → Manage Table Bindings → Link Mode = *MI Participant Row*），或 `tableId` 等于该 binding 的 `tableId` |
| **participant-child** | 挂在某参与者名下的明细（People 式） | 字段级 FK（`fieldDefinitions[].isForeignKey`）的 **`refTableId` 指向 collection 的 tableId** |
| **shared** | 流程级共享（附件式），所有子任务看同一份 | 字段级 FK 的 **`refTableId` 指向主表**（PRIMARY binding 的 tableId） |

### 必须记住的三条（每条都踩过坑）

1. **`bindingLinkMode` 区分不了 child 和 shared** —— 两者都是 `structuralFk`。现场实测：

   | 表 | linkMode | FK 字段 | FK 指向 | 类 |
   |---|---|---|---|---|
   | `attachment` | structuralFk | `main_idv` | → `main`（主表） | **shared** |
   | `people` | structuralFk | `sub_task_idq` | → `subtable`（collection） | **participant-child** |

   **区分二者的唯一信息是 `refTableId` 指向哪张表。**

2. **`shared` 必须是正面判据，不能写成 else 兜底。** FK 元数据缺失（只有 `isForeignKey`、没有 `refTableId`）时
   MUST 返回 `null`（"判不了"），由调用方退回旧启发式。静默判成 `shared` 会让真 child 失去参与者隔离。

3. **`foreignKeyField`（binding 层）不是父引用。** 在 `miParticipantRow` binding 上它指的是 **collection 自己的主键**；
   在 People 式 child 上它常等于该表自己的 PK。父引用只在**字段级** `fieldDefinitions` 里。

### 禁止 —— 以下启发式**已全部物理删除**，不得以任何形式重新引入

| 已删除的猜法 | 原位置 |
|---|---|
| 列名撞表 `task_status` / `task_current_node` / `sub_task_status` | `MI_DASHBOARD_STATUS_FIELDS` |
| 表名撞表 `tableName === 'participants'` / `endsWith('participants')` | `isMiDashboardSubTableBinding`、`isParticipantsBinding`、`resolveAssigneeFieldForBinding` |
| FK 列名撞表 `fk === 'id'` → participant-scoped | `isMiParticipantScopedSubTableBinding`（**真 collection 被当 link-child，Inline Form 空白 + 保存追加孤儿行**） |
| FK 列名清单 `main_id` / `id_idw` / `row_id` / … | `SHARED_PROCESS_SUB_TABLE_FK`、`MI_PARTICIPANT_SUB_TABLE_FK` |
| 结构 FK 列名清单 `sub_task_id` / `participant_id` / `parent_id` | `MI_STRUCTURAL_PARENT_FK_FIELDS` |

**没有兜底了**：判不出来就返回 `null` / `false`，不猜。
`grep -rn "fk === 'id'\|MI_PARTICIPANT_SUB_TABLE_FK\|endsWith('participants')" src/` 必须为空。

### 上下文怎么来（删掉兜底的前提）

分类谓词有 **39 个调用点**，多数是深层纯函数，不可能逐个传参。上下文走**模块级注册表**，
与 `setActiveMiConfig` 同一套路：

- 详情页在 binding 构建完成后调 `registerMiKindTableIdsFromBindings(bindings, primaryTableId)`
  —— **To Do（`useTaskDetailFuLoader`）与 My Request（`useApplicationDetailMiScope`）两条链路都必须调**，漏一条那条链路就判不出 child
- 深层函数通过 `resolveMiKindContext()` 隐式读取；显式传入的 `MiKindContext` 永远优先
- 离开详情页 `clearActiveMiConfig()` 会一并清空，避免跨 FU 泄漏

**新增 binding 构建点时 MUST 透传 `bindingLinkMode` 与 `fieldDefinitions`** —— 漏传等于把配置藏起来，
分类会静默降级（实测 `assignmentTaskLayoutSync` 就因为构建时漏了 `bindingLinkMode` 而判错）。

### 各类的 resync / merge 规则

| 类型 | 识别 | resync / merge 规则 |
|---|---|---|
| **MI collection** | `bindingLinkMode === 'miParticipantRow'`（回退：`isMiDashboardSubTableBinding`） | **办理人** 可 `mergeAllSubTableSlicesFromVariables`；**发起人 My Request MUST 仅用本 binding slice**（禁止 allSlices 注入 People 占位行 → Sub Task 4 条） |
| **Shared attachment** | `isSharedAttachmentFileBinding`（`main_id` FK 等） | 仅 `applySharedAttachmentFinalizeAndMaterialize`；勿走 per-binding numeric slice 拆分 |
| **File-only 子表** | `isFileOnlySubTableBinding`（列 **仅** `file`，如 HMDC_Attachment；FK 可为 `row_id`） | **禁止** `mergeAllSubTableSlicesFromVariables` — 会把 transaction 行注入附件表 → **空 file 行**；只用 **本 binding slice** + `filterRowsForSharedProcessSubTableBinding` |
| **MI participant-scoped 子表** | `isMiParticipantScopedSubTableBinding(binding, ctx)` —— **务必传 ctx** | 仅 own slice + `filterRowsForMiParticipantSubTableBinding` |

### 行归属（"这一行是谁的"）同样读配置

FK **列名**由 `miChildFkConfigOfBinding(binding, miCollectionTableId)` 从 `fieldDefinitions` 解析，
再用 `refTableId` 限定为"指向 collection"的那一个。**不存在列名清单兜底**（`MI_STRUCTURAL_PARENT_FK_FIELDS` 已删除）。

```typescript
// ✅ resyncMiDashboardFieldsFromVariablesOnBindings — file-only 分支（applications/detail.vue）
if (isFileOnlySubTableBinding(b)) {
  // 只用 bindingSaved，禁止 allSlicesMerged
  b.data = dropSubsumedSubTableRows(
    filterRowsForSharedProcessSubTableBinding(merged, b),
  )
  continue
}
```

## Shared Attachment — 全参与者共享，禁止 foreign-id 误删（#1438）

**语义：** `isSharedAttachmentFileBinding`（`main_id` FK、tableId 74、`attachment` 表名）的行 **全案共享**，不属于任一 MI 参与者切片；sub form1 新增一条附件后，**原有 N 条 + 新增 1 条 MUST 全部可见**。

**典型症状（task 093962c4 / Process_1_KK）：** 流程变量 `__subTables__` / `Attachment` 切片仍有 3 行 UUID+file，UI 只显示刚新增的第 3 条；Sub Task 网格误混入纯 `id+file` 行。

**根因链（禁止再犯）：**

1. Attachment UUID 行被 merge 泄漏进 Sub Task / MI 切片 → `collectForeignSubTableRowIdsFromVariables` 把 **合法附件 PK** 登记为 `foreignSubTableRowIds`。
2. `isLeakedForeignRowOnSharedAttachment` 在 **`pickNonEmptyAttachmentFile` 之前** 按 foreign id 过滤 → 误删已有附件行；尚未进入 Sub Task 切片的新 UUID 反而能显示。
3. `filterRowsForMiParticipantSubTableBinding` 在列表含 `file` 列时 `colFields.has('file') return true` 早退 → Sub Task 保留纯附件泄漏行。
4. `patchFormDataSubTablesFromCurrentBindings` 若只写当前 `binding.data` 而不 merge `miFullSubTablesSnapshotRef`，Save 可能持久化截断切片。

**正确行为（MUST）：**

```typescript
// ✅ isLeakedForeignRowOnSharedAttachment — 有 file 的行 NEVER 当 foreign leak
if (pickNonEmptyAttachmentFile(rec)) {
  // 仅 name/id_idw 等 MI 字段泄漏才 return true
  return false  // 默认：合法附件行保留
}

// ✅ collectForeignSubTableRowIdsFromRowWalk — 纯 id+file 行（无 sub_task_id/id_idw/name/MI 列）不得进入 foreign 集合
if (!isPureAttachmentLeakRow(rec)) {  // 实现：pickNonEmptyAttachmentFile && !MI dashboard && !participant FK
  addRowIdToForeignSet(row, ids)
}

// ✅ filterRowsForMiParticipantSubTableBinding — 含 file 列仍剔除 id+file-only 泄漏
if (colFields.has('file') && pickNonEmptyAttachmentFile(row)) {
  if (isSubTableMiDashboardRow(rec)) return true
  if (rec.sub_task_id?.trim()) return true
  if (rec.id_idw?.trim()) return true
  if (rec.name?.trim()) return true
  return false  // 纯附件行不得留在 Sub Task / People 网格
}

// ✅ patchFormDataSubTablesFromCurrentBindings — shared attachment merge 全量快照
if (fullSnap && isSharedAttachmentFileBinding(binding)) {
  rows = mergeSubTableRowsByRowId(snapRows, rows, binding.primaryKeyFields)
}
```

**验证：** task `093962c4` Attachment 表 3 行；截图 `verification-screenshots/2026-06-08_task-093962-subform1-attachment-table.png`；单测 `subTableRowMetaFields.test.ts`（foreign id + finalize 3 行 + file 列泄漏剔除）。

## People link-child UUID PK — scrub 不得覆写（#1435）

**语义：** People 等 link-child 行 Save 后 `id` 为 **allocate 的 UUID**；`sub_task_id` 才是参与者 FK。`id_idw` 若误复制父参与者 expansion key，**不得**触发 scrub 把 UUID 改成 `Test-000058`。

**正确行为：**

```typescript
// ✅ scrubMiCorruptLinkChildRowsForParent — id 已是 UUID 时保留 id，仅删 erroneous id_idw
if (isAllocatedUuidPrimaryKey(rec.id)) {
  const cleaned = { ...rec }
  delete cleaned.id_idw
  out.push(cleaned)
} else {
  out.push(repairMiCorruptLinkChildRowId(rec, key))
}
```

**验证：** task `09367c90` People 内联 `id` 显示 UUID；单测 `linkFormMiIsolation.test.ts`（UUID + id_idw 镜像父参与者）。

## MI 参与者过滤 — 子表附件

`filterBindingsToMiParticipantRow` 裁剪 **办理人** 视图时：

- Collection 行：按设计师 PK 留一条。
- 子表 FK **等于** collection PK（如 `row_id`）且行 **仅有 file**：MUST 保留（case 级附件，无 `row_id` 字段）— 见 `rowHasAttachmentFile` / `miSubProcessScope.ts`。

## People link-child — sub form1 → sub form2 字段透传（#1439）

**语义：** 同一 MI 参与者在 **sub form1** 填写的 People 业务字段（age/sex/name…）MUST 在 **sub form2** 内联表单可见；数据在 `variables.__subTables__` 中跨 bindingId（30/63）与 nested 切片均可存在。

**典型症状：** sub form2 只显示 `id`/`sub_task_id`，age/sex 空白；变量里 binding 63 行有 age 但 `sub_task_id` 指向错误参与者。

**根因链（禁止再犯）：**

1. legacy People 行 `id`/`id_idw` 已是当前参与者，但 `sub_task_id` 仍为其它参与者 → `miLinkChildRowBelongsToParticipant` 过滤丢弃 sub form1  payload。
2. 同参与者 UUID 行（binding 30）与 legacy 行（binding 63）PK 不同 → `collapseMiLinkChildRowsToOnePerParticipant` 只 pick-best 不 merge 字段。
3. `hydrateCurrentSubTablesFromPreviousForms` 在 `current.data.length > 0` 且仅有 id/FK 时跳过 merge。

**修复要点：** `repairMisassignedLinkChildStructuralFk`；collapse 按 `sub_task_id` 做 `mergeSubTableRowsByRowId`；前序表单/ nested 薄行 merge；`SUB_TABLE_MI_PLACEHOLDER_KEYS` 排除 id/FK 再判「缺业务字段」。`miLinkChildRowBelongsToParticipant`：structural FK 为主，但 stale `sub_task_id` 时若 `id_idw`+UUID `id` 锚定当前参与者仍归属（见单测 stale sub_task_id case）。

## People nested __subTables__ — 禁止全 slice 写入单参与者 (#1440)

**语义：** `syncMiLinkChildRowsIntoParentNested` 写入 `parentRow.__subTables__[childBindingId]` 的 MUST 仅为 **该 parent 对齐** 的 link-child 行，**不得**写入整份 `binding.data` 全参与者切片。

**典型症状（task 6c6c5cc6）：** 参与者 B 的 People 内联 age 空，**仅切换 sex** 后 age/id 突然出现（与参与者 A 相同）。

**根因链（禁止再犯）：**

1. `patchMiParentRowsWithNestedChildSlice` 把完整 `childSlice`（全参与者）塞进当前 parent 的 nested。
2. `FormRenderer.mergeRowsForInlineFormTarget` pull nested 后未 `pickMiLinkChildRowsForParent` → inline merge/collapse 混入他人行。
3. `getCurrentRowForInlineForm` PK backfill 在 `pack.rows` 上跨参与者 spread 字段。
4. `syncMainSubTableRows` 曾把 `binding.data = nextRows`（仅 inline 1 行）而非 merge 后 `out`，与 `__subTables__` 不一致。

**正确行为（MUST）：**

```typescript
// ✅ patch — nested 只存本 parent 子行
const scopedSlice = scopeMiLinkChildRowsForParentRow(parentRec, childSlice)

// ✅ syncMainSubTableRows — binding.data 与 formData 同步为 merge 后 out
binding.data = out

// ✅ FormRenderer MI isolate — nested 先 pickMiLinkChildRowsForParent
let rows = pickMiLinkChildRowsForParent(parentRow, nestedOnly, pk)

// ✅ PK backfill — 只 merge scopedRows（同参与者）
const scopedRows = pack.rows.filter(r => miParentRowAlignsWithChildRow(parentRow, r))
```

**验证：** `linkFormMiIsolation.test.ts`（scopeMiLinkChildRowsForParentRow）；`node scripts/verify-sex-toggle-isolation.mjs <taskId> <participantId>`；截图 `2026-06-09_task-6c6c-sex-before/after.png`。

## My Request Details 弹窗 — Link Form 全链路（#1441）

**语义：** 发起人 My Request 点击 Sub Task 上 **Details / Link Form** 时，弹窗 MUST 按参与者正确显示 People 字段；**未处理**（IN_PROGRESS、无 UUID）与 **已填写**（COMPLETED / 有 age）行为不同。

### 字段映射（禁止混用）

| 弹窗字段 | 含义 | 未处理（如 Test-000060 IN_PROGRESS） | 已填写（如 Test-000061） |
|---|---|---|---|
| **sub task id** | 父 Sub Task 参与者 FK | 父行 `id_idw`（`Test-000060`） | `Test-000061` |
| **id** | People 行 **allocate 的 UUID PK** | **空**（用户尚未 Save） | UUID（如 `bbb5379a-...`） |
| **age / sex …** | 业务字段 | 空（或仅占位） | 变量中的值 |

**禁止：** 把父行 `id_idw`（`Test-xxxx`）写入 link-child 的 **`id`** 字段 — 那是 **sub_task_id** 语义。

```typescript
// ✅ backfillMiLinkFormModalFieldsFromParent（SubTableField.vue）
// sub_task_id ← parent.id_idw；id 仅 isAllocatedLinkChildBusinessId(UUID) 时回填
function isAllocatedLinkChildBusinessId(childId, parentKey) {
  if (normalizeFkIdForMatch(childId) === parentKey) return false
  if (/^test-\d+$/i.test(normalizeFkIdForMatch(childId) ?? '')) return false
  return true
}
```

### 典型症状与根因链（禁止再犯）

| 症状 | 根因 |
|---|---|
| 弹窗 id/age 全空 | `filterLinkedChildRowsByParentIdIdw` + `filterRowsByMiLinkFormParent` 返回 `[]` 仍赋值 → `effectiveSavedRows` 被清空；`linkFormRowsLackFormPayload([])` 误 return false |
| Sub Task **4 条**（应为 3） | 发起人 `resyncMiDashboardFieldsFromVariablesOnBindings` 对 MI collection 使用 `mergeAllSubTableSlicesFromVariables`，People 占位行混入 Sub Task |
| 已填写行 **id 空**、age 有值 | `id` 为 UUID 但表单字段 type=number → `resolveLinkFormFieldValueForModal` 丢弃；只读弹窗须 `opts.readonly` 保留 UUID 字符串（`FieldRenderer.showNumberAsText`） |
| Test-000060 **id=Test-000060** | id 回填误用父 `id_idw` 作 donor |
| 变量在 key 30/63、binding 69 无 age | link-child 须 `hydrateMiLinkChildBindingsForInitiatorMyRequest` + `collectLinkFormRowsFromProcessVariables`；`resolveLinkedFallbackRows` 在 thin slice 时 union peer |

### 正确行为（MUST）

```typescript
// ✅ applications/detail.vue — 发起人 MI collection：仅本 binding slice
if (isInitiatorMyRequestView.value && isMiDashboardSubTableBinding(b)) {
  b.data = dropSubsumedSubTableRows(
    mergeSubTableRowsByRowId(b.data ?? [], bindingSaved ?? [], pk),
  )
  continue  // 禁止 mergeAllSubTableSlicesFromVariables
}

// ✅ applications/detail.vue — 发起人 link-child：hydrateMiLinkChildBindingsForInitiatorMyRequest
// 跳过 isMiDashboardSubTableBinding；union 重叠 slice 后 collapseMiLinkChildRowsToOnePerParticipant

// ✅ SubTableField handleLinkFormClick
// - miLinkFormChildRowMatchesParent：sub_task_id ↔ parent id_idw
// - filterLinkedChildRowsByMiTaskStatus：status 占位行无业务字段时不独占
// - id_idw 收窄：mi 过滤为空时勿清空 effectiveSavedRows
// - collectLinkFormRowsFromProcessVariables(primaryFormData.__subTables__)
// - backfillMiLinkFormModalFieldsFromParent
```

**验证：**

```bash
cd frontend && node scripts/verify-myrequest-details-modal.mjs [applicationId]
```

- Test-000060：`id` 空，`sub task id` = `Test-000060` → `…-details-060-unprocessed.png`
- Test-000061：`id` UUID，`age` = `666` → `…-details-061-filled.png`
- Sub Task 行数 = 3（`Test-000060/061/062`）

## 变更检查清单

- [ ] **规范 key**：新增/改动的 `__subTables__` 读写点是否都走 `subTableStore.ts`？有无按 `bindingId` / 表名扇出第二份？
- [ ] **规范 key 读侧**：`grep -rn "savedSubTables\[" src/` 是否已全部收敛？**To Do 与 My Request 两条读链是否都改了**？
- [ ] **规范 key RT**：构造 binding 对象时是否透传 `relationTableId` / `relationTableName`（否则 `rt:` 切片解析不到）？
- [ ] **无硬编码**：PK / 状态列 / 当前节点列 / 表名 / 分派字段是否都取自 binding `primaryKeyFields` 与 Sub-Task Config，而非 `id_idw` / `task_status` / `participants` 等字面量？
- [ ] **binding 分类**：是否用 `resolveMiBindingKindFromConfig`？有没有新增列名/表名/FK 名判断？（`grep -rn "fk === 'id'\|endsWith('participants')" src/` 必须为空）
- [ ] **上下文注册**：新的详情/加载链路是否调了 `registerMiKindTableIdsFromBindings`？新的 binding 构建点是否透传了 `bindingLinkMode` 与 `fieldDefinitions`？
- [ ] **child vs shared**：是否靠**字段级 FK 的 `refTableId` 指向 collection 还是主表**区分？（`bindingLinkMode` 对二者都是 `structuralFk`，区分不了）
- [ ] **判不了要返回 null**：配置缺失时是否**没有**静默降级成 `shared`（那会让真 child 丢失参与者隔离）？
- [ ] 发起人 My Request 是否仍 **跳过** MI 参与者 filter？
- [ ] File-only 子表 resync 是否 **未** 使用 `mergeAllSubTableSlicesFromVariables`？
- [ ] 同 `tableId` 的 **transaction** binding union merge 是否未误伤 **attachment** binding？
- [ ] **Shared attachment**：`pickNonEmptyAttachmentFile` 行是否 **未** 被 `foreignSubTableRowIds` 误删？纯 `id+file` 是否 **未** 泄漏进 Sub Task 网格？
- [ ] **Shared attachment** Save：`patchFormDataSubTablesFromCurrentBindings` 是否从 `miFullSubTablesSnapshotRef` merge 全量行？
- [ ] **People sub form1→2**：错误 `sub_task_id` 是否已 repair？同参与者多行是否 merge 而非 pick-one？
- [ ] **People nested / inline**：`patchMiParentRowsWithNestedChildSlice` 是否 **scope** 子行？`syncMainSubTableRows` 是否用 merge 后 `out` 更新 binding.data？inline merge 是否 **pickMiLinkChildRowsForParent**？
- [ ] **My Request Details（#1441）**：发起人 Sub Task 是否 **未** allSlices merge（仅 3 条参与者）？未处理行 `id` 空、`sub_task_id`=父 `id_idw`？已填写行 `id`=UUID？`verify-myrequest-details-modal.mjs` + 截图？
- [ ] `tasks/detail.vue` 与 `applications/detail.vue` 是否仍行为对齐（除「发起人全案 vs 办理人切片」差异）？
- [ ] 回归：`mcyInitiatorMyRequest.test.ts`；MCY 手测：2 条 HMDC Transaction + 1 条 HMDC Attachment（有 PDF）。
- [ ] 回归：`pnpm run regression:mi`（单元 + 截图）；或分项见 `user-portal/MI_REGRESSION.md`

## 参考

- 实现：`applications/detail.vue`（`isInitiatorMyRequestView`、`resyncMiDashboardFieldsFromVariablesOnBindings`、`hydrateMiLinkChildBindingsForInitiatorMyRequest`）；`SubTableField.vue`（`handleLinkFormClick`、`backfillMiLinkFormModalFieldsFromParent`、`collectLinkFormRowsFromProcessVariables`）
- 类型/helpers：`shared.ts`（`isFileOnlySubTableBinding`、`isSharedAttachmentFileBinding`、`isMiDashboardSubTableBinding`、`collectForeignSubTableRowIdsFromVariables`、`isLeakedForeignRowOnSharedAttachment`、`filterRowsForMiParticipantSubTableBinding`、`scrubMiCorruptLinkChildRowsForParent`、`scopeMiLinkChildRowsForParentRow`、`collapseMiLinkChildRowsToOnePerParticipant`；纯 `id+file` 判定见 `collectForeignSubTableRowIdsFromRowWalk` 内联逻辑）
- MI 范围：`miSubProcessScope.ts`（`filterBindingsToMiParticipantRow`）
- 验证：`frontend/scripts/mi-regression-scenarios.mjs`；`pnpm run regression:mi`；截图 `frontend/user-portal/verification-screenshots/`
- Issue：**#1414**（MCY Debit Card Transaction Investigation）、**#1435**（People hydrate/id）、**#1438**（Attachment 新增后旧行消失）、**#1439**（sub form2 People 未继承 sub form1 字段）、**#1440**（People inline 开 sex 串上一参与者数据）、**#1441**（My Request Details 弹窗 / Sub Task 行数 / id vs sub_task_id 映射）
