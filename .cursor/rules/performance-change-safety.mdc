---
description: 性能变更安全 — 取数/语义分层、MI 回归门禁、禁止与功能混 PR
globs: frontend/user-portal/src/{views/applications,views/tasks,composables/tasks,components,utils}/**/*
alwaysApply: false
---

# 性能变更安全 — 防止优化误伤多实例子任务

与 `performance-guardrails.mdc`（指标与模式）、`portal-mi-subtable-my-request.mdc`（MI 语义）配套。**规则不能替代自动化门禁**；动热路径 MUST 跑回归命令。

## 两层管道（性能优化默认只动第一层）

```
取数层（可优化）          语义层（禁止为性能改行为）
─────────────────        ─────────────────────────────
API / prefetch / cache   viewContext 分支
parallel Promise.all     binding 类型分类（MI collection / file-only / attachment / participant-scoped）
module-level FU cache    merge / filter / scope / backfill / Save patch
flatten __subTables__    pickMiLinkChildRowsForParent / collapse / scrub UUID
```

**语义层 invariant（改性能前 MUST 保持不变）：**

| # | Invariant | 违反典型症状 | 规则 / Issue |
|---|---|---|---|
| I1 | 发起人 My Request + MI collection → **禁止** `mergeAllSubTableSlicesFromVariables` | Sub Task 4 条变 3 条外多 People 占位 | `portal-mi-subtable-my-request.mdc` §#1441 |
| I2 | `isFileOnlySubTableBinding` → **禁止** global MI merge | 附件表空 file 行 | 同上 |
| I3 | `isSharedAttachmentFileBinding` Save → merge **全量** snapshot | 新增附件后旧行消失 | #1438 |
| I4 | nested `__subTables__` → **scope** 本 parent 子行 | 开 sex 串上一参与者 age | #1440 |
| I5 | Link Form Details → `id` vs `sub_task_id` 映射 | id=Test-xxxx 或 UUID 空 | #1441 / `form-preview-fk-pk-runtime.mdc` §6.5 |
| I6 | form-below-table 多行 → 按 `sub_task_id` / `currentMiRowId` 选行 | 内联表单空白 | #1435 / R12 |
| I7 | 缓存 key 含 **viewContext + binding 类型**，不得仅 processKey | 发起人/办理人数据串 | 本节 §缓存 |

## 热路径（触达任一文件 → 必跑 MI 回归）

```
frontend/user-portal/src/views/applications/detail.vue
frontend/user-portal/src/views/tasks/detail.vue
frontend/user-portal/src/composables/tasks/shared.ts
frontend/user-portal/src/composables/tasks/miSubProcessScope.ts
frontend/user-portal/src/components/SubTableField.vue
frontend/user-portal/src/components/FormRenderer.vue
frontend/user-portal/src/components/SubTableInlineForm.vue
frontend/user-portal/src/utils/subTableRowRuntime.ts
frontend/user-portal/src/utils/inlineFormBelowTableRuntime.ts
```

后端若改 `ProcessComponent` / task variables 组装，同样跑前端 MI 回归 + 相关单测。

## 必跑命令（门禁）

**完整 MI 回归 = 单元测试 + Playwright 截图（缺一不可）。** 仅跑单元 `--unit-only` 为 portal 未启动时的临时手段，**不算**通过完整回归。

### 1. 完整回归（默认）

```bash
cd frontend && npm run regression:mi
```

- Phase 1：`test:regression:mi`（9 文件 / 99 tests）
- Phase 2：`verify-mi-regression-all.mjs`（6 场景，每个 MUST 写 PNG 至 `verification-screenshots/`）

### 2. 分项命令

```bash
cd frontend/user-portal && npm run test:regression:mi   # 仅单元
cd frontend && npm run regression:mi:screenshots        # 仅截图 E2E
cd frontend && npm run regression:mi:unit-only          # 临时跳过截图（WARN）
```

### 3. 场景 ↔ 单测 ↔ 截图

见 `frontend/user-portal/MI_REGRESSION.md` 与 `frontend/scripts/mi-regression-scenarios.mjs`。

| 脚本 | Issue | 截图 |
|------|-------|------|
| `verify-myrequest-details-modal.mjs` | #1441 | 060 未处理 / 061 已填 |
| `verify-sex-toggle-isolation.mjs` | #1440 | sex before/after |
| `verify-mi-attachment-rows.mjs` | #1438 | attachment + subtask grid |
| `verify-subform2-people-carry-forward.mjs` | #1439 | sub form2 People |
| `verify-mi-people-inline-uuid.mjs` | #1435 | inline UUID id |
| `verify-mi-assignee-subtask-slice.mjs` | miSubProcessScope | assignee Sub Task |

## PR / 变更协议

### 识别为「性能变更」当满足任一：

- 加 cache / TTL / module-level Map
- `Promise.all` 并行化原串行 API
- 减少 `JSON.parse(JSON.stringify)` / 改 `ref` → `shallowRef`
- 合并/跳过 hydrate、resync、rehydrate 调用
- 改 `detail.vue` 加载顺序「为了快」

### 性能 PR MUST：

1. **与功能/fix 分 PR** — 禁止同一 diff 既优化又改 merge/filter 语义（除非 regression 全绿且 issue 明确允许）
2. **声明动哪一层** — 仅取数层 / 还是语义层（语义层须先 failing test）
3. **跑 `npm run regression:mi`** — PR 描述贴单元 + 截图通过结果（截图路径列表）
4. **对照 invariant I1–I7** — 逐项勾选
5. **截图 MUST 保留** — `verification-screenshots/` 内 PNG 禁止验证后删除

### 性能 PR MUST NOT：

- 删除或绕过 `isInitiatorMyRequestView` / `isMiDashboardSubTableBinding` 等 guard 以「少分支」
- 用「全量 merge 一次搞定」替代按 binding 类型分支
- 在语义函数里 early return 跳过 viewContext 判定
- 缓存未含 viewContext 的 hydrate 结果

## 缓存 key 模板

```typescript
// ❌ 禁止 — 发起人与办理人共用
const key = `${processDefinitionKey}:${formId}`

// ✅ 必须含语义维度
const key = `${processDefinitionKey}:${formId}:${viewContext}:${bindingId}:${bindingKind}`
// bindingKind: mi-collection | file-only | shared-attachment | participant-scoped | plain
```

## 新 issue 闭环

每个 MI 相关 closed issue（#1435–#144x）MUST 至少增加以下之一，避免只写 rule：

- 纳入 `vitest.mi-regression.config.ts` 的单测 **且** 对应 `mi-regression-scenarios.mjs` 截图脚本，或
- 扩展现有 verify 脚本断言 + 截图 slug

## 参考

- 变更剧本：`change-playbook.mdc` §Performance Optimization
- MI 语义：`portal-mi-subtable-my-request.mdc`
- FK/PK / R12–R16：`form-preview-fk-pk-runtime.mdc`
- 截图：`frontend-screenshot-verification.mdc`
- 套件列表：`frontend/user-portal/vitest.mi-regression.config.ts`
