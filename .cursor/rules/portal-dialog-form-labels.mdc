---
description: 三前端弹窗/抽屉表单 label 必须一行完整显示、不折行、不被输入框遮挡
globs: frontend/{user-portal,admin-center,developer-workstation}/**/*.{vue,scss}
alwaysApply: false
---

# 弹窗表单 label 显示规范（user-portal / admin-center / developer-workstation）

三个前端应用内弹窗（`el-dialog`）/ 抽屉（`el-drawer`）的 `el-form-item__label` **必须一行完整显示、不折行**，字段后面的输入框 / 下拉框等**不得遮挡** label 文字。

## 不可违反

- **不要**在弹窗表单上写死会导致折行的 `label-width` 像素值。需要对齐时用 `label-width="auto"`，**禁止**固定像素。
- 全局修复已在下列文件生效（`.el-dialog` / `.el-drawer` → `width:auto !important` + `white-space:nowrap`）；**不要删除或反改**：
  - `frontend/user-portal/src/styles/index.scss`
  - `frontend/admin-center/src/styles/index.scss`
  - `frontend/developer-workstation/src/styles/index.scss`
- 微调 label 时改 `.el-form-item__label` 的 `width`（用 auto），**不要**给 `.el-form-item__content` 加 margin 兜底。

## 完整规范与验证

见 skill **`portal-dialog-form-labels`**。改可见弹窗 UI 后按 `verify-ui-fix-with-screenshot` 截图，存对应 `frontend/<app>/verification-screenshots/`。
