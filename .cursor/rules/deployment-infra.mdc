---
description: Docker 构建、部署脚本与基础设施配置规范
globs: "deploy/**,**/Dockerfile*,**/docker-compose*,**/*.yaml,**/nginx.conf"
alwaysApply: false
---

# 部署与基础设施规范

## Docker 构建红线

| 规则 | 说明 |
|------|------|
| 不使用多阶段构建 | 本地构建 + 复制产物到镜像 |
| 前端用 `Dockerfile.local` | 不是默认 `Dockerfile` |
| `.dockerignore` 不含 `dist` | 否则前端产物被忽略 |
| 后端基础镜像 | `eclipse-temurin:17-jre-alpine` |
| 非 root 运行 | 容器用户: `platform` |
| 文件换行 | `.sh` 和 `.sql` 必须 LF（非 CRLF） |

## nginx envsubst 陷阱

各前端应用（`admin-center` / `user-portal` / `developer-workstation` / `login`）的 `docker-entrypoint.sh` 已统一为 **`KONG_PROXY_URL`**（代理 `/api/*` 至 Kong）。示例：

```sh
# 正确 — 显式指定变量列表（与仓库 frontend/*/docker-entrypoint.sh 一致）
envsubst '${KONG_PROXY_URL}' < /etc/nginx/conf.d/default.conf.template > /etc/nginx/conf.d/default.conf

# 错误 — 会替换 nginx 内置的 $host, $uri 导致 502
envsubst < template > default.conf
```

## 服务 Healthcheck

| 服务 | context-path | Healthcheck 路径 |
|------|-------------|-----------------|
| workflow-engine | `/` | `/actuator/health` |
| admin-center | `/api/v1/admin` | `/api/v1/admin/actuator/health` |
| developer-workstation | `/api/v1` | `/api/v1/actuator/health` |
| user-portal | `/api/portal` | `/api/portal/actuator/health` |

## API 边缘与前端代理

- **默认路径**：浏览器 → 前端 **nginx** → **`KONG_PROXY_URL`（Kong）** → 各后端（`deploy/kong/`、`deploy/k8s/kong.yaml`）。
- **例外**：可按环境让 nginx **跳过 Kong、直连后端**（仅本地/特殊联调）；与 `project-context.mdc` 中「前端 ↔ 后端映射」表一致。

## 不部署的组件

- PostgreSQL — SIT/UAT/PROD 使用公司现有数据库

## Schema 管理（单一来源）

**唯一 schema 来源 = `deploy/init-scripts/00-schema/`**（快照式 DDL + 增量编号脚本）。

| 规则 | 说明 |
|------|------|
| 新增/改表 | 在 `deploy/init-scripts/00-schema/` **新建**递增编号 `.sql`（禁止改已有文件，见 `init-scripts-append-only.mdc`） |
| Flyway 已清退（2026-06） | 后端 **不再有** `db/migration`；历史归档于 `docs/legacy-flyway-migrations/` |
| 禁止双轨 | **不要**再写 Flyway 迁移，也 **不要**做「Flyway ↔ init-scripts 双轨同步」 |
| Dev Compose | `docker-compose.dev.yml` 三服务 `SPRING_FLYWAY_ENABLED=false`；Postgres 首次启动跑 `00-init-all.sh` |
| 改 schema 后验证 | 删卷重建 Postgres（`dev_postgres_dev_data`）或按 `BUILD_GUIDE.md` 手工迁移；读 `docker logs platform-postgres-dev` 确认 init 无 ERROR |

### init-scripts 编写注意

- **只增不改（强制）**：禁止编辑已有 `.sql`；任何 DDL/DML/种子变更 **必须新建** 递增编号脚本。详见 [init-scripts-append-only.mdc](init-scripts-append-only.mdc)。
- **`.sh` / `.sql` 必须 LF**（CRLF 会导致 Linux 容器 init 失败）。
- **`init-scripts/` 根目录的宿主机脚本**（如 `superset-init.sh`、`superset-db-upgrade.sh`）会在 Postgres 首次 init 时被 entrypoint 自动执行——脚本内 **必须** 检测 `docker` 是否可用，容器内应 skip；仅设计为 **宿主机** `docker exec` 调用。
- **`dw_table_definitions` upsert**：约束为全局 `UNIQUE(table_name)`（V320）；seed 脚本用 `ON CONFLICT (table_name)`，**禁止** `ON CONFLICT (function_unit_id, table_name)`。
- 种子顺序与编排见 `deploy/init-scripts/00-init-all.sh`；失败时日志里的 ERROR 行才是真因（勿把 Postgres 未 healthy 误判为镜像拉取失败）。

## 第三方镜像拉取

Dev 部署脚本 `deploy/environments/dev/build-and-deploy.ps1` 会顺序 pre-pull compose 镜像。Docker Hub **未登录** 时易触发 rate limit。

| 镜像 | compose 引用 | 限流时的替代 |
|------|-------------|-------------|
| Activepieces | `activepieces/activepieces:0.84.0` | `docker pull ghcr.io/activepieces/activepieces:0.84.0` 后 `docker tag … activepieces/activepieces:0.84.0` |
| n8n | `docker.n8n.io/n8nio/n8n:latest` | 脚本会 fallback 到 `n8nio/n8n:latest` 并 retag |
| 其他 | 见 compose | `docker login` 或公司私有 registry（`deploy/scripts/mirror-thirdparty-images-k8s.ps1`） |

K8s 侧第三方镜像走私有 Nexus，见 `deploy/k8s/activepieces.yaml` 与各 manifest 的 `image:`。

## 构建部署流程

1. `mvn clean package -DskipTests`（8 模块全部 BUILD SUCCESS）
2. 每个前端: `npm install && npx vite build`
3. `docker compose up -d --build`
4. 等待所有容器 `healthy`
5. 冒烟验证核心功能

任何步骤失败必须立即修复，不允许跳过。

## Docker dev 与 K8s 配置同步

修改 `deploy/environments/dev/.env`、`docker-compose.dev.yml` 或后端 `application*.yml` 中的**环境变量/可配置项**时，必须在同一次变更中同步 K8s。

- **Checklist 与路径表**：[deploy/CONFIG_SYNC.md](../../deploy/CONFIG_SYNC.md)
- **Agent 强制规则**：[docker-k8s-config-sync.mdc](docker-k8s-config-sync.mdc)
- K8s 非敏感：`deploy/k8s/config_map/<Environment>/configmap-workflow-platform-config.yml`
- K8s 敏感：`deploy/k8s/secret/<Environment>/secret-workflow-paltform.yml`
