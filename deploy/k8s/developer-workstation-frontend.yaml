# Replace __INGRESS_HOST__ with your real ingress host before applying.
# This frontend is DEV-only in the original k8s folder; keep it isolated if you do not want it in SIT/UAT/PROD.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: developer-workstation-frontend
  namespace: __NAMESPACE__
  labels:
    app: developer-workstation-frontend
    component: frontend
    app.kubernetes.io/name: developer-workstation-frontend
    app.kubernetes.io/instance: developer-workstation-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: developer-workstation-frontend
  template:
    metadata:
      annotations:
        sidecar.istio.io/proxyCPU: 50m
        sidecar.istio.io/proxyCPULimit: 200m
        sidecar.istio.io/proxyMemory: 400Mi
        sidecar.istio.io/proxyMemoryLimit: 400Mi
        sidecar.istio.io/inject: "true"
        sidecar.istio.io/rewriteAppHTTPProbers: "true"
        proxy.istio.io/config: '{ "holdApplicationUntilProxyStarts": true }'
      labels:
        app: developer-workstation-frontend
        component: frontend
        app.kubernetes.io/name: developer-workstation-frontend
        app.kubernetes.io/instance: developer-workstation-frontend
    spec:
      imagePullSecrets:
      - name: nexus3
      containers:
      - name: developer-workstation-frontend
        image: nexus3.hk.hsbc:18080/hsbc-238092-cmbhase-bisp/workflow-station2/developer-workstation-frontend:__IMAGE_TAG__
        imagePullPolicy: Always
        env:
        - name: KONG_PROXY_URL
          valueFrom:
            configMapKeyRef:
              name: workflow-platform-config
              key: KONG_PROXY_URL
        ports:
        - containerPort: 80
          name: http
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /dev/
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /dev/
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: developer-workstation-frontend-service
  namespace: __NAMESPACE__
  labels:
    app: developer-workstation-frontend
spec:
  selector:
    app: developer-workstation-frontend
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP
---
apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: developer-workstation-frontend-destination-rule
  namespace: __NAMESPACE__
spec:
  host: developer-workstation-frontend-service
  subsets:
  - name: v1
    labels:
      app: developer-workstation-frontend
---
apiVersion: networking.istio.io/v1beta1
kind: Sidecar
metadata:
  name: developer-workstation-frontend-sidecar
  namespace: __NAMESPACE__
spec:
  workloadSelector:
    labels:
      app: developer-workstation-frontend
  egress:
  - hosts:
    - ./*
    - istio-system/*
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: developer-workstation-frontend-virtual-service
  namespace: __NAMESPACE__
spec:
  hosts:
  - __INGRESS_HOST__
  gateways:
  - workflow-platform-ingress-gateway
  http:
  - match:
    - uri:
        exact: /dev
    - uri:
        prefix: /dev/
    retries:
      attempts: 0
    route:
    - destination:
        host: developer-workstation-frontend-service
        subset: v1
        port:
          number: 80
    timeout: 30s