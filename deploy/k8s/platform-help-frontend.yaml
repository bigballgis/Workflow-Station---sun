# Replace __INGRESS_HOST__ with your real ingress host before applying.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: platform-help-frontend
  namespace: __NAMESPACE__
  labels:
    app: platform-help-frontend
    component: frontend
    app.kubernetes.io/name: platform-help-frontend
    app.kubernetes.io/instance: platform-help-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: platform-help-frontend
  template:
    metadata:
      annotations:
        sidecar.istio.io/proxyCPU: 50m
        sidecar.istio.io/proxyCPULimit: 200m
        sidecar.istio.io/proxyMemory: 400Mi
        sidecar.istio.io/proxyMemoryLimit: 400Mi
        sidecar.istio.io/inject: "true"
        sidecar.istio.io/rewriteAppHTTPProbers: "true"
        proxy.istio.io/config: '{ "holdApplicationUntilProxyStarts": true }'
      labels:
        app: platform-help-frontend
        component: frontend
        app.kubernetes.io/name: platform-help-frontend
        app.kubernetes.io/instance: platform-help-frontend
    spec:
      imagePullSecrets:
      - name: nexus3
      containers:
      - name: platform-help-frontend
        image: nexus3.hk.hsbc:18080/hsbc-238092-cmbhase-bisp/workflow-station2/platform-help-frontend:__IMAGE_TAG__
        imagePullPolicy: Always
        ports:
        - containerPort: 80
          name: http
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /help/
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /help/
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: platform-help-frontend-service
  namespace: __NAMESPACE__
  labels:
    app: platform-help-frontend
spec:
  selector:
    app: platform-help-frontend
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP
---
apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: platform-help-frontend-destination-rule
  namespace: __NAMESPACE__
spec:
  host: platform-help-frontend-service
  subsets:
  - name: v1
    labels:
      app: platform-help-frontend
---
apiVersion: networking.istio.io/v1beta1
kind: Sidecar
metadata:
  name: platform-help-frontend-sidecar
  namespace: __NAMESPACE__
spec:
  workloadSelector:
    labels:
      app: platform-help-frontend
  egress:
  - hosts:
    - ./*
    - istio-system/*
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: platform-help-frontend-virtual-service
  namespace: __NAMESPACE__
spec:
  hosts:
  - __INGRESS_HOST__
  gateways:
  - workflow-platform-ingress-gateway
  http:
  - match:
    - uri:
        exact: /dev/help/computed-fields
    redirect:
      uri: /help/computed-fields
  - match:
    - uri:
        exact: /help
    - uri:
        prefix: /help/
    retries:
      attempts: 0
    route:
    - destination:
        host: platform-help-frontend-service
        subset: v1
        port:
          number: 80
    timeout: 30s
