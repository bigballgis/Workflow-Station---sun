kestra:
  server:
    basic-auth:
      enabled: true
      username: admin@kestra.io
      password: kestra
  repository:
    type: postgres
  storage:
    type: local
    local:
      base-path: /app/storage
  queue:
    type: postgres
datasources:
  default:
    url: jdbc:postgresql://postgres:5432/workflow_platform_dev?currentSchema=kestra
    username: platform_dev
    password: dev_password_123
    driverClassName: org.postgresql.Driver
