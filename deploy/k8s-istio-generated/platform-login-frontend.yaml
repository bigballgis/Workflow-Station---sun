# Replace __INGRESS_HOST__ with your real ingress host before applying.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: platform-login-frontend
  namespace: __NAMESPACE__
  labels:
    app: platform-login-frontend
    component: frontend
    app.kubernetes.io/name: platform-login-frontend
    app.kubernetes.io/instance: platform-login-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: platform-login-frontend
  template:
    metadata:
      annotations:
        sidecar.istio.io/proxyCPU: 50m
        sidecar.istio.io/proxyCPULimit: 200m
        sidecar.istio.io/proxyMemory: 400Mi
        sidecar.istio.io/proxyMemoryLimit: 400Mi
        sidecar.istio.io/inject: "true"
        sidecar.istio.io/rewriteAppHTTPProbers: "true"
        proxy.istio.io/config: '{ "holdApplicationUntilProxyStarts": true }'
      labels:
        app: platform-login-frontend
        component: frontend
        app.kubernetes.io/name: platform-login-frontend
        app.kubernetes.io/instance: platform-login-frontend
    spec:
      imagePullSecrets:
      - name: nexus3
      containers:
      - name: platform-login-frontend
        image: nexus3.hk.hsbc:18080/hsbc-238092-cmbhase-bisp/workflow-station2/platform-login-frontend:__IMAGE_TAG__
        imagePullPolicy: Always
        env:
        - name: KONG_PROXY_URL
          valueFrom:
            configMapKeyRef:
              name: workflow-platform-config
              key: KONG_PROXY_URL
        ports:
        - containerPort: 80
          name: http
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /login/
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /login/
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: platform-login-frontend-service
  namespace: __NAMESPACE__
  labels:
    app: platform-login-frontend
spec:
  selector:
    app: platform-login-frontend
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP
---
apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: platform-login-frontend-destination-rule
  namespace: __NAMESPACE__
spec:
  host: platform-login-frontend-service
  subsets:
  - name: v1
    labels:
      app: platform-login-frontend
---
apiVersion: networking.istio.io/v1beta1
kind: Sidecar
metadata:
  name: platform-login-frontend-sidecar
  namespace: __NAMESPACE__
spec:
  workloadSelector:
    labels:
      app: platform-login-frontend
  egress:
  - hosts:
    - ./*
    - istio-system/*
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: platform-login-frontend-virtual-service
  namespace: __NAMESPACE__
spec:
  hosts:
  - __INGRESS_HOST__
  gateways:
  - workflow-platform-ingress-gateway
  http:
  - match:
    - uri:
        exact: /login
    - uri:
        prefix: /login/
    retries:
      attempts: 0
    route:
    - destination:
        host: platform-login-frontend-service
        subset: v1
        port:
          number: 80
    timeout: 30s