/** Align all Portal list pagers with Main Table Views. */
export const PORTAL_LIST_PAGE_SIZES = [10, 20, 50, 100] as const

export const PORTAL_LIST_DEFAULT_PAGE_SIZE = 20

/** Element Plus layout string — must stay identical across Portal lists. */
export const PORTAL_LIST_PAGINATION_LAYOUT = 'total, sizes, prev, pager, next, jumper'
