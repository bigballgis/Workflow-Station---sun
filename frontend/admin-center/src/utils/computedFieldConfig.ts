/**
 * Re-export shim — the canonical implementation lives in frontend/shared/src/computedFieldConfig.ts
 * (single source across developer-workstation / admin-center; edit it there, never fork here).
 */
export * from '@platform-shared/computedFieldConfig'
