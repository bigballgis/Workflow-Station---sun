# UI verification screenshots (Admin Center)

Playwright 截图输出目录。**验证后保留，不要删除。**

```bash
cd frontend && pnpm run verify:screenshot -- --app admin --url "http://localhost:3000/admin/..." --name my-feature
```

详见 `.cursor/rules/frontend-screenshot-verification.mdc`。
