export default {
  formEventsGuide: {
    pageTitle: 'Form events',
    crumb: 'Developer Workstation · Function Units · Form Design · Controls',
    intro:
      'Write scripts in two places: Edit on a selected control, and Form event on the Form tab. Jump to a task, paste the sample, then Save, Ok, and save the form. Samples use Purchase Request (help_pr / help_pr_line).',
    flowTitle: 'Order of work',
    flow1: 'Open Form Design and the form',
    flow2: 'For one control, select it on the canvas. For the whole form, open the Form tab and Form event',
    flow3: 'Click Edit (the badge shows how many functions exist)',
    flow4: 'Create an event (or pick a Form event name), write the script, click Save, then Ok',
    flow5: 'Save the form',
    jumpValues: 'Fill Requester',
    jumpRequired: 'Required when dropdown A',
    jumpVis: 'Hide a field',
    jumpErrors: 'Block Save',
    jumpDisabled: 'Lock a field',
    jumpOptions: 'Change options',
    jumpNotify: 'Form banner',
    jumpLookup: 'Filter Lookup',
    jumpChrome: 'Focus / label',
    jumpForm: 'Form event',
    whatTitle: 'Control events and Form event',
    whatBody:
      'A control event is a script on one selected control. A Form event is a script for the whole form, on the Form tab; the button still says Edit. Neither is a Table Design Formula. Use only the methods on this page — whole-form readonly and audit fields stay locked even if a script calls disabled(false). Samples use Purchase Request (help_pr). **Scenario** is the dropdown labeled Scenario (field `scenario`, options A / B / C). **Title**, **Cost center**, **Start date**, and **Need-by** are other fields on the same form — match names in scripts to labels in Preview or on the canvas.',
    openFigure: 'Purchase Request. Field properties: Edit dialog with Create on the left and the script editor on the right.',
    openTitle: 'Open the dialog',
    openBody:
      'For a control: select it on the canvas, then Edit. For the whole form: Form tab → Form event → Edit. Create picks a built-in event, then hooks, then Custom. Save writes the function; Ok keeps the list. Footer Cancel discards dialog edits.',
    openSample: 'button on the selected control; the dialog title is Edit',
    createSample: 'dropdown on the left of the dialog',
    saveSample: 'writes the function you are editing',
    okSample: 'keeps the event list on the control',
    canvasFigure: 'Purchase Request canvas. The Scenario dropdown (A / B / C), Title, Requester, Cost center, and Start date are on help_pr.',
    paramsTitle: 'Script parameters',
    paramsBody:
      'Every control event and hook receives the same names. Write the bare name (api, value, field, user) or the same thing on $inject ($inject.api). Do not treat user as a string.',
    paramsRare:
      'Rarely needed: formData and data (read snapshot; do not assign — use setValue), options (same as api), option (config, not business data), rule and self (generate rule), args (Upload or Tree payload). Form event uses that function’s signature; onChange still has field, value, and options.',
    paramInject: 'the inject bag. Bare names below are copied from it at the start of the script',
    paramApi: 'current form API (same object as options). Use this to read and write fields',
    paramValue: 'this control’s value after the event. On the Scenario dropdown (`scenario`) it is the chosen key, for example A',
    paramField: 'this control’s field name, for example scenario or request_title',
    paramUser: 'signed-in person as an object, or empty. Always write user && before a property',
    valuesTitle: 'Read and write values',
    valuesIntent: 'Fill Requester from the signed-in person. Use api.setValue on hook_load or change.',
    valuesBefore: 'Requester is empty or an old value.',
    valuesAfter: 'Requester shows the display name.',
    valuesNote: 'getValue only reads. Do not assign into api.form — use setValue. Names: request_title, requester, scenario, start_date, end_date, cost_center, notes, quantity.',
    valuesFigure: 'Purchase Request canvas. Requester and Title are on this form — setValue writes Requester.',
    apiGetValue: 'Read only; the Title box does not change',
    apiSetValue: 'Fill Requester from the signed-in display name',
    apiSetMany: 'Fill Requester and clear Cost center in one call',
    apiForm: 'Snapshot to read; assigning into it does nothing',
    requiredTitle: 'Make fields required',
    requiredIntent: 'Make Start date and Need-by required only when the Scenario dropdown is A. Use api.required on that dropdown’s change event.',
    requiredBefore: 'Those two fields follow the designer Required tick.',
    requiredAfter: 'Scenario dropdown A → both required and Save checks them; another value → optional again.',
    requiredNote: 'requiredStatus is true only after a script last called required(true). Pass one name or an array.',
    requiredFigure: 'Scenario dropdown, Start date, and Need-by on the same canvas. Required follows dropdown A.',
    apiRequiredOn: 'Force both required until required(false)',
    apiRequiredOff: 'Those two fields are optional again',
    apiRequired: 'A requires Start date and Need-by',
    apiRequiredStatus: 'must is true only after a script set Start date required',
    visTitle: 'Show or hide a field',
    visIntent: 'Take Cost center off the page (and off submit). Use api.hidden(true) on cost_center.',
    visBefore: 'Cost center and Notes show as designed.',
    visAfter: 'Cost center is gone and is not submitted.',
    visNote: 'display(true) CSS-hides Notes but still submits. Same methods on Card, Tabs, Collapse, and Row (that layout’s field name). display(true) means hide; display(false) means show.',
    visFigure: 'Cost center and Notes on the canvas. hidden removes Cost center; display only CSS-hides Notes.',
    apiHidden: 'Cost center is gone and not submitted',
    apiHiddenOff: 'Cost center is back on the page',
    apiDisplay: 'Notes is CSS-hidden; still submitted. display(true) means hide',
    apiDisplayOff: 'Notes is visible again. display(false) means show',
    apiHiddenStatus: 'If Cost center was removed, put it back',
    apiDisplayStatus: 'If Notes is CSS-hidden, show it again',
    errorsTitle: 'Block Save with a field message',
    errorsIntent: 'Stop Save until Title has a value. Use api.setFieldError on Title blur.',
    errorsBefore: 'No extra message; Save follows designer required.',
    errorsAfter: 'A message under Title; it blocks Save until clearFieldError.',
    errorsNote: 'beforeSubmit can also stop Save by returning false. An ERROR banner does not block Save.',
    errorsFigure: 'Form Preview: setFieldError shows a message under Title and blocks Save.',
    apiSetError: 'Message under Title until clearFieldError',
    apiClearError: 'Related: remove the setFieldError message',
    disabledTitle: 'Lock a field',
    disabledIntent: 'Lock Cost center when the Scenario dropdown is A. Use api.disabled on that dropdown’s change event.',
    disabledBefore: 'Cost center is editable (designer default).',
    disabledAfter: 'Scenario dropdown A → Cost center is grey; another value → unlocked. Save still works.',
    disabledNote: 'Whole-form readonly and audit columns stay locked — disabled(false) cannot unlock them. Script lock wins over linkage and designer disabled.',
    disabledFigure: 'Form Preview: Scenario dropdown A greys out Cost center; other values unlock it.',
    apiDisabledOn: 'A locks Cost center; other values unlock it',
    apiDisabledOff: 'Cost center is editable again (unless whole-form readonly or audit)',
    apiDisabledStatus: 'locked is true only if a script last locked Cost center',
    optionsTitle: 'Change Select options',
    optionsIntent: 'Replace the Scenario dropdown list at runtime. Use api.setOptions on Select, Radio, or Checkbox only.',
    optionsBefore: 'The designer option list.',
    optionsAfter: 'The dropdown shows only the list you passed.',
    optionsNote: 'clearOptions empties the list but does not clear the stored value. Script options win over linkage filters and the designer list.',
    optionsFigure: 'The Scenario dropdown on this canvas. setOptions replaces its list at runtime.',
    apiSetOptions: 'Scenario dropdown shows only option A',
    apiAddOption: 'Append one item C',
    apiRemoveOption: 'Drop option B; the stored value is unchanged',
    apiClearOptions: 'Empty list; the stored value is unchanged',
    apiResetOptions: 'Restore the designer list',
    notifyTitle: 'Show a form banner',
    notifyIntent: 'Warn above the form without blocking Save. Use api.setFormNotification.',
    notifyBefore: 'No script banner.',
    notifyAfter: 'A text-only WARNING banner is visible; Save still works.',
    notifyNote: 'uniqueId replaces a previous banner with the same id. An ERROR banner does not block Save — use setFieldError or beforeSubmit return false.',
    notifyFigure: 'Form Preview: WARNING banner above the fields; Save still works.',
    apiSetNotify: 'WARNING banner with uniqueId title-warn',
    apiClearNotify: 'Related: remove the banner with that uniqueId',
    lookupTitle: 'Filter a Lookup',
    lookupIntent: 'List only Active rows on this Lookup. Use api.setLookupFilter (field is that control’s Field name), then api.refresh.',
    lookupBefore: 'Designer filters and cascade only.',
    lookupAfter: 'Those filters AND the script conditions; the dropdown reloads.',
    lookupNote: 'If the current selection is not in the new results, the field is cleared. There is no Sub-Table refresh method.',
    lookupFigure: 'The form canvas. Lookup filter uses that control’s Field name, then refresh.',
    apiSetLookupFilter: 'AND-filter status Active, then refresh',
    apiClearLookupFilter: 'Remove the script filter; designer and cascade remain',
    apiRefreshLookup: 'Reload this Lookup’s dropdown',
    chromeTitle: 'Move focus or rename a label',
    chromeIntent: 'Put the cursor in Title after a check fails. Use api.setFocus on request_title.',
    chromeBefore: 'Designer labels and no extra focus.',
    chromeAfter: 'Title is focused.',
    chromeNote: 'setLabel is plain text, not HTML. getLabel reads the current label; resetLabel restores the designer label.',
    chromeFigure: 'Title and Cost center on the canvas. setFocus moves the cursor; setLabel changes the label text.',
    apiSetFocus: 'Cursor moves to Title',
    apiSetLabel: 'Cost center’s label reads Charge code (plain text)',
    apiGetLabel: 'Read the current Cost center label; the page is unchanged',
    apiResetLabel: 'Restore the designer label',
    userTitle: 'Current user object',
    userBody:
      'user is an object, or empty when nobody is signed in. Always write user && before a property. Each event reads the person who is signed in right now, including after a workspace switch.',
    userBuSample: 'current workspace business unit name on User Portal',
    userRoleSample: 'current workspace role name on User Portal',
    userSetSample: 'Fill Requester from the signed-in display name',
    userIdSample: 'account identifiers and mailbox',
    userIdsSample: 'workspace identifiers; names are the labels people see',
    userExtraSample: 'role name list, and interface language',
    hooksTitle: 'Hooks (every control)',
    hooksBody:
      'Create always lists these after the control’s own events. They are not a substitute for change: hook_value runs after the value changes. Put one function per hook.',
    hookLoad: 'hook_load — after this control’s rules are loaded. Fill Requester from user',
    hookMounted: 'hook_mounted — after this control is mounted. Hide Cost center when nobody is signed in',
    hookDeleted: 'hook_deleted — after this control’s rules are removed. Clear an error you had set',
    hookWatch: 'hook_watch — after this control’s rules change. Re-apply required from the Scenario dropdown value',
    hookValue: 'hook_value — after this control’s value changes. Same idea as change',
    hookHidden: 'hook_hidden — after display status changes. Clear Cost center’s error when it is hidden',
    hookTitleClick: 'hook_titleClick — when the label is clicked. Not click on the control body',
    formLevelTitle: 'Form event (whole form)',
    formLevelIntent: 'Run one script when any field changes. Open Form tab → Form event → Edit and put it on onChange.',
    formLevelBefore: 'No whole-form script. Each control has its own Edit list.',
    formLevelAfter: 'Scenario dropdown A requires Start date and Need-by and hides Notes unless A.',
    formLevelNote: 'The button still says Edit. Return false from beforeSubmit to stop Save. Save then Ok then save the form.',
    formLevelFigure: 'Form tab: Form event. Edit still opens the script list for the whole form.',
    formOnSubmit: 'Triggered when the form is submitted. formData is the payload; api is the form API',
    formOnReset: 'Triggered after the form is reset. api is the form API',
    formOnCreated: 'Triggered after the form component is initialized. api is the form API',
    formOnMounted: 'Triggered after the form component is mounted. api is the form API',
    formOnReload: 'Triggered after the form rendering rule is reloaded. api is the form API',
    formOnChange: 'Triggered when any component value changes. field is the name, value is the new value, options is the form API',
    formOnChangeScript: 'Dropdown A requires dates and hides Notes unless A',
    formBeforeSubmit: 'Returning false stops Save; the payload is still in formData',
    formBeforeFetch: 'Triggered before a remote data request is sent. config is the request; return false to cancel',
    failTitle: 'When it fails',
    failUser: 'If user is empty, user.activeBusinessUnitName throws unless you write user &&.',
    failPreview: 'Form Preview has no Portal workspace: activeBusinessUnitName and activeRoleName stay empty until User Portal.',
    failScript: 'A script error stops that event; the rest of the form still works.',
    failSave: 'Save then Ok then save the form, or the function is not stored.',
    failCustom: 'Do not invent event names that Create does not list — use Custom only when you know the control emits that name.',
    failReadonly: 'disabled(false) cannot unlock a whole-form readonly view or an audit column.',
    failOptions: 'setOptions, addOption, removeOption, clearOptions, and resetOptions apply only to Select, Radio, and Checkbox. clearOptions does not clear the stored value.',
    failLookup: 'setLookupFilter, clearLookupFilter, and refresh apply only to Lookup; there is no Sub-Table refresh.',
    failBlocks: 'setFieldError blocks Save until clearFieldError (or beforeSubmit return false). An ERROR banner does not block Save.',
  },
  formEventsBasic: {
    pageTitle: 'Basic controls — events',
    crumb: 'Developer Workstation · Function Units · Form Design · Controls · Basic',
    intro:
      'Palette group Basic. Each control below lists the events Create shows for that type (plus blur and focus on field controls). Shared parameters, api methods, hooks, and Form event are on How to write events. Scripts use help_pr / help_pr_line names. In the sidebar click the control name under Controls — there is no Event item.',
    flowTitle: 'Order of work',
    flow1: 'Open Form Design and drag a Basic control',
    flow2: 'Select it and click Edit',
    flow3: 'Create the event named in the samples, write the script, Save, then Ok',
    flow4: 'Save the form',
    inputTitle: 'Input',
    inputBody:
      'Single-line text. Create lists change, blur, focus. change fires when the value changes, when the control loses focus, or when the user presses Enter. value is the text. field is the column, for example request_title.',
    inputChange: 'change — after Title changes, copy the signed-in display name into Requester',
    inputBlur: 'blur — when Title loses focus. value is still the text. Show an error if it is empty',
    inputFocus: 'focus — when Title receives focus. Clear the error so the person can type',
    textareaTitle: 'Textarea',
    textareaBody:
      'Multi-line text. Create lists change, blur, focus. change fires when the value changes, when the control loses focus, or when the user presses Enter. value is the text. Example field: notes on help_pr_line.',
    textareaChange: 'change — value is the full text. Reject notes longer than 400 characters',
    textareaBlur: 'blur — when the box loses focus. Clear the error if notes are empty on purpose',
    textareaFocus: 'focus — when the box receives focus. Clear the error',
    passwordTitle: 'Password',
    passwordBody:
      'Masked single-line text. Create lists change, blur, focus. change fires when the value changes, on blur, or on Enter. value is the typed secret. Do not write it into other fields or logs.',
    passwordChange: 'change — value is the password text. field is this control’s name',
    passwordBlur: 'blur — clear the error when the box is left empty',
    passwordFocus: 'focus — clear the error when the person starts typing',
    inputNumberTitle: 'InputNumber',
    inputNumberBody:
      'Numeric stepper. Create lists change, blur, focus. value is a number (or empty). Example field: quantity on help_pr_line.',
    inputNumberChange: 'change — if quantity is below 1, write 1 back',
    inputNumberBlur: 'blur — require a number when the stepper is left empty',
    inputNumberFocus: 'focus — clear the quantity error',
    radioTitle: 'Radio',
    radioBody:
      'One option from a list. Create lists change, blur, focus. value is the selected option value. Example: the Scenario dropdown on help_pr / help_pr_line (A makes Start date and Need-by required). To replace the option list at runtime, use setOptions on How to write events.',
    radioChange: 'change — value is the chosen key. Same required rule as the Purchase Request demo',
    radioBlur: 'blur — ask for a choice when the Scenario dropdown is empty',
    radioFocus: 'focus — clear the Scenario dropdown error',
    checkboxTitle: 'Checkbox',
    checkboxBody:
      'Zero or more options. Create lists change, blur, focus. value is usually an array of selected keys. Use value && value.length to test “anything checked”. To replace the option list at runtime, use setOptions on How to write events.',
    checkboxChange: 'change — require Notes when at least one box is checked',
    checkboxBlur: 'blur — value is the array. Ask for at least one if the array is empty',
    checkboxFocus: 'focus — clear the error on this control',
    selectTitle: 'Select',
    selectBody:
      'Dropdown. Create lists change, removeTag, visibleChange, blur, focus. change: value is the selected key (or array in multi-select). removeTag: multi-select only; value is the removed tag. visibleChange: value is true when the dropdown opens, false when it hides. Example: Scenario dropdown A → required Start date and Need-by (help_pr / help_pr_line). To replace the option list at runtime, use setOptions on How to write events (Select, Radio, and Checkbox only).',
    selectChange: 'change — triggered when the binding value changes. Demo rule from Purchase Request',
    selectOptions: 'After: Scenario dropdown shows only option A (Select / Radio / Checkbox only)',
    selectRemoveTag: 'removeTag — triggered when a tag is removed in multi-select mode. value is the removed item',
    selectVisibleChange: 'visibleChange — triggered when the drop-down box appears or hides. value is true or false',
    selectBlur: 'blur — if the Scenario dropdown is still empty, show an error',
    selectFocus: 'focus — clear the Scenario dropdown error',
    switchTitle: 'Switch',
    switchBody:
      'On/off. Create lists change, blur, focus. value is the activeValue / inactiveValue you configured (often true/false). Example: hide Cost center when the switch is off.',
    switchChange: 'change — hidden(true) removes Cost center; hidden(false) brings it back',
    switchBlur: 'blur — clear Cost center’s error after the switch loses focus',
    switchFocus: 'focus — display(false) shows Cost center again (display(true) hides with CSS)',
    sliderTitle: 'Slider',
    sliderBody:
      'Numeric slider. Create lists change, blur, focus. value is the number (or a two-number array in range mode).',
    sliderChange: 'change — value is the thumb position. Show an error above 80',
    sliderBlur: 'blur — clear the error when the person leaves the slider',
    sliderFocus: 'focus — if there is no number yet, write 0',
    rateTitle: 'Rate',
    rateBody:
      'Star rating. Create lists change, blur, focus. value is the score number.',
    rateChange: 'change — require Notes when the score is below 3',
    rateBlur: 'blur — ask for a score if it is still empty',
    rateFocus: 'focus — clear the error on this control',
    datePickerTitle: 'Date',
    datePickerBody:
      'One date. Create lists change, calendarChange, panelChange, blur, focus. change: value is the picked date. calendarChange: the highlighted date in the calendar changed (not yet confirmed). panelChange: the year/month panel changed. Compare with end_date on help_pr.',
    dateChange: 'change — triggered when the binding value changes. Reject Start after Need-by',
    dateCalendarChange: 'calendarChange — triggered when the selected date in the calendar changes. Clear Start’s error while picking',
    datePanelChange: 'panelChange — fires when the date panel changes (year/month). Clear Need-by’s error',
    dateBlur: 'blur — require Start date if it is empty',
    dateFocus: 'focus — clear the Start date error',
    dateRangeTitle: 'DateRange',
    dateRangeBody:
      'Start and end dates in one control. Create lists change, calendarChange, blur, focus. value is usually [start, end]. You can copy them onto start_date and end_date on help_pr.',
    dateRangeChange: 'change — write the two dates onto Start date and Need-by',
    dateRangeCalendarChange: 'calendarChange — highlighted range in the calendar changed. Clear this control’s error',
    dateRangeBlur: 'blur — require a range if nothing is picked',
    dateRangeFocus: 'focus — clear the error',
    timePickerTitle: 'Time',
    timePickerBody:
      'One time of day. Create lists change, blur, focus. value is the picked time.',
    timeChange: 'change — clear the error once a time exists',
    timeBlur: 'blur — require a time if it is empty',
    timeFocus: 'focus — clear the error',
    timeRangeTitle: 'TimeRange',
    timeRangeBody:
      'Start and end times in one control. Create lists change, blur, focus. value is usually [start, end].',
    timeRangeChange: 'change — reject equal start and end',
    timeRangeBlur: 'blur — require a range if nothing is picked',
    timeRangeFocus: 'focus — clear the error',
    cascaderTitle: 'Cascader',
    cascaderBody:
      'Nested options. Create lists change, expandChange, removeTag, blur, focus. change: value is the selected path. expandChange: the expanded node changed; args holds the node payload. removeTag: multi-select only; value is the removed tag.',
    cascaderChange: 'change — triggered when the binding value changes. Clear the error when a path exists',
    cascaderExpandChange: 'expandChange — triggered when the expanded node changes. args is the original payload',
    cascaderRemoveTag: 'removeTag — in multi-select mode, triggered when a tag is removed. value is the removed tag',
    cascaderBlur: 'blur — require a path if nothing is selected',
    cascaderFocus: 'focus — clear the error',
    colorPickerTitle: 'ColorPicker',
    colorPickerBody:
      'Colour value. Create lists change, blur, focus. value is the colour string. Extra panel events that do not appear in Create are not listed here.',
    colorChange: 'change — clear the error when a colour exists',
    colorBlur: 'blur — require a colour if it is empty',
    colorFocus: 'focus — clear the error',
    uploadTitle: 'Upload',
    uploadBody:
      'File list. Create lists remove, preview, error, progress, exceed, blur, focus (not change). value may be the file list. args is the original payload: the file for preview/error, progress data while uploading. After a successful upload, the file URL belongs on the file object for submit — do not invent a second store.',
    uploadRemove: 'remove — triggered when a file is removed from the file list',
    uploadPreview: 'preview — triggered when clicking an uploaded file in the file list. args[0] is often the file',
    uploadError: 'error — triggered when file upload fails',
    uploadProgress: 'progress — triggered while the file is uploading. args holds progress data',
    uploadExceed: 'exceed — triggered when the limit is exceeded',
    uploadBlur: 'blur — clear the error when the control loses focus without a new file',
    uploadFocus: 'focus — clear the error when the control is focused',
    treeTitle: 'Tree',
    treeBody:
      'Tree. Create lists nodeClick, checkChange, nodeExpand, nodeCollapse, blur, focus. Do not add drag events unless they appear in Create. value is the node value or checked set. args is the original node payload. Example: write Cost center from the clicked node.',
    treeNodeClick: 'nodeClick — triggered when the node is clicked. value is the node value',
    treeCheckChange: 'checkChange — triggered when the check box is clicked. Require Notes when something is checked',
    treeNodeExpand: 'nodeExpand — event triggered when a node is expanded. Clear Cost center’s error',
    treeNodeCollapse: 'nodeCollapse — event triggered when a node is closed. args is the original node',
    treeBlur: 'blur — require Cost center if nothing was written from the tree',
    treeFocus: 'focus — clear the Cost center error',
    elTreeSelectTitle: 'TreeSelect',
    elTreeSelectBody:
      'Tree in a dropdown. Create lists change, removeTag, blur, focus. change: value is the selected node key (or array). removeTag: multi-select only.',
    treeSelectChange: 'change — write Cost center from the selected node',
    treeSelectRemoveTag: 'removeTag — triggered when a tag is removed in multi-select mode',
    treeSelectBlur: 'blur — require a node if Cost center is still empty',
    treeSelectFocus: 'focus — clear the Cost center error',
    transferTitle: 'Transfer',
    transferBody:
      'Two lists. The Hermes Transfer on Basic uses type transfer: Create lists change, blur, focus; value is the keys on the right. The stock Transfer (elTransfer) also lists leftCheckChange and rightCheckChange — those fire when the person ticks rows on that side; args is the checked keys. Use the names you actually see in Create.',
    transferChange: 'change — value is the right-hand keys. Clear the error when at least one item was moved',
    transferLeftCheck: 'leftCheckChange — triggered when the left list element is selected or unselected. args is the original payload',
    transferRightCheck: 'rightCheckChange — triggered when the right list element is selected or unselected. args is the original payload',
    transferBlur: 'blur — require at least one item on the right',
    transferFocus: 'focus — clear the error',
    editorTitle: 'Editor',
    editorBody:
      'Rich text. Create lists change, blur, focus. value is the HTML or text. Example field: notes.',
    editorChange: 'change — clear the Notes error when there is content',
    editorBlur: 'blur — require Notes if the editor is empty',
    editorFocus: 'focus — clear the Notes error',
  },
  formEventsExtend: {
    pageTitle: 'Extend and MI — events',
    crumb: 'Developer Workstation · Function Units · Form Design · Controls · Extend',
    intro:
      'Palette groups Extend and MI. Sub-Table and Link Form keep change, blur, and focus even without a Field name. Inline Form, Record Note, and Assignment Mode have no Field — Create lists click (then hooks). Shared api methods and Form event are on How to write events. In the sidebar click the control name under Controls — there is no Event item.',
    flowTitle: 'Order of work',
    flow1: 'Open Form Design and drag an Extend or MI control',
    flow2: 'Select it and click Edit',
    flow3: 'Create the event named in the samples, write the script, Save, then Ok',
    flow4: 'Save the form',
    subTableTitle: 'Sub-Table',
    subTableBody:
      'Grid of the bound sub-table (help_pr_line on Purchase Request). Create lists change, blur, focus. value is the row list after add, edit, or delete. Empty value means no rows. There is no refresh method for Sub-Table.',
    subTableChange: 'change — triggered when the binding value changes (the rows). Clear the error when at least one line exists',
    subTableBlur: 'blur — require at least one line',
    subTableFocus: 'focus — clear the error',
    inlineSubFormTitle: 'Inline Form',
    inlineSubFormBody:
      'Lays the bound SUB table’s designed form in place (no grid, no dialog). No Field on the rule. Create lists click, then hooks. Put row logic on the inner fields (for example the Scenario dropdown’s change on help_pr_line), not on this shell.',
    inlineClick: 'click — fires when the Inline Form is clicked. Clear Item’s error as a simple signal',
    linkFormTitle: 'Link Form',
    linkFormBody:
      'Opens another form in a dialog. Create lists change, blur, focus. value is the linked payload after the dialog returns.',
    linkFormChange: 'change — clear the error when the linked form returned a value',
    linkFormBlur: 'blur — ask the person to open the linked form if value is empty',
    linkFormFocus: 'focus — clear the error',
    lookupTitle: 'Lookup',
    lookupBody:
      'Search-and-pick. Create lists change, blur, focus. value is the selected key or display value from Lookup Config. Example: copy it into Cost center on help_pr. To filter rows at runtime, use setLookupFilter on How to write events (Lookup only; field is this control’s Field name).',
    lookupChange: 'change — write Cost center from the lookup value',
    lookupFilter: 'After: this Lookup lists only Active rows, then reloads',
    lookupBlur: 'blur — require a row if nothing is selected',
    lookupFocus: 'focus — clear the error',
    ownerTitle: 'Owner',
    ownerBody:
      'Creator or current assignee, from Owner source. Create lists change, blur, focus. value is the owner display name or id. Example: copy it into Requester.',
    ownerChange: 'change — copy Owner into Requester on help_pr',
    ownerBlur: 'blur — warn when Owner is empty',
    ownerFocus: 'focus — clear the error',
    recordNoteTitle: 'Record Note',
    recordNoteBody:
      'Notes panel for the hosting table. Display-only; no Field. Create lists click, then hooks. People add notes in the panel; this click is the shell, not each note.',
    recordNoteClick: 'click — fires when the Record Note panel is clicked. Clear Notes’ error as a simple signal',
    miAssignmentTitle: 'Assignment Mode',
    miAssignmentBody:
      'MI palette. Container for assignee / BU / Role fields; BPMN still owns assignment settings. No Field. Create lists click, then hooks. Put scripts on the child fields when you need their values.',
    miClick: 'click — fires when the Assignment Mode container is clicked',
  },
  formEventsLayout: {
    pageTitle: 'Layout and Auxiliary — events',
    crumb: 'Developer Workstation · Function Units · Form Design · Controls · Layout',
    intro:
      'Palette groups Layout and Auxiliary. These controls usually have no Field. Create lists click, then hooks. Tabs also lists tabClick, tabChange, tabAdd, and tabRemove when those names appear in Create. Shared api methods and Form event are on How to write events. In the sidebar click the control name under Controls — there is no Event item.',
    flowTitle: 'Order of work',
    flow1: 'Open Form Design and drag a Layout or Auxiliary control',
    flow2: 'Select it and click Edit',
    flow3: 'Create click (or a Tabs event below), write the script, Save, then Ok',
    flow4: 'Save the form',
    fcRowTitle: 'Row',
    fcRowBody: 'Grid row. Create lists click. value is usually unused; use api to touch fields on help_pr.',
    rowClick: 'click — fires when the row is clicked. Clear Title’s error',
    colTitle: 'Col',
    colBody: 'Grid column. Create lists click.',
    colClick: 'click — clear Requester’s error',
    elCardTitle: 'Card',
    elCardBody: 'Card wrapper. Create lists click. Hide or show this Card with hidden / display on How to write events, using this layout’s field name.',
    cardClick: 'click — display(false) shows Cost center (display(true) would hide it with CSS)',
    elTabsTitle: 'Tabs',
    elTabsBody:
      'Tab bar. click always appears. tabClick: a tab was selected. tabChange: activeName changed; value is the new name. tabAdd: the add button. tabRemove: the remove button.',
    tabsClick: 'click — the Tabs control was clicked',
    tabsTabClick: 'tabClick — triggered when a tab is selected',
    tabsTabChange: 'tabChange — triggered when activeName changes. value is the active tab name',
    tabsTabAdd: 'tabAdd — triggered when a new tab button is clicked',
    tabsTabRemove: 'tabRemove — triggered when the tab remove button is clicked',
    elTabPaneTitle: 'TabPane',
    elTabPaneBody: 'One tab panel. Create lists click.',
    tabPaneClick: 'click — clear Notes’ error',
    elCollapseTitle: 'Collapse',
    elCollapseBody: 'Accordion group. Create lists click. Do not add change unless Create lists it.',
    collapseClick: 'click — clear Title’s error',
    elCollapseItemTitle: 'CollapseItem',
    elCollapseItemBody: 'One accordion panel. Create lists click.',
    collapseItemClick: 'click — clear Cost center’s error',
    fcTitleTitle: 'Title',
    fcTitleBody: 'Section title. Create lists click. Different from hook_titleClick on a field label.',
    titleClick: 'click — write a short mark into Notes',
    htmlTitle: 'HTML',
    htmlBody: 'Static HTML block. Create lists click.',
    htmlClick: 'click — clear Title’s error',
    elDividerTitle: 'Divider',
    elDividerBody: 'Horizontal rule. Create lists click.',
    dividerClick: 'click — clear Requester’s error',
    elAlertTitle: 'Alert',
    elAlertBody: 'Banner. Create lists click.',
    alertClick: 'click — hidden(true) removes Cost center',
    spaceTitle: 'Space',
    spaceBody: 'Vertical gap. Create lists click.',
    spaceClick: 'click — clear Notes’ error',
    elButtonTitle: 'Button',
    elButtonBody: 'Auxiliary button. Create lists click. Typical place to fill Requester from user.',
    buttonClick: 'click — write the signed-in display name into Requester on help_pr',
    elTagTitle: 'Tag',
    elTagBody: 'Label chip. Create lists click.',
    tagClick: 'click — clear Cost center’s error',
    elImageTitle: 'Image',
    elImageBody: 'Static image. Create lists click.',
    imageClick: 'click — clear Notes’ error',
  },
}
