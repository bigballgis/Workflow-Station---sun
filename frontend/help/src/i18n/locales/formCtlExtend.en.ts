export default {
  extendFlowTitle: 'Order of work',
  extendFlow1: 'Open Form Design and drag this control from Extend',
  extendFlow2: 'Select it. Fill Sub Table Binding or Lookup Config on the right',
  extendFlow3: 'Save the form',
  extendFlow4: 'Preview. For a script, select the control, Edit, Create, Save, then Ok',
  extendFlow5: 'Save the form again after Ok',
  catalogExtendBody:
    'Labels below match this control’s Component props. Designer locale is English. Blank means the listed default unless the row says required.',
  subTable: {
    summary: 'Bound child grid (help_pr_line). Binding, Allow Add/Edit/Delete, List View columns.',
    crumb: 'Developer Workstation · Function Units · Form Design · Controls · Extend · Sub-Table',
    intro:
      'Grid of a bound SUB table. Attach the table in [[/table-bindings]] first. On Purchase Request this is help_pr_line. Differs from [[/form-ctl-lookup]] (search-and-pick one row) and Inline Form (the same SUB form in place, no grid).',
    whatTitle: 'What Sub-Table is',
    whatBody:
      'A grid of SUB table rows on the form. On Purchase Request that is help_pr_line. The person adds, edits, and deletes rows when those Allow switches are on. Event value is the row list after those edits; empty means no rows.',
    howBody:
      'Select the placeholder. Sub Table Binding: choose the SUB already attached in [[/table-bindings]] (help_pr_line). Tick Allow Add, Allow Edit, Allow Delete as needed. Compact cells keeps lookup and user cells on one line. Open List View to pick Table Columns and Extend Action (Link Form or Lookup columns). Save the form. Preview the grid.',
    propsFigure: 'Form Design. Sub-Table selected. Right panel: Sub Table Binding, Allow Add / Edit / Delete, Compact cells.',
    catBinding: 'Required. The SUB or ACTION table this grid shows. Blank: the canvas reads No Sub Table selected, and save is blocked.',
    catSelect: 'Dropdown placeholder on Sub Table Binding. Empty list reads No Sub Tables available — bind a SUB table in [[/table-bindings]] first.',
    catAllowAdd: 'On: people can add rows. Off hides Add. ACTION bindings omit this row.',
    catAllowEdit: 'On: people can edit rows. Off is view-only. ACTION bindings omit this row.',
    catAllowDelete: 'On: people can delete rows. Off keeps rows. ACTION bindings omit this row.',
    catCompact: 'On: lookup and user cells stay on one line. Off expands a detail block below them.',
    catFormDesign: 'Tab on a selected Sub-Table. The inner form for add/edit.',
    catListView: 'Tab on a selected Sub-Table. Columns people see in the grid, including Portal lists.',
    catColumns: 'List View. Tick which bound fields appear as columns.',
    catExtendAction: 'List View. Add a Link Form or Lookup column on the grid.',
    eventIntent: 'Block Save until help_pr_line has at least one line. Use api.setFieldError on change.',
    eventBefore: 'An empty grid can still Save unless designer required or this script runs.',
    eventAfter: 'A message under the grid when there are no rows; it blocks Save until a line exists.',
    eventNote: 'value is the row list. Create lists change, blur, focus. There is no refresh method for Sub-Table.',
    eventHint: 'change — require at least one line on help_pr_line',
    failBinding: 'Save is blocked until every Sub-Table has a binding. The toast is All Sub-Table placeholders must have a binding selected before saving.',
    failType: 'On a process or task form, the binding must be SUB or ACTION. A MAIN binding is rejected.',
    failSave: 'Event scripts need Save then Ok then save the form.',
  },
  lookup: {
    summary: 'Search-and-pick. Relation Table, Search / Display fields, Fixed Filters, cascade, Backfill View.',
    crumb: 'Developer Workstation · Function Units · Form Design · Controls · Extend · Lookup',
    intro:
      'Search-and-pick from a bound relation table. Differs from [[/form-ctl-select]] (fixed Options) and [[/form-ctl-sub-table]] (the whole child grid). Bind Field so setLookupFilter on [[/form-events#lookup]] can target this control.',
    whatTitle: 'What Lookup is',
    whatBody:
      'A search box that lists rows from a bound Relation Table. The person picks one row, or several when Allow multiple is on. The Field stores the selected key, or the Selected Display Field. Bind Field so setLookupFilter on [[/form-events#lookup]] can target this control.',
    howBody:
      'Select the Lookup. Set Placeholder (default Click to search) and Readonly if needed. Lookup Config: Relation Table, Search Fields, Display Fields, Selected Display Field. Add Fixed Filters only when rows must be pre-filtered. Derived / Cascade Field when this Lookup follows another. Save the form. Preview search-and-pick.',
    propsFigure: 'Form Design. Lookup selected. Lookup Config: Relation Table, Search Fields, Display Fields, Fixed Filters.',
    catConfig: 'Opens Relation Table, search/display fields, filters, cascade, Allow multiple, and Backfill View.',
    catRelation: 'Required data source. Placeholder Select relation table. Empty: No relation tables bound — bind a table on the form, or use Go To Designer.',
    catSearch: 'Columns typed into the search box. Blank: search has nothing to match.',
    catDisplay: 'Columns shown in the result list. Blank: the list has no extra columns.',
    catSelected: 'Field shown after a row is picked. Blank: the stored key is shown.',
    catFilters: 'Optional. Pre-filter rows before search. Add starts an empty row. Hint: Optional: pre-filter rows by field value (exact or fuzzy) before lookup search.',
    catFilterField: 'Filter column. Blank skips that row.',
    catFilterMatch: 'Exact, Contains, Starts with, or Ends with.',
    catFilterValue: 'Value for that match. Boolean shows True / False. Blank matches nothing useful.',
    catDerived: 'Optional parent Lookup this one follows. Placeholder Select a lookup field. Blank: this Lookup is independent.',
    catDerivedMode: 'Auto-fill writes this value from the parent row. Filter only narrows this list from Join Columns.',
    catJoins: 'When a parent is set: left from the parent Lookup, right from this Lookup. Match is Equals / Contains / Starts with / Ends with. Add starts a join row.',
    catMultiple: 'On: several rows. Off (default): one row.',
    catBackfill: 'On: picking a row writes other bound columns from that row. Off is the default.',
    eventIntent: 'Copy the Lookup value into Cost center. Use api.setValue on change.',
    eventBefore: 'Cost center is empty or an old value.',
    eventAfter: 'Cost center shows the Lookup value.',
    eventNote: 'Create lists change, blur, focus. Runtime filters: setLookupFilter then refresh on [[/form-events#lookup]]. If the current pick is not in the new results, the field is cleared.',
    eventHint: 'change — write Cost center from the lookup value',
    eventHintFilter: 'After: this Lookup lists only Active rows, then reloads',
    failField: 'Field that is not a Data_Table column cannot store the pick. setLookupFilter needs this Field name.',
    failRelation: 'Blank Relation Table: search has no rows. Save the form after you pick a table.',
    failSave: 'Event scripts need Save then Ok then save the form.',
  },
}
