/**
 * Re-export shim — the canonical implementation lives in frontend/shared/src/tableFkRuntime.ts
 * (single source across portal and developer-workstation; edit it there, never fork here).
 */
export * from '@platform-shared/tableFkRuntime'
