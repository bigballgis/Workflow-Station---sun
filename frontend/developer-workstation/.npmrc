# Windows + Node 22: pnpm's isolated node_modules layout breaks unplugin-auto-import's
# ESM/CJS resolution (it can't find hoisted deps through the symlink structure).
# Hoisting sidesteps this without affecting other frontends.
node-linker=hoisted
