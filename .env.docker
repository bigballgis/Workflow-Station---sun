# =====================================================
# Docker 环境配置 (.env.docker)
# 用于 Docker Compose 或多容器环境
# =====================================================

# Spring Profile
SPRING_PROFILES_ACTIVE=docker

# Server Port (Docker 容器内部统一使用 8080)
SERVER_PORT=8080

# Database Configuration (Docker 网络内)
SPRING_DATASOURCE_URL=jdbc:postgresql://platform-postgres:5432/workflow_platform
SPRING_DATASOURCE_USERNAME=platform
SPRING_DATASOURCE_PASSWORD=platform123

# Redis Configuration (Docker 网络内)
SPRING_REDIS_HOST=platform-redis
SPRING_REDIS_PORT=6379
SPRING_REDIS_PASSWORD=redis123

# Service URLs (Docker 服务名，容器内部端口 8080)
ADMIN_CENTER_URL=http://platform-admin-center:8080
WORKFLOW_ENGINE_URL=http://platform-workflow-engine:8080

# Security
JWT_SECRET_KEY=workflow-engine-jwt-secret-key-2026
ENCRYPTION_KEY=workflow-aes-256-encryption-key!

# Logging
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_COM_PLATFORM=INFO
