### False Positive Challenge List

| Function Name | Risk Code | Challenge |
|---------------|-----------|-----------|
| `TaskProcessComponent.java` | Second_Order_SQL_Injection | False Positive — all dynamic table names from hardcoded constants or pg_class.relname via quote_ident(); no user-supplied identifiers |
| `RelationTableDataServiceImpl.java` | Second_Order_SQL_Injection | False Positive — was real, now FIXED: quoteIdentifier() validated with SafeSqlIdentifier regex; table names checked before SQL construction |
| `PortalRelationTableController.java` | SQL_Injection | False Positive — pure pass-through controller; zero SQL construction, delegates all data operations to service layer |
| `PortalRelationTableServiceImpl.java` | Second_Order_SQL_Injection | False Positive — sanitizeIdentifier() with regex validation before all dynamic SQL; table/column names from designer metadata |
| `RelationTableDeployServiceImpl.java` | Second_Order_SQL_Injection | False Positive — all table/column names wrapped in quoteIdentifier() (PostgreSQL double-quote escaping); sources from designer metadata not user input |
| `TaskController.java` | SQL_Injection | False Positive — Flowable API calls + delegation only; no raw dynamic SQL construction anywhere in controller |
| `FunctionUnitManagerComponent.java` | Second_Order_SQL_Injection | False Positive — all jdbcTemplate SQL fully hardcoded with ? parameterized placeholders; zero table/column name concatenation |
| `RelationTableDataController.java` | SQL_Injection | False Positive — pure REST controller; delegates to RelationTableDataService, no SQL construction in controller layer |
| `ProcessController.java` | SSRF | False Positive — controller delegates to service layer; no RestTemplate/HTTP client usage in controller |
| `FunctionUnitAccessComponent.java` | SSRF | False Positive — all HTTP calls use fixed @Value(${admin-center.url}) base URL; Java URI normalization prevents host changes from path segments |
| `TaskController.java` | SSRF | False Positive — controller only; no HTTP client (RestTemplate) usage; all external calls delegated to service/clients |
| `WorkflowEngineClient.java` | SSRF | False Positive — internal service client with fixed @Value-injected base URL; only path segments are dynamic, host is fixed config |
| `ExitControllerClient.java` | SSRF | False Positive — file does not exist in codebase; scanner false alarm on non-existent class |
| `ExitController.java` | SSRF | False Positive — pure controller; no RestTemplate or HTTP client usage; delegates to service layer |
| `PermissionController.java` | SSRF | False Positive — pure controller; no HTTP client code; all operations through service layer |
| `VirtualGroupAccessComponent.java` | SSRF | False Positive — internal component with fixed @Value(${admin-center.url}) base URL; host fixed, path-only dynamic segments |
| `ProcessComponent.java` | SSRF | False Positive — pure internal service-to-service RestTemplate calls with fixed base URLs; no user-controlled host portions |
| `MemberController.java` | SSRF | False Positive — pure controller; no RestTemplate usage; delegates to service layer only |
| `AdminCenterClient.java` | SSRF | False Positive — internal client with fixed @Value(${admin-center.url}) base URL; all calls to admin-center with fixed host |
| `ApiDataController.java` | SSRF | False Positive — pure controller with zero RestTemplate references; no HTTP outbound calls |
| `RoleAccessComponent.java` | SSRF | False Positive — internal access component with fixed @Value(${admin-center.url}) base URL; no user-controlled hosts |
| `UserPermissionController.java` | SSRF | False Positive — controller with fixed @Value(${admin-center.url}) base URL for internal calls; no user-supplied full URLs |
| `ChangeHistoryController.java` | SSRF | False Positive — pure controller; zero RestTemplate references; no HTTP outbound calls |
| `ApprovalController.java` | SSRF | False Positive — pure controller; no RestTemplate usage; delegates all processing to service layer |
| `FileUploadController.java` | Absolute_Path_Traversal | False Positive — FIXED: triple defense in place — (1) rejects ../ \\ in filename, (2) startsWith() check after normalize(), (3) audit logging for blocked attempts |
| `DeploymentController.java` | SSRF | False Positive — pure controller; no RestTemplate usage; delegates to service layer |
| `DeploymentComponentImpl.java` | SSRF | False Positive — internal component with fixed @Value(${admin-center.url}) base URL; no user-controlled host portions |
| `TaskFormController.java` | SSRF | False Positive — pure controller; zero RestTemplate references; no HTTP outbound calls |
| `N8nExecutionController.java` | SSRF | False Positive — FIXED: SsrfProtection.validate(request.getWebhookUrl()) added at line 118 before all RestTemplate calls; blocks private IPs + DNS rebinding |
| `N8nTaskExecutor.java` | SSRF | False Positive — FIXED: SsrfProtection.validate(webhookUrl) added at lines 86 and 168 for both sync and async N8N webhook calls; blocks SSRF |
| `PortalRelationTableController.java` | SSRF | False Positive — pure controller; no RestTemplate usage; delegates to service layer |
| `AiGenerationController.java` | SSRF | False Positive — controller delegates to AiGenerationServiceImpl; no RestTemplate in controller; SsrfProtection applied in service layer (line 126 SsrfProtection.validate) |
| `UserDisplayNameService.java` | SSRF | False Positive — internal service with fixed @Value(${admin-center.url}) base URL; no user-controlled host portions |
| `WorkflowActorResolver.java` | SSRF | False Positive — utility class; zero RestTemplate references; no HTTP outbound calls |
| `PermissionRequestController.java` | SSRF | False Positive — pure controller; no RestTemplate usage; delegates to service layer |
| `DashboardController.java` | SSRF | False Positive — pure controller; zero RestTemplate references; no HTTP outbound calls |
| `TaskController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId() (66 occurrences fixed across 16 files); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `TaskController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `AuthController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `AuthController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `AuthController.java` | JWT_Sensitive_Information_Exposure | False Positive — AuthController is a pure REST controller; reads tokens from Authorization header but never constructs JWT claims; token generation is in JwtTokenServiceImpl/AuthServiceImpl |
| `AuthController.java` | Execessive_Data_Exposure | False Positive — LoginResponse.UserLoginInfo returns standard login data (userId, username, roles, permissions) needed by frontend for UI rendering; not excessive for a login endpoint |
| `AuthController.java` | Frameable_Login_Page | False Positive — Frameable_Login_Page is a server-level HTTP response header concern (X-Frame-Options/CSP frame-ancestors); individual controllers don't set frame policies — configured at SecurityConfig or nginx layer |
| `PoleController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `RelationTableViewController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `RelationTableViewController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `UserController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `ProcessDesignController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `ProcessDesignController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `ApproverController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `ApproverController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `DelegationController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `DelegationController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `PermissionController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `PermissionController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `DecisionDesignController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `DecisionDesignController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `RelationTableStructureController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `RelationTableStructureController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `DeploymentController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `DeploymentController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `ProcessController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `ProcessController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `RoleController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `DictionaryController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `DictionaryController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `RelationTableDataController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `FunctionUnitController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `FunctionUnitController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `MemberController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `MemberController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `UserBusinessUnitRoleController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `UserBusinessUnitRoleController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `BusinessUnitRoleController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `BusinessUnitRoleController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `ExitController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `ExitController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `PreferenceController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `PreferenceController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `IconLibraryController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `PortalRelationTableController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `SecurityManagerComponent.java` | Use_of_a_One_Way_Hash_Without_a_Salt | False Positive — SHA-256 hashPassword() used for data anonymization (ANON_ prefix IDs) and message integrity, NOT for password storage; password storage uses BCrypt; salt not needed for non-password hashing |
| `ActionDesignController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `LookupComponentController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `LookupComponentController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `VersionController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `VersionController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `FunctionUnitImportController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `FunctionUnitImportController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `N8nConfigController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `N8nExecutionController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `N8nExecutionController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `VirtualGroupController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `VirtualGroupController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `TableDesignController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `BiDashboardAssignmentController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `BiDashboardAssignmentController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `BusinessUnitController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `BusinessUnitController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `FunctionUnitWorkspaceAccessInterceptor.java` | Missing_HSTS_Header | False Positive — FunctionUnitWorkspaceAccessInterceptor is a Spring HandlerInterceptor for workspace access control; HSTS is a server response header configured in SecurityConfig (already fixed with hsts.enabled property gate), not in interceptors |
| `FormDesignController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `WorkflowAuditAspect.java` | Parameter_Tampering | False Positive — AOP aspect class, not a controller; audit interceptor reads parameters for logging only; no identity spoofing vector |
| `TableRelationController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `TableRelationController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `AiGenerationController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `AiGenerationController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `ProcessFormController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `ProcessFormController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `i18n-no-chinese-component.test.ts` | Unchecked_Input_For_Loop_Condition | False Positive — test file with hardcoded test data; Unchecked_Input_For_Loop_Condition only applies to production code processing untrusted user input; test loops run on fixed fixtures |
| `VirtualGroupRoleController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `VirtualGroupRoleController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `N8nCallbackHandler.java` | Unsafe_Object_Binding | False Positive — uses @Valid N8nCallbackRequest DTO for input; internal record.setStatus/setErrorMessage are tracking field updates on internally-fetched execution records, not user-controlled entity binding |
| `RelationTableBindingController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `RelationTableBindingController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `ActionDefinitionController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `ActionDefinitionController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `JwtTokenServiceImpl.java` | JWT_Sensitive_Information_Exposure | REAL — JWT tokens contain email, displayName, roles[], permissions[] in claims (lines 72-77); email and displayName are PII unnecessary for stateless auth; roles/permissions in JWT are standard but still classified as sensitive claims; consider removing email/displayName claims |
| `TaskAssignmentController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `ActionQueryController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `ConfigController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `ConfigController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `AiGenerationServiceImpl.java` | Privacy_Violation | False Positive — no email/phone/mobile PII found in log statements; service-level logging uses userId/taskId identifiers only; AI generation content is user-provided workflow data, not system-logged PII |
| `BiGuestTokenController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `BiDashboardRegistryController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `BiDashboardRegistryController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `SystemMonitorController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `SystemMonitorController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `LogController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `LogController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `BiRbacMappingController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `BiRbacMappingController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `NotificationController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `DeveloperPermissionController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `ExportImportController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `LinkFormComponentController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `LinkFormComponentController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `TaskFormController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `TaskFormController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `SubTableViewController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `SubTableViewController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `SecurityAuditController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
| `SecurityAuditController.java` | Unsafe_Object_Binding | False Positive — controller uses validated DTOs with explicit field mapping, not raw entity mass assignment; service-layer authorization guards protect sensitive field setters (role, active, permissions) |
| `AuthSsoExchangeController.java` | Frameable_Login_Page | False Positive — server-level HTTP response header concern; SSO exchange page is a programmatic API endpoint, not a user-facing login page; frame policies set at SecurityConfig/nginx layer |
| `AuthSsoExchangeController.java` | Parameter_Tampering | False Positive — standard Spring MVC controller with @RequestBody DTO; SSO code exchange with server-side validation; identity from SecurityContext |
| `AuthSsoExchangeController.java` | Execessive_Data_Exposure | False Positive — SSO exchange returns standard token/user info needed by consuming frontend; not excessive data exposure |
| `auth.ts` | Missing_HSTS_Header | False Positive — auth.ts is a frontend TypeScript API module; HSTS is an HTTP response header set by the server (Spring SecurityConfig or nginx), not applicable to client-side code |
| `auth.ts` | Client_HTML5_Store_Sensitive_data_In_Web_Storage | False Positive — FIXED: token storage migrated to httpOnly cookies; localStorage now only stores non-sensitive user profile data (userId, username); access/refresh tokens managed via Set-Cookie httpOnly headers |
| `AuthServiceImpl.java` | Frameable_Login_Page | False Positive — AuthServiceImpl is a backend service class; doesn't handle HTTP response headers; frame policies are set at SecurityConfig/nginx layer, not in service code |
| `ChangeHistoryController.java` | Parameter_Tampering | False Positive — all X-User-Id header reads replaced with SecurityContextUtils.getCurrentUserId(); identity from JWT not spoofable headers; standard Spring MVC @RequestParam/@RequestBody with validated DTOs |
