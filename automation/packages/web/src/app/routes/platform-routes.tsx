// HERMES FR-D2: the platform-admin console (billing / usage / infra / security /
// setup / users / analytics) is removed from the DW-embedded builder build.
// Platform administration lives in HERMES admin-center; the flow-running core
// (builder, runs, connections, pieces, variables) does not route through here.
export const platformRoutes: never[] = [];
