# HERMES: upstream had @activepieces:registry + NPM_TOKEN auth for publishing; removed (D13 hard fork, no publish).
save-exact=true
auto-install-peers=true
strict-peer-dependencies=false
legacy-peer-deps=true
