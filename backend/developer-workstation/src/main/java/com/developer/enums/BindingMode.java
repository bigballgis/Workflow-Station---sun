package com.developer.enums;

/**
 * 表单表绑定模式
 */
public enum BindingMode {
    /**
     * 可编辑 - 允许通过表单对绑定表数据进行增删改操作
     */
    EDITABLE,
    
    /**
     * 只读 - 仅允许查看绑定表数据，不可编辑
     */
    READONLY
}
