spring:
  config:
    activate:
      on-profile: docker

# Messaging Configuration - Kafka disabled (not used in application)
app:
  messaging:
    enabled: false

logging:
  level:
    root: INFO
    com.portal: INFO
